## Packages
framer-motion | For smooth page transitions and micro-interactions
lucide-react | For beautiful iconography

## Notes
Authentication endpoints assumed: POST /api/register, POST /api/login, POST /api/logout, GET /api/user
Standard session-based auth pattern expected.
