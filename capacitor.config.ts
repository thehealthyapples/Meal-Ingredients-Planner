import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.thehealthyapples.app',
  appName: 'The Healthy Apples',
  webDir: 'dist/public',
};

export default config;
