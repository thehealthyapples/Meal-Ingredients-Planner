THA Replit Execution Rules — Cost Efficient / Do Not Deviate

• Follow this plan exactly
• Modify only the minimum code required
• Do not redesign basket architecture
• Do not change backend or DB
• Do not alter basket totals or comparison strip
• Do not create a second scoring system
• Reuse the existing THA scoring/filter concepts already in the codebase
• Do not change the fixed THA Rating logic
• Do not refactor unrelated code
• Do not generate tests
• Only run a TypeScript compile check
• Start dev server when finished
• I will manually verify behaviour

Objective

Rename the Basket pantry preference column from "UPF / Nasties" to "THA Optimizer" and make it context-aware.

Important product rule:
• THA Rating remains fixed and comes from the existing single THA scoring engine
• THA Optimizer affects recommendation/filter preference only
• Do not create a new scoring system

This pass is about:
1. renaming the column
2. reusing the existing THA filter/switch concepts already present in the app
3. making the visible optimizer options context-aware by product family
4. keeping the implementation lightweight

Do NOT fully wire a recommendation engine in this pass unless the existing wiring is already trivial.

--------------------------------------------------
Required behaviour

1. Rename the pantry preference column to:
   THA Optimizer

2. Keep this column before:
   Choice

3. Pantry rows should show a compact optimizer control
4. Non-Pantry rows should show —
5. The optimizer must only surface relevant options for the item/product family

Examples:
• Passata:
  show additive / UPF-related options
  do NOT show Bovaer
• Milk:
  may show Bovaer-related option(s)
• Eggs:
  may show free range / organic related option(s)
• Beef:
  may show grass finished / pasture raised related option(s)

--------------------------------------------------
T001 — Rename column and visible labels

File:
client/src/pages/shopping-list-page.tsx

Change the visible column/header name from:
• UPF / Nasties

to:
• THA Optimizer

Also update any visible row text/labels in this column to use the new terminology.

Acceptance:
• Basket column is labeled THA Optimizer
• No visible UPF / Nasties label remains in this column

--------------------------------------------------
T002 — Reuse existing THA filter/switch concepts

Files to inspect and reuse if already present:
• existing product analyser / THA filtering logic
• any existing switch/filter definitions already used in the app

Important:
Do NOT invent a completely separate set of optimizer rules if equivalent concepts already exist.

Goal:
Use the same existing THA concepts where possible, such as:
• UPF-related filtering
• acidity regulator exclusion
• emulsifier exclusion
• high-risk additive exclusion
• Bovaer-related exclusion
• organic / free range / grass-finished preference where relevant

This pass should reuse and surface the existing concepts, not duplicate them.

Acceptance:
• THA Optimizer options are based on existing THA concepts already present in the app where possible

--------------------------------------------------
T003 — Make optimizer options context-aware

File:
client/src/pages/shopping-list-page.tsx
(and a tiny helper in the same file or a small helper file only if clearly cleaner)

Implement a lightweight helper that determines which optimizer options are relevant for a given basket row.

Example behaviour:

For tomato/passata/sauce pantry products:
• relevant:
  - THA Safe
  - No UPF
  - No acidity regulator
  - No emulsifiers
  - No high-risk additives
• not relevant:
  - Bovaer
  - Free range
  - Grass finished

For dairy products:
• relevant:
  - THA Safe
  - No UPF
  - No high-risk additives
  - Bovaer Free (if dairy context)

For eggs:
• relevant:
  - Organic
  - Free Range
  - THA Safe
• not relevant:
  - Bovaer
  - Acidity regulator

For beef/meat:
• relevant:
  - Grass Finished
  - Pasture Raised
  - Organic
  - THA Safe

Requirements:
• keep this mapping simple and conservative
• if uncertain, omit the option rather than showing irrelevant controls
• do not attempt a full universal ontology in this pass

Acceptance:
• Passata does not show Bovaer-related options
• category/product-family relevant options are shown only where sensible

--------------------------------------------------
T004 — Keep the control compact

File:
client/src/pages/shopping-list-page.tsx

Render THA Optimizer as a compact selector/control suitable for a table row.

Do not show a giant list of raw switches in every row by default.

Preferred behaviour:
• compact trigger / select / summary label in the cell
• reveals the relevant options on interaction

If the current lightweight implementation already uses a compact Select, keep that approach.

Acceptance:
• THA Optimizer remains compact
• Basket row height does not materially increase

--------------------------------------------------
T005 — Preserve rating / logic boundaries

Important:
Do NOT change:
• THA Rating calculation
• Product scoring engine
• Basket Totals
• Comparison Strip
• Pantry merge logic
• non-pantry basket logic

Rules:
• THA Rating stays fixed
• THA Optimizer is a recommendation/filter preference control only
• Do not let optimizer settings change the intrinsic THA Rating

Acceptance:
• no second scoring system introduced
• THA Rating remains unchanged by optimizer selections

--------------------------------------------------
T006 — Minimal verification only

Steps:
• Run npx tsc --noEmit
• Start dev server
• Do not generate tests
• Do not run automated testing

--------------------------------------------------
Final acceptance

• Column is renamed to THA Optimizer
• Pantry rows show compact optimizer control
• Non-Pantry rows show —
• Optimizer options reuse existing THA concepts where possible
• Optimizer options are context-aware
• Irrelevant options (example: Bovaer for passata) are not shown
• THA Rating remains fixed
• No TypeScript errors