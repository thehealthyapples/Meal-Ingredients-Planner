#2 - Premium Hero — Kitchen to Basket What & Why Replace the current left-panel hero on the auth/landing page with a polished "Kitchen to Basket" split visual. Right now the panel shows a headline and a stock Unsplash photo. The new design tells the product story in one glance: THA learns what is in your kitchen and helps you build a smarter, healthier basket. Done looks like The left panel of the auth page shows the new headline and supporting copy: Headline: "Plan meals. Build smarter baskets. Make better choices." Sub-copy: "The Healthy Apples learns what's in your kitchen and helps you choose better ingredients and groceries with less effort." The stock Unsplash photo is replaced by a composed React/Tailwind "Kitchen → Basket" visual with three sections: Kitchen side: warm pantry-shelf cards (Rice, Pasta, Lentils, Oats) and a fridge row (Eggs, Milk, Butter), using rounded cards with soft shadows and calm spacing Connector: a subtle directional cue (arrow or line) bridging kitchen and basket, labelled "Your Kitchen → Smart Basket" Basket side: a compact panel showing 4 example rows (Free Range Eggs, Organic Passata, Greek Yoghurt, Olive Oil) each with a price and small apple-rating display using tha-apple.png A low-opacity apple watermark (tha-apple.png) sits absolutely positioned behind the hero text, decorative only, not distracting Layout is responsive: side-by-side composition on desktop, readable stacked on tablet, text-first on mobile The right-hand login/register form is completely untouched No backend routes, schemas, or unrelated pages are changed Out of scope Any changes below the hero on the auth page (login form, register form, forgot-password, resend flows) New pages or routes Backend changes of any kind Heavy animation libraries Changes to any page other than auth-page.tsx Tasks Update hero text — Replace the existing headline and supporting copy in the left panel with the new "Plan meals. Build smarter baskets." text and sub-copy. Keep the existing CTA pattern. Build KitchenToBasketVisual component — Create a new self-contained component (in client/src/components/) that renders the three-part kitchen → basket visual using React and Tailwind only. Pantry shelf cards, fridge row, directional connector label, and basket result rows are all plain Tailwind UI elements — no new libraries. Apple rating uses the existing tha-apple.png asset (already imported elsewhere via @assets/icons/). Wire visual + watermark into auth panel — Replace the Unsplash <img> block in the left panel with the new component. Add the apple watermark as an absolutely positioned <img> with low opacity and pointer-events-none. Ensure text remains readable. Keep the existing decorative blobs. Relevant files client/src/pages/auth-page.tsx:159-192 client/src/components/FiveApplesLogo.tsx client/src/assets/icons/tha-apple.png