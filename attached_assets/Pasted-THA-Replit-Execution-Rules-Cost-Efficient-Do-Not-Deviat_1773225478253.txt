THA Replit Execution Rules — Cost Efficient / Do Not Deviate

• Follow this plan exactly
• Modify only the minimum code required
• Do not redesign architecture
• Do not refactor unrelated logic
• Do not change backend logic
• Do not remove state unless clearly unused
• Do not generate automated tests
• Do not run test frameworks
• Only run a TypeScript compile check
• Start dev server when finished
• I will manually verify behaviour

Objective

Refine the basket table by merging Variant, Tier, and Shop into a single Choice column, and move Confidence into the Match column.

This is a UI/table presentation refactor only.
Do not redesign the category layout.
Do not change basket totals or comparison strip.
Do not change pantry logic.
Do not change the underlying basket decision logic beyond rendering the same controls inside a single Choice cell.

New target column order

Checkbox | Ingredient | Choice | Match | Qty | Price | THA Rating | Meal | Analyse | Edit | Delete

Definitions

Choice column contains:
• Variant
• Tier
• Shop

Match column contains:
• matched product
• confidence label/badge

--------------------------------------------------
T001 — Update table headers

File:
client/src/pages/shopping-list-page.tsx

Replace current headers with:

• Checkbox
• Ingredient
• Choice
• Match
• Qty
• Price
• THA Rating
• Meal
• Analyse
• Edit
• Delete

Rules:
• Remove standalone headers for Variant, Tier, Shop, and Confidence
• Keep Match, Price, and THA Rating guarded by hasPrices if that is how the current table works
• Keep Meal visible as its own column
• Keep Analyse / Edit / Delete as their own columns

Acceptance:
• thead matches the new target structure exactly

--------------------------------------------------
T002 — Build Choice cell from existing controls

File:
client/src/pages/shopping-list-page.tsx

For each row, replace the separate Variant / Tier / Shop cells with one Choice cell.

The Choice cell should render the existing controls compactly inside one td:

1. Variant display/control
2. Tier display/control
3. Shop display/control

Layout guidance:
• compact vertical stack or tight grouped layout
• preserve current functionality
• do not change the logic behind these controls
• use the existing row/category behaviour exactly as-is
• keep row height reasonable

Important:
• Do not remove any control logic
• Only move the existing UI into one shared Choice td
• If Variant does not apply, show "—"
• If Shop is unavailable because hasPrices is false, omit it cleanly
• Tier must remain editable
• Shop must remain editable where currently supported

Acceptance:
• Variant, Tier, and Shop no longer appear as separate columns
• all three are visible/useable inside Choice
• current behaviour still works

--------------------------------------------------
T003 — Move Confidence into Match cell

File:
client/src/pages/shopping-list-page.tsx

Remove the standalone Confidence td and merge its content into the Match td.

Match cell should now contain:
• matched product text/name
• confidence label/badge directly below or beside it

Layout guidance:
• matched product remains primary
• confidence remains visible but secondary
• keep compact
• do not lose current confidence text/badge behaviour

If there is no match:
• show existing no-match state
• confidence should remain blank or omitted appropriately

Acceptance:
• Confidence is no longer a separate column
• Confidence is visible inside Match
• Match remains readable

--------------------------------------------------
T004 — Update colgroup / fixed widths

File:
client/src/pages/shopping-list-page.tsx

Update the table colgroup and fixed widths to match the new table shape.

Recommended target widths:

• Checkbox: 28
• Ingredient: 220
• Choice: 240
• Match: 260
• Qty: 80
• Price: 100
• THA Rating: 120
• Meal: 90
• Analyse: 70
• Edit: 70
• Delete: 70

Rules:
• keep table-fixed
• keep widths stable
• remove only width rules that conflict with the new colgroup
• keep useful truncation / ellipsis / alignment classes

Acceptance:
• table width remains stable
• Choice and Match have enough space
• rows are more readable than before

--------------------------------------------------
T005 — Preserve current behaviour

Important:
Do NOT change:
• category sticky headers
• category supermarket/tier inheritance logic
• Mixed header logic
• Basket Totals
• Comparison Strip
• pantry-specific logic
• basket matching engine
• confidence calculation logic

This pass is about presentation only:
• merge three columns into Choice
• move Confidence into Match

Acceptance:
• no regressions in current basket behaviour
• table is visually calmer

--------------------------------------------------
T006 — Minimal verification only

Steps:
• Run npx tsc --noEmit
• Start dev server
• Do not generate tests
• Do not run automated testing

Manual verification will be performed by the user.

--------------------------------------------------
Final acceptance

• Column order becomes:
  Checkbox | Ingredient | Choice | Match | Qty | Price | THA Rating | Meal | Analyse | Edit | Delete

• Choice contains Variant + Tier + Shop
• Match contains matched product + Confidence
• Table remains fixed-width and readable
• Current basket logic still works
• No TypeScript errors