now i want the bakground of the modals to use the same back ground as the rest of the site - you are trying to make the modal opque to see the rest of the site - this is not what i have requested - add the orchard background to the modal as well - and make the menus in the modal opaque!

Got it. You want the modal to carry the orchard background inside it — same as the page — and then the widgets/panels inside the modal should be fully solid so they're readable on top of it. Two-part fix: orchard inside the modal panel, opaque cards inside.

Now I have what I need. The fix is:

DialogContent — give it the orchard image as its own background (same as the page), remove the blur
DialogOverlay — remove it entirely (no dark veil needed since orchard is inside the modal)
Cards inside modals — fully opaque via a CSS override so they're legible on top of the orchard
Now reduce the overlay to almost nothing (just a subtle border effect), and add the CSS override for fully opaque cards inside modals:

Now add the CSS rule that makes cards inside the modal fully opaque:

Let me also check that the Radix dialog selector will match correctly — [role="dialog"] is safer than the attribute selector:

Everything looks clean. Let me do a quick check on the final dialog state:

Changes are live via hot reload. Here's what now happens inside every modal:

The modal panel itself has the orchard background image set to background-attachment: fixed — this means the orchard appears to continue from the page behind, exactly aligned. When you open a modal, the green trees you see inside it will be the same orchard from the same position, creating a seamless look
The dark overlay (bg-black/30) dims the rest of the page slightly behind the modal, giving it focus
Cards and panels inside the modal are now fully opaque white via the CSS rule targeting [role="dialog"] .shadcn-card — so any summary cards, stat boxes, or list containers inside the modal are solid and readable
Open the week plan modal (or any modal) and you should see the orchard inside the panel, with solid white cards on top of it. Let me know if the balance needs adjusting.