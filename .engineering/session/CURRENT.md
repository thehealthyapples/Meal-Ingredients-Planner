<!-- ESR:ACTIVE -->
<!--
  Engineering Session Recovery — ACTIVE-SESSION DASHBOARD
  Keep the marker above as "ESR:ACTIVE" while ANY session below is live.
  Change it to "ESR:IDLE" only when every session has moved to the archive
  (INDEX.md) and the table is empty. The Claude Code hooks read this marker.
-->

# Active Sessions — Recovery Dashboard

This is the first file to read when recovering an interrupted session.
Find the active session, open its run file, and continue from **Next action**.

| Session ID | Stage | Rollback ID | Next action | Run file |
|---|---|---|---|---|
| `ENGINT2_Engineering_Knowledge_Graph` | Complete — committed (`72412052`), awaiting owner review | `rollback/ENGINT2-engineering-knowledge-graph-20260718` → `0efcfc7f` | Owner to review `docs/implementation/engineering/ENGINT2_ENGINEERING_KNOWLEDGE_GRAPH.md`. **Derived relationship reasoning over the EXISTING Developer Intelligence capability — 4,882 edges across 796 documents, each carrying its path:line, NONE stored. Created no capability, registry, index, database or source of truth; `capability-registry.ts` untouched and no verb added.** Built from an empirical study of the corpus, not assumed rules. **Three of the five requested relationships are NOT derivable and are refused with evidence: no release tags exist (900 tags, 750 rollback, 0 release), the WS<n> space collides across three schemes (~1369 'WS0' occurrences), and `Status:` has 317 values where `Complete` and `Investigation complete` mean opposite things.** Found: 2 architecture docs with no implementation (both declare themselves implementation-free — a validating result), 94 ungoverned reports, 285 investigations with no follow-through. Fixed 2 defects incl. a stale ENGINT1 typecheck claim. Verified 36/0 + ENGINT1 34/0 unbroken. | [runs/ENGINT2_Engineering_Knowledge_Graph.md](./runs/ENGINT2_Engineering_Knowledge_Graph.md) |
| `ENGINT1_Engineering_Intelligence_Foundation` | Complete — committed (`bc360ba5`), awaiting owner review | `rollback/ENGINT1-engineering-intelligence-foundation-20260718` → `7bfad50c` | Owner to review `docs/implementation/engineering/ENGINT1_ENGINEERING_INTELLIGENCE_FOUNDATION.md`. **Engineering Intelligence Phase 1–2: ACTIVATED the `developer` capability TIP1 declared and never built — created NO new capability, because `developer` already owned "repo + docs/ + SoT Register" and a rival would have given one body of knowledge two owners. Read-only over 796 documents (architecture · investigations · implementation · roadmap · protocols · releases) + git history, every answer carrying a path:line citation or an explicit gap. Isolated per §7/§4.2 behind FOUR independent locks; the household Companion is byte-unchanged and cannot reach it. Verified 34/0 against the live repo, 4 sibling suites green, no new tsc error.** Headline: the roadmap records NO per-workstream completion anywhere, so `completionRecorded` is permanently `false` — evidence returned, verdict refused. Open: roadmap status field? developer-plane deployment target (would need a Product Registry entry + `admin_audit_log` wiring)? | [runs/ENGINT1_Engineering_Intelligence_Foundation.md](./runs/ENGINT1_Engineering_Intelligence_Foundation.md) |
| `COMP_ACT2_Companion_Action_Surfacing` | Complete — awaiting owner review | `rollback/COMP_ACT2-companion-action-surfacing-20260718` → `7bfad50c` | Owner to review `docs/implementation/COMP_ACT2_COMPANION_ACTION_SURFACING.md` & decide commit + follow-ons. **Surfaced the 6 write verbs COMP_ACT1 bound as contextual Companion Action proposals — Planner Move, Planner Replace, Shopping Delete, Pantry Add, Pantry Remove, Diary Log — built in the EXISTING pure `buildActionProposals` from 7 new pass-through surface pointers. NO new capability, route, API, storage call, business rule or client view component; confirmation flow untouched; no existing action's behaviour changed. Verified: 93/0 companion-actions (+31), COMP_ACT1 22/0, gateway 64/0, typecheck clean (+4 INHERITED errors repaired), and all 6 verbs proposed LIVE through the real gateway against real household rows with every sufficiency refusal firing. Open: browser screenshots blocked on beta credentials; ~36 probe chat turns written to owner account thread 286 (0 action proposals) awaiting cleanup decision.** | [runs/COMP_ACT2_Companion_Action_Surfacing.md](./runs/COMP_ACT2_Companion_Action_Surfacing.md) |
| `AFI2_Planner_Ambient_Intelligence` | Complete — awaiting owner review | `rollback/AFI2-planner-ambient-intelligence-20260718` → `7bfad50c` | Owner to review `docs/implementation/AFI2_PLANNER_AMBIENT_INTELLIGENCE.md` & decide commit + follow-ons. **Extended Ambient Food Intelligence into the Planner — new `planner-batch-cook` recommendation ("cook one batch and it covers the week" for a meal planned on ≥2 days) through the EXISTING FI4/OD1/DEC1/PHASE5C pipeline (NO new pipeline, NO second engine, NO new nutrition knowledge, NO contested-domain computation, NO client change — planner already mounts the ambient surface). Household-specific, cited, actionable, calm (one/week). Reuses AFI1's already-resolved planned meals (no new read). Verified: 64/64 tests, API 200 w/ cited output, opportunity-engine typecheck-clean, 3 screenshots. Discovered `assembleHouseholdNutrition` broken at HEAD (blocked plant-diversity re-voicing → follow-on #1).** | [runs/AFI2_Planner_Ambient_Intelligence.md](./runs/AFI2_Planner_Ambient_Intelligence.md) |
| `HOUSE5_KITCHEN_EXPERIENCE` | **Complete** | `rollback/HOUSE5-KITCHEN-EXPERIENCE-20260717` → `7bfad50c` (tag `house5-kitchen-experience-wip-snapshot-7bfad50c`; wt-snapshot `3cc05f9f`) | **The second room designed: THE KITCHEN — the emotional heart / the recipe book by the window.** Same house as Arrival (Concept B · Kept House · Warm Hour · pressed apple · Companion apple · constant shell), **one move for its own personality: the window turns to the SIDE (E2, not Arrival's full E3 wall) so the light rakes *across the page* and the hero shifts from the VIEW to the FOOD on the shelf** — the whole brief is the Blueprint's own Cookbook row (§5.1: *"The recipe book by the window · E2 · warm side-light across the page · Shelf, recipe cards as objects · The well-thumbed page"*). Delivered: emotional philosophy · architectural blueprint · interior language · journey from Arrival · **desktop/tablet/mobile concepts in two views (the shelf + a recipe opened)** · production recommendations · **12 rules K1–K12 every future Cookbook feature must follow** (each cited to an owner — no second owner). Feels like: confidence · warmth · craftsmanship · preparation · gathering · creativity · calm · nourishment; **never** a database/dashboard/collection-of-cards/management-software (defence: *the food is the colour, the family's own book is the content*). 🟢 **6 viewport renders + 4 full-page; E2 sanity 6/6 clear (content never covers the window); Fraunces real.** Honours every COOK1 hard finding: **no "cooked" claim** (K5 — *"featured in your plans"*), **no favourite star** (K4), **season in the food not the room** (K7), **orchard a committed window not wallpaper** (K6 — retiring the live `card.tsx` defect). ⏱ **Timing:** COOK1 §15 gated Cookbook visual work behind *"P1 (Home) has run"* — the Arrival lock satisfied it, so this is EXP5-P2 on schedule. **No product source/data/hook/route/schema/migration/test changed;** `meals-page.tsx` · `index.css` untouched. Report: `docs/implementation/HOUSE5_KITCHEN_EXPERIENCE.md` (renders `docs/ui-audit/house5-kitchen/`; composition `scripts/north4-concepts/kitchen-experience.html`; harness `…/render-kitchen.ts`). | [`runs/HOUSE5_KITCHEN_EXPERIENCE.md`](./runs/HOUSE5_KITCHEN_EXPERIENCE.md) |
| `HOME_ARRIVAL_PRODUCTION_LOCK` | **Complete** | `rollback/HOME-ARRIVAL-PRODUCTION-LOCK-20260717` → `7bfad50c` (tag `home-arrival-production-lock-wip-snapshot-7bfad50c`; wt-snapshot `507e2123`) | **Design LOCKED and rendered production-ready — no further exploration.** The exploration line (NORTH4 → Emotional Interior → BRAND2 → COMP1) is over; this session **assembles the four agreed decisions into one Arrival composition** and renders it on both honest days at desktop · tablet · mobile. **Lock:** *Architecture* = NORTH4 Concept B (panoramic orchard behind glass · oak sill · calm plaster room · permanent Arrival viewpoint); *Interior* = **The Kept House** warmed by **The Warm Hour**, one grain of **The Linen Calm** (aged oak · matte plaster · worn-lip patina · **no props**); *Brand* = the prominent band **retired**, sole identity a **monochrome apple pressed into the plaster wall** (BRAND2 — tone-on-tone, discovered not displayed); *Companion* = the **single THA apple** carved into a **soft sage embossed button**, calm/idle, always available, never dominant (COMP1), voice a data-borne line on the wall. Preserved: hospitality before productivity · technology quieter as it improves · orchard the anchor · **arrival feels like coming home.** 🟢 **6 viewport renders (3 breakpoints × full + quiet day) + 4 full-page; collision 12/12 clear; Fraunces real.** ⚠ **Two build notes for the gated step:** the pressed apple is a *rendered spec* — rebuild from the room's real light model and re-judge on device (BRAND2 § 7); ship the Companion **calm carved emblem** now, hold the **aware light** behind the COMP1 § 320 amendment. Defect caught by measuring: the `file://` apple mask loaded **empty** (marks invisible — the BRAND2 floor at zero) → fixed by **data-URI mask** (contrast 13 → 99). **No product source, data, hook, route, schema, migration, or test changed;** `home-experience-page.tsx` · `index.css` · `FloatingAssistant.tsx` untouched. Report: `docs/implementation/HOME_ARRIVAL_PRODUCTION_LOCK.md` (renders `docs/ui-audit/home-arrival-lock/`; composition `scripts/north4-concepts/home-arrival-lock.html`; harness `…/render-arrival-lock.ts`). | [`runs/HOME_ARRIVAL_PRODUCTION_LOCK.md`](./runs/HOME_ARRIVAL_PRODUCTION_LOCK.md) |
| `HOME_FINAL_CONCEPTS` | Complete | `rollback/HOME-FINAL-CONCEPTS-20260717` → `7bfad50c` (tag `home-final-concepts-wip-snapshot-7bfad50c`) | Awaiting owner decision on which of five becomes the permanent Home. **Exploration only — product source byte-untouched; every concept injected in the browser on the live canonical `/home`, nothing edits app source.** 🟢 **THE HEADLINE: the last question before Home is locked — not *where* identity lives (BRAND1) or *the mark* (BRAND2), but how arriving Home should FEEL. Five genuinely different EXPERIENCES of the ONE canonical ARRIVAL1 room (orchard · arch · oak console · stone floor · layout all byte-untouched), each rendered at desktop 1440 + mobile 430 on a populated household.** **A The Signature Wall** (*made by hand* — banner dissolves to a single apple · the Pressed Apple beside the arch · "The Healthy Apples" routed into the oak console · the name in THA's hand; three scales of one signature) · **B The Family Home** (*you are cared for* — a lived-in golden morning · "Morning, Chloe" · a warm data-borne line of the day · the Companion foregrounded · a small "· a home, est. ·" plaque in the floor) · **C The Architect's House** (*composed emptiness* — NO mark at all, the architecture is the brand · the name re-set in a refined light editorial serif · cool museum daylight · more air; highest design ceiling and the coolest — the mission's named enemy) · **D The Orchard House** (*a window onto life* — the arch's one morning amplified into a luminous green-gold halo · the greeting steps back so the VIEW is met first · no mark, the orchard is the logo) · **E "The House That Was Expecting You"** (*you were expected* — the synthesis, tuned for the smile: warm first-light dawn · "Good morning," in THA's hand · the Pressed Apple + banner-apple · and the ONE new beat none of the others has — a single warm TRUE line of good news: *"The house is ready for you. The kettle's on, today's already planned — and the first thing worth knowing is that the greens are at their best."* — a friend telling you the one good thing about today, not a dashboard). 🔴 **RECOMMENDATION: E** — it wins the mission's only test (*the household should smile slightly every time they arrive Home*) without surrendering premium/calm/warmth; refuses the B-warmth-vs-C-composure choice by holding both; its warmth is RENEWABLE and honest (the line is different and true every morning) not decorative (C's pretty-room-admired-once risk); built almost entirely from already-blessed decisions (BRAND1 banner reform + BRAND2 canonical Pressed Apple + the room's own hand + D's one-morning light) plus ONE cheap new element. **B is the warmth-first fallback; C the timeless/cool fallback (correct only if Home is later allowed to read cooler than the Emotional Palette § 3A now permits — *"calm must never become lifeless"*).** 🔴 **The one honesty caveat recorded so E is buildable not mocked:** the good-news line must be **data-borne or dead** (Blueprint § 12.1), **one owner / one voice** (the Behaviour Engine's, verbatim — Home composes, never writes; NORTH1 § 5.7 / Notice Engine), **says less when there is less** (Core Principle 6 non-fabrication — *a home that invents good news is worse than one that stays quiet*), and **aims words never light** (the dawn/halo is a composition of the room's one existing morning, never a second sun — Blueprint § 6/§ 16; Household Time aims words & doors, never light). So built, E adds NO new data/store/route/capability/fabricated content. ✅ Method = the BRAND1/BRAND2 discipline. 12 renders captured via the real authenticated `/home` at `deviceScaleFactor 2`. **No code, schema, migration, route, component, token, or behaviour.** Report: `docs/implementation/HOME_FINAL_CONCEPTS.md` (+ renders under `docs/ui-audit/home-final-concepts/`, harness `scripts/capture-home-final-concepts.ts`). | [`runs/HOME_FINAL_CONCEPTS.md`](./runs/HOME_FINAL_CONCEPTS.md) |
| `SUP3_Support_Hub_Checkin` | Complete | `rollback/SUP3-support-hub-checkin-20260717` → `7bfad50ca198f2b86f6501a4f82d8ae41af9260b` | None — complete; awaiting review. **Implementation — 1 product file (`client/src/pages/admin-page.tsx`), presentation only: NO backend, NO new API/endpoint, NO new calculation, NO schema/route/migration, NO nav change.** 🟢 **THE HEADLINE: the Support Hub stopped being a *dashboard* and became a *check-in*.** SUP2 left four question-cards in a 2×2 grid of **equal visual weight** — four things competing for the eye whether or not anything is wrong. SUP3 replaces that grid with a single **executive summary** — the first and largest thing on the page — answering *"Is everything okay?"* in one line: **"THA is healthy today. No action required."** or **"Three things need your attention."** Beneath it, **ONLY the items that need attention are surfaced**; every healthy signal **quietly collapses** into the summary rather than holding a card. When all is well the page is one calm green line and nothing else. 🔴 **Each attention item = the brief's three parts: WHAT (short headline) · WHY it matters (one sentence on the household consequence) · WHERE to go (one recommended action linking DIRECTLY to the owning tool).** Ranked severity-first (real failure red › caution amber › honest "couldn't be checked" muted). 🔴 **Reuse, not rebuild — held precisely:** the SAME four EXISTING, read-only, `assertAdmin` GETs SUP2 read (publication integrity · knowledge backlog · learning queue · benchmark run-history), same query keys → TanStack **dedupes**; **computes no new health metric**, wires no endpoint, **never triggers** a benchmark/publish (reads history from disk only). **Dropped SUP2's "What has changed?"** activity feed — recent activity is informational and would compete with the summary (brief: *"Nothing else should compete for attention"*). 🔴 **UNKNOWN≠green (UI §14) throughout:** a signal that fails to load becomes an honest *"couldn't be determined"* item AND softens the summary to *"THA looks calm — but not everything could be checked… this isn't a clean bill of health"*; a green **"healthy today"** shows ONLY when every signal actually resolved clear. ✅ **typecheck: 0 errors in `admin-page.tsx`, 0 in any `client/src` file** (293 gate lines = 100% pre-existing `server/` sibling debt); **`vite build` exit 0** (3286 modules); **proved end-to-end** — real authenticated admin, both viewports, **0 console errors**, live real data (**"Three things need your attention"** · 4 domains failed publication [red] · 24 knowledge terms [amber] · benchmark partial 76.4 [amber] · learning queue empty → collapsed/silent); **healthy state proved via mocked responses** (→ "THA is healthy today." + zero items). **Disposable admin created AND deleted same run** (0 leftover `sup3-shot` verified); throwaway harness removed. **0 raw buttons** (all actions `Link`ed spans) → no adoption entry, 538 ceiling untouched; **no second owner**. ⚠️ **Flagged (carried from SUP2):** publication GET runs a full verification pass per visit — a cached `/api/health` is follow-on #1 (out of scope, backend forbidden). ⚠️ **NOT done (named):** cached health readout, progressive summary, an operator activity-log *History* home for the retired feed, banner "Overview"→"Check-in" nav rename (ADMIN1 QW-4). Report: `docs/implementation/SUP3_SUPPORT_HUB_CHECKIN.md` (+ desktop/mobile/healthy screenshots under `docs/implementation/assets/SUP3/`). | [`runs/SUP3_Support_Hub_Checkin.md`](./runs/SUP3_Support_Hub_Checkin.md) |
| `SUP2_Steward_Dashboard` | Complete | `rollback/SUP2-steward-dashboard-20260717` → `7bfad50ca198f2b86f6501a4f82d8ae41af9260b` | None — complete; awaiting review. **Implementation — 1 product file (`client/src/pages/admin-page.tsx`), presentation only: NO backend, NO new API/endpoint, NO new calculation, NO schema/route/migration, NO nav change.** 🟢 **THE HEADLINE: the Support Hub Overview became a true Steward Dashboard — the operator's calm first look — by SURFACING existing status the platform's tools already compute, and refusing everything that would build or fabricate.** SUP1 left the Overview as an *intro + 3 static "Engineering health" links*; SUP2 replaces that strip with four question-cards answering the brief's four questions, each **a simple status + (when appropriate) one action**: **How is THA today?** (publication-integrity `summary` → worst existing per-domain status + healthy count) · **What needs my attention?** (knowledge `backlog.outstandingReviews` + learning `recommendationCounts.pending` + domains needing attention → a ranked list of open queues, or "Nothing needs your attention") · **Can I safely release?** (latest benchmark `verdict` PASS/PARTIAL/FAIL + score, framed as the **last run** with timestamp so it never poses as current) · **What has changed?** (last knowledge release + last benchmark run). Page order is now **Orientation → State → Rooms** (ADMIN2 Part 2.1) — the SUP1 five tool groups unchanged beneath. 🔴 **The scope line, held precisely: "surface existing information only, no new health calculation, no backend, no new APIs."** Five **existing, read-only, `assertAdmin` GETs** reused (same query keys as the tool pages → TanStack **dedupes**); the dashboard **classifies nothing new** (surfaces the server's own domain `status`/`verdict`/counts, not a composite score), **wires no health endpoint** (ADMIN1's `buildOperationsStatus` stays a follow-on), and **never triggers work** — reads benchmark *history from disk*, never `POST benchmark/run`/`publish`/`snapshot`. 🔴 **UNKNOWN≠green (UI §14) throughout:** unloaded → "Checking…", failed/unrun → "Can't tell just now"/"No benchmark run yet" (muted, never green); colour spent only on a status the server reports; a **healthy card carries no action** (room stays calm). ✅ **typecheck: 0 errors in `admin-page.tsx`, 0 in any client file** (275 baseline = 100% pre-existing `server/` sibling debt); **`vite build` exit 0** (3286 modules); **proved end-to-end** — real authenticated admin, both viewports, all 4 cards render **real data** with **0 console errors** (captured: Needs attention · 7/23 domains healthy · Proceed with care · last run 76.4 · 2 things need attention). **Disposable admin created AND deleted same run** (0 leftover verified); throwaway harness removed. **0 raw buttons added** (all actions are `Link`ed spans) → no adoption entry; **no second owner** (OBS §7). ⚠️ **Flagged honestly:** the publication-integrity GET runs a full verification pass per Overview visit (same call the tool makes) — a cached `/api/health` is follow-on #1, out of scope. ⚠️ **NOT done (named, not dropped):** cached health readout, banner "Overview"→"Dashboard" nav rename (ADMIN1 QW-4), operator activity log, Operation Card pattern. Report: `docs/implementation/SUP2_STEWARD_DASHBOARD.md` (+ desktop/mobile/cards screenshots under `docs/implementation/assets/SUP2/`). | [`runs/SUP2_Steward_Dashboard.md`](./runs/SUP2_Steward_Dashboard.md) |
| `SUP1_Support_Hub_Foundation` | Complete | `rollback/SUP1-support-hub-foundation-20260717` → `7bfad50ca198f2b86f6501a4f82d8ae41af9260b` | None — complete; awaiting review. **Implementation — the ADMIN2 design's first, calm step. 2 product files, presentation only: NO backend, NO new API, NO new calculation, NO schema/route/data.** 🟢 **THE HEADLINE: the Admin console became the Support Hub — "the study off the hall" — by re-dressing ONE landing page, and refusing everything that would build.** `admin-page.tsx` rewritten: **"Support Hub"** header + a two-sentence intro (the arrival beat), an **Engineering health** orientation panel, and the 11 existing tools grouped into **Platform · Intelligence · Knowledge · People · Development**, each with a **collapsed-by-default "Learn more"** (canonical `ui/collapsible`, progressive disclosure by intent). The permanent `coming-soon` "Overview" stub is replaced by the intro+health panel (its natural purpose per ADMIN1/ADMIN2). Banner brand label **"Admin" → "Support Hub"** (one word) so the rename is coherent — **no nav item, route, icon, guard or behaviour changed**; every tool card links to its original route; `data-testid="admin-page"` and the `role!=="admin"→NotFound` guard are byte-identical. 🔴 **The one honest judgement call, made explicit: "surface existing Engineering Health information (no new calculations)" — but ADMIN1 proved THA has NO wired health endpoint** (`buildOperationsStatus()` zero callers, no `/api/health`), and the brief forbids backend/API. So the health panel **surfaces WHERE health is observable and links to the tools that own each signal** (Publication integrity · Runtime telemetry · Release readiness) — computing nothing and **fabricating no status** (Visual Trust §14: UNKNOWN≠green; status never poses as current). Wiring a real readout is named as follow-on QW-1/QW-2, out of scope. ✅ **typecheck: 0 errors in both changed files** (the 27 baseline "NEW" are 100% pre-existing `server/` sibling debt, none in my files); **`vite build` exit 0**; **screenshots captured** at desktop 1280 + mobile 390 against the live dev server via the Replit Chromium, using a **disposable admin created AND deleted in the same run** (0 leftover rows verified) — throwaway harness removed after use. **No duplicate functionality** (health links are navigation to existing pages, not a second implementation); **no second owner** (OBS §7 respected). ⚠️ **Deliberately NOT done, named not silently dropped:** the deeper ADMIN2 Operation-Card pattern, the one-nav-owner convergence (banner `ADMIN_NAV` still a divergent second list), one-benchmark-home, namespace renames, and a formal Admin→Support-Hub experience-map amendment — all listed as follow-on recommendations. Report: `docs/implementation/SUP1_SUPPORT_HUB_FOUNDATION.md` (+ desktop/mobile screenshots under `docs/implementation/assets/SUP1/`). | [`runs/SUP1_Support_Hub_Foundation.md`](./runs/SUP1_Support_Hub_Foundation.md) |
| `BRAND1_Brand_Constitution` | Complete | `rollback/BRAND1-brand-constitution-20260717` → `7bfad50c` (WIP tag `brand1-wip-snapshot-7bfad50c`) | Owner review. **Governing architecture document only — NO implementation, no duplication of existing architecture; one new Markdown doc + README index edit; product source byte-untouched.** Authored `docs/architecture/THA_BRAND_CONSTITUTION.md` — THA's **enduring identity**, the single statement of *who The Healthy Apples is*: **why THA exists · Our Promise · Our Character · Our Responsibility · what THA will never become · the Household First Principle · the Trust Test · the One Question before every release**, closing with **ten Constitutional Principles** every future feature must satisfy. 🟢 **It restates no rule — it references and quotes owners throughout** and owns only what none of them owns (*the enduring identity, assembled in one place*, + six instruments the concepts add up to: mission statement, consolidated *never become*, Household First Principle, Trust Test, One Question, ten Principles — each built *from* its layer owner and citing it). 🔴 **Positioned on two axes (§1, §11):** (1) **subordinate to `ARCHITECTURE_PRINCIPLES.md`** on the engineering law — bound by **Principle 2** (one owner per fact) + **Principle 6** (non-fabrication, *"Trust is the product … non-negotiable"*), which is *why* it references rather than restates; (2) **upstream of the four Experience Governance docs** on the identity axis (states the identity Experience Architecture/Language/UI/Blueprint each express a face of) — but **overrides none**: on any *rule* question the rule's owner prevails and the Constitution is corrected. 🔴 **Explicit boundary vs `THA_UI_ARCHITECTURE.md` §10/§14** — UI owns THA's **visual** identity (marks · logo · apple · imagery · Visual Trust); the Constitution owns **enduring** identity and touches no pixel. The **One Question** (*"Does this leave the household with less to carry, and could they trust everything it tells them?"*) + ten Principles sit **above** the existing gates as the identity check they each verify a part of, never replacing them. Referenced owners (quoted, not restated): the Promise (Exp Lang §4A-A) · Premium-as-care/not-exclusivity (Exp Arch §17/§17.12) · feelings + Emotional Palette (Exp Lang §3/§3A) · anti-patterns (§7) · voice (TIP3 §12.1) · honest-gaps (TIP3 §12.2) · vision (Blueprint §1.1/§1.4/§1.5/§17) · Household-before-technology (Kept Room §5.6). Indexed in `docs/architecture/README.md` (Platform Governance table + blockquote) at creation. ⚠️ repo-structure **3 FAILs — proven pre-existing sibling debt** (stray root `.txt`; loose files under `docs/implementation/`+`docs/investigations/`); my only changes (new doc + README edit) are both under `docs/architecture/`, no failing check touches them. ✅ No code, schema, migration, route, token, capability, string, or behaviour; no runtime code reads it. | [`runs/BRAND1_Brand_Constitution.md`](./runs/BRAND1_Brand_Constitution.md) |
| `ADMIN2_Support_Hub_Experience` | Waiting for User | `rollback/ADMIN2-support-hub-experience-20260717` → `7bfad50ca198f2b86f6501a4f82d8ae41af9260b` | Awaiting owner decision (Option A pattern-first vs B full re-skin; Admin→Support Hub rename; backend progress scope). **Design investigation only — NOTHING IMPLEMENTED, by instruction; product source byte-untouched (`git status` modified-list identical to session start).** Experience-design companion to ADMIN1: *what the Support Hub should feel like.* 🔴 **THE HEADLINE: the console is the one room that "left the house."** `/admin` opens on a generic card grid — *"Admin / Manage users, configurations, and platform settings"* (`admin-page.tsx:189-192`) — the exact *"cold, clinical admin-console temperature that leaves the house"* the Orchard House blueprint names as the thing to avoid (`ORCHARD_HOUSE:308`). Yet governance **already binds it the other way**: `THA_EXPERIENCE_BLUEPRINT.md:138` — *"Every realm, dialog, **admin surface**, and document is a room… inside the same house"*; the map assigns **Admin = "the study off the hall"**, exposure **E0**, no Living Detail (`:181`); `UI:127` — *"Calm Orchard governs… **administrative surfaces**. No surface is exempt as 'internal'"*; `LANGUAGE:453` — *"Admin is quieter and denser by necessity, but **never louder, never anxious, and never a second product**."* **So the brief's objective ("feel like THA, not a traditional admin console") is a RETURN to a law already in force, not a new invention.** 🔴 **The defining defect is SILENCE.** The two operations that mutate **canonical knowledge every household sees** — **Publish** and **Roll Back a Knowledge Release** (`admin-knowledge-review:738-745, :794-799`) — run with **NO confirmation, NO progress, NO spinner: a disabled button + a post-hoc toast.** The **longest-running op in the platform** — a Full benchmark across 10 households (`admin-benchmark-households:305-312`) — reports nothing but a spinner and **silently resets those households first, unconfirmed.** **Confirmation is INVERSELY correlated with consequence** (deactivating one Pick confirms; publishing canonical knowledge doesn't). Per the brief, silence is the defect — and for a room whose *"honesty is its warmth"* (`ORCHARD_HOUSE:307`), a silent op is the study going cold. 🟠 **Feedback uniform in mechanism, wildly uneven in application** — same `useMutation`+toast+disabled stack everywhere, yet toast usage **18→0** across the 13 pages, and **NO true progress reporting anywhere** (0 progress bars / percent / job-status polls repo-wide). 🟠 **Benchmark = the task-confusion case study the brief predicted** — appears on **5 surfaces**: two *different* pages both "run the Intelligence Benchmark" (vs your live household `admin-intelligence:473-481` and the 10 canonical households `admin-benchmark-households:305-312`), one view-only echo (`admin-observation` Tab 8), and **two separate hub cards** presented as unrelated (`admin-page:54-61, :70-77`) — no single obvious place to benchmark. 🟢 **Mostly a *presentation* fix, not a build** — the best patterns already exist in-codebase (`admin-recipe-sources` optimistic toggle w/ rollback `:121-142`; `admin-canonical-publication-integrity` clean re-verify + "Verified {timestamp}" `:273-277`). **7 deliverables:** Support Hub philosophy (the study off the hall — separated by *access*, not *architecture*; Home stays exclusively the household's per `LANGUAGE:348`) · 5-domain IA (ADMIN1's, given a threshold + arrival beat) · progressive-guidance standard (3 layers, the brief's 7 questions, **the guidance panel IS the confirm gate**) · long-running lifecycle (acknowledged→queued→running→progress→ETA→completed[/warnings/failed]→next; **UNKNOWN≠green** carried from ADMIN1) · operator-feedback standard · areas of confusion · prioritised roadmap (**QW-1** gate the 2 canonical mutations · **QW-2** benchmark lifecycle · **QW-3** re-dress hub as the study's threshold · **QW-4** one benchmark home). **The one canonical pattern = the "Operation Card"** — the operator twin of the Companion Card (`UI:80` one-owner-per-concern). ⚠️ Recommends **inheriting the Admin map row's character, NOT minting a new one**; a formal Admin→Support Hub rename is a governance amendment (`BLUEPRINT:377`) required *before* build. ✅ No code, schema, migration, route, component, or token — only 2 new Markdown files; every product-source change in the tree is pre-existing sibling debt, untouched. Report: `docs/investigations/admin/ADMIN2_SUPPORT_HUB_EXPERIENCE.md`. | [`runs/ADMIN2_Support_Hub_Experience.md`](./runs/ADMIN2_Support_Hub_Experience.md) |
| `INTLANG1_Intelligence_Language_Guide` | Complete | `rollback/INTLANG1-intelligence-language-guide-20260717` → `7bfad50c` (WIP tag `intlang1-wip-snapshot-7bfad50c`) | Owner review. **Governing architecture document only — NO implementation, no runtime change, no capability; one new Markdown doc + README index edit; product source byte-untouched.** Authored `docs/architecture/THA_INTELLIGENCE_LANGUAGE_GUIDE.md` — the permanent **per-moment word-craft of THA Intelligence**: the sentence THA speaks at each of the thirteen named moments (Welcome Home · Quiet moments · Planner suggestions · Pantry discoveries · Shopping guidance · Cookbook exploration · Celebrations · Micro-learning · Household achievements · Safety alerts · Honest uncertainty · Errors · Empty states), each across five faces (*ordinary app language · THA language · why it feels human · emotional objective · anti-patterns*), closing with **ten enduring principles** every future Intelligence feature must satisfy before release + the **Intelligence Language Check** gate. 🟢 **It restates no rule — it cites owners throughout** and owns only what none of them owns (*the word, at each moment*): subordinate to **TIP3** (conversation voice, prevails in conflict), non-overriding sibling of the **Experience Language** (feeling) and UI/Kept Room (look); non-fabrication spine cited to TIP1 §3/§4.3/§5.3, Notice Engine §0/§6/§9, the Discovery Principle firewall, and Core Principle 6. 🔴 **Two boundaries corrected against the mission's own wording before writing:** (1) **there is no `critical` notice tier and the doc coins none** — Notice priority is only high/medium/low copied verbatim from producers; a restriction conflict is an ordinary `shopping-opportunity` notice, and safety's licence to interrupt is **TIP3 §11.2 rule 3** (*recalls = the canonical immediate interrupt*), not a new tier (coining one trips Notice Engine §9's hard stop); (2) **no runtime dependency, no canonical string** — every §4 example sentence is an *illustration of the register*, never a string to paste; a household's real words come from their owners (Notice producer verbatim; Companion via INT17). Indexed in `docs/architecture/README.md` (Intelligence Governance table + blockquote) at creation, per INT17/INTA1 §4.1. ⚠️ repo-structure **3 FAILs — proven pre-existing sibling debt** (stray root `.txt` files; loose files under `docs/implementation/`+`docs/investigations/`); no failing check touches `docs/architecture/`, where the new doc lives, indexed. ✅ No code, schema, migration, route, token, capability, or behaviour. | [`runs/INTLANG1_Intelligence_Language_Guide.md`](./runs/INTLANG1_Intelligence_Language_Guide.md) |
| `ADMIN1_Admin_Console_Engineering_Health_Audit` | Waiting for User | `rollback/ADMIN1-admin-console-engineering-health-audit-20260717` → `10573dd2` | Awaiting owner decision (Option A surface-first vs Option B build-with-history). **Investigation only — NOTHING IMPLEMENTED, by instruction; product source byte-untouched.** 🔴 **THE HEADLINE: THA can see its Intelligence runtime in exquisite detail and is BLIND to its own production/engineering health.** `buildOperationsStatus()` (`server/lib/platform-status.ts:180`) — uptime/memory/DB reachability/dependency circuits/env config — has **ZERO callers** repo-wide; no `/api/admin/platform/operations` route exists; yet `PLATFORM_QUALITY_ARCHITECTURE.md` §2/§11 records it *"substantially closed (EWO-PRO1) … surfaced at /api/admin/platform/operations."* Written, called by nothing — the same authored-but-never-adopted class as P0's dead test chain. 🔴 **No `/health`/readiness endpoint anywhere** (external liveness monitoring impossible); every CI gate (141-suite `test` chain, typecheck, adoption, coherence, schema-coverage, build) is **ephemeral CI stdout with no operator surface** — the exact condition under which P0's suite rotted silently. 🟠 **Duplicated monitoring is real (the brief's core rule already has 2 violations):** companion health computed **3× from 2 disagreeing stores** (durable `platform_observations` vs volatile in-memory ring buffer); benchmark/intent-quality on **3 admin pages from 1 engine** — framework names ONE owner each (Companion Learning DB snapshot; benchmark run-history) and retires the orphan `/observability/companion`. 🟠 **Admin IA has 2 rival nav declarations that disagree** — hub cards (`admin-page.tsx`, 12) vs banner (`admin-banner.tsx`, 9, omits Dev World / Knowledge Review / CPI); **admin auth enforced 3 ways** (`assertAdmin` ×76 · 9 inline `role==="admin"` · `ProtectedRoute` checks role NEVER; `admin-recipe-sources` has no client guard) — nothing unguarded, but one decision in 3 copies. ✅ **13 admin pages, ~85 endpoints: NO orphaned page, NO obsolete page, NO duplicate page** — the defects are drift + blindness, not clutter. **The "Overview" hub card is a permanent `coming-soon` stub — the natural home for the dashboard.** Governance frame: dashboard+alerts = the **Operations layer of `PLATFORM_QUALITY_ARCHITECTURE.md`** (§1 spine → §8) + its §2 open "alerting" — NOT a new quality owner, NOT a 2nd telemetry store (OBS §7 forbids). **~60% of signals already have endpoints → fix is mostly surfacing.** Publication register = **23 domains + 3 cross-cutting** (verified; corrected a pass's "26"). LLM = OpenAI `gpt-4o-mini`, degrades to silent NoOp. **CI ≠ `release:check`** (CI omits adoption/deployment/packaging gates). ⚠️ repo-structure **3 FAILs — PROVEN pre-existing** (byte-identical with/without my file; loose `DEV_DIET_PATTERN_LAUNCH_FAILURE.md` + sibling strays); my report filed under `admin/` per §4. ✅ No code/schema/migration/route. Report: `docs/investigations/admin/ADMIN1_ADMIN_CONSOLE_ENGINEERING_HEALTH_AUDIT.md`. | [`runs/ADMIN1_Admin_Console_Engineering_Health_Audit.md`](./runs/ADMIN1_Admin_Console_Engineering_Health_Audit.md) |
| `INT19_Intelligence_Activation` | Complete | `rollback/INT19-intelligence-activation-20260717` → `10573dd2` | None — complete; awaiting review. **ACTIVATION only — the intelligence that already exists, connected. 2 client files, +~37 lines; NO engine, capability, schema, migration, `OPPORTUNITY_SOURCES` entry, resolver matcher, notice category, or route.** 🟢 **THE HEADLINE: activation meant mounting two already-built components through two doors the architecture already opened — and refusing everything that would *build*.** The mandate ("add no engine/capability/schema/behaviour") is the scope filter, and it excluded more of FI18 than it admitted. **QW1 — `<AmbientIntelligence surfaceKey="home">` mounted on Home** (mission §1/§5), at the foot of the room BELOW the doors so it never re-triggers the NORTH2/NORTH3 door-behind-nav collision. It is a MOUNT not a channel: composes the SAME `useFoodOpportunities` bundle Home already fetched (`:337`) and rendered nowhere (until now read only to aim the door's safety tier at `:481`) — TanStack dedupes, no new request/endpoint/ownership; renders **NOTHING for the 192/195 unanchored households** (quiet day stays quiet), a restriction-conflict `critical` auto-opens it. Mirrors the pairing the dashboard ALREADY ships (phrased Companion card + resolvable ambient panel) — not a second Notice Engine. **QW2 — `ShoppingIntelligencePanel` remounted in `WorkspaceAnalyserSheet`** (mission §3): the per-item food story (in-season · Cookbook meal support · connected foods · the one "simply better") that the workspace LOST in the legacy→workspace move and the legacy `/basket` dialog still shows; reads existing `/api/shopping/intelligence`, owns nothing, hides on honest absence. 🔴 **FI18's "the Domain Intelligence layer is mute" is STALE — subagent-verified:** the resolver already routes `food-intelligence:report` (BENCH4) + `:compare` (COMP1); `recommend`/`explain` are DELIBERATELY unreachable (would force the resolver to invent a slug — its own hard boundary forbids it), and nutrient/benefit Qs ("high in iron?") already route to `nutrition-knowledge`. So **QW4 correctly NOT done.** ⚠️ **QW3 NOT ACTIONABLE in-mandate** — the `CompanionSurfaceHints` contract has no pantry/shopping/diary pointer, so publishing there sends empty hints (adding pointers = new ownership); Cookbook's meal pointer is ALREADY published. **Every exclusion named with a reason** (QW3, QW4, PANTRO1/NTC-P3 producer, NUTRITION_CONTEXT editorial, NTC-P2 convergence, COMP1 UI) so none is silently dropped. ✅ **typecheck: clean HEAD + my edits = 0 errors (fresh worktree proof); neither edited file in any error** — dirty tree's 27 are 100% sibling debt. **adoption 2 failures (button ceiling 539/538, HNP1 orphan) present at the TAG with/without my edits** — my delta 0 raw buttons, 0 orphans. **`verify:publication` 🟢 Companion/Notice**; 5 reds pre-existing (4 domains), untouched. **Client+server build EXIT 0 (Vite 3286 modules ✓).** repo-structure 3 FAILs = sibling loose files; my report filed under `intelligence/`. 🔴 **Inherited blocking owner question (from FI18, unchanged): is the 10-food `NUTRITION_CONTEXT` ceiling deliberate? — 600/610 foods uncitable (Rule E1), the true ceiling, editorial not engineering, no mount raises it.** Report: `docs/implementation/intelligence/INT19_INTELLIGENCE_ACTIVATION.md`. | [`runs/INT19_Intelligence_Activation.md`](./runs/INT19_Intelligence_Activation.md) |
| `BRAND1_Architectural_Branding` | Waiting for User | `rollback/BRAND1-architectural-branding-20260717` → `7bfad50c` (WIP tag: `brand1-wip-snapshot-7bfad50c`) | Awaiting owner decision. **DESIGN EXPLORATION only — NOTHING implemented; the ARRIVAL1 room/arch/orchard/furniture are BYTE-UNTOUCHED.** How THA's identity becomes part of the *architecture* of the Home (built in, not placed on top). **10 branding concepts + 4 top-banner fates, all RENDERED on the live canonical `/home`** by injecting each treatment in the browser only (`scripts/capture-brand1-concepts.ts` — never edits app source); renders in `docs/ui-audit/brand1/`. 🔴 **THE FRAMING FINDING: THA's apple mark already CONTAINS a sunrise-over-a-path orchard scene — the exact image the ARRIVAL1 arch shows at architectural scale.** The arch *is* the apple, opened to the size of a home — so branding here is *revealing* an identity the room already embodies, not adding a logo; the most premium move is the most restrained (obeys "premium through restraint", "technology quietly disappears", "the household's own life is the only ornament"). **Concepts (loud→silent):** 1 no branding (the room is the brand) · 2 embossed apple in plaster · 3 embossed wordmark in plaster · 4 carved oak signature · 5 brass apple inlay (introduces a *metal* — a signature material) · 6 etched stone threshold · 7 revealed only by light · 8 architectural maker's mark (foundation stone — THA as *builder of homes*) · 9 keystone apple (the apple crowns the arch — load-bearing, the highest ceiling & highest risk) · 10 orchard-integrated (the orchard IS the logo → loops back to concept 1). Each analysed for emotional feeling · first-time UX · returning UX · strengths · weaknesses · long-term brand implications. 🔴 **The top banner is CHALLENGED as the LEAST architectural place identity can live:** A disappear (too far — strands cart/profile controls) · B dissolve · **C join the orchard (band → plaster wall)** · **D reform (long logo → a single quiet apple, wordmark/tagline retired)** — **recommends C+D together.** 🟠 **Genuine finding: the ARRIVAL1 floor is already furnished (doors + dashboard link), so the etched-stone-threshold mark competes with the door labels** — a reason to prefer wall/oak/arch over floor for THA's mark. **Recommends the maker's-mark + embossed-apple family** (identity built into plaster & oak, discovered not announced) with the keystone apple *prototyped but held* (never gild the arch's calm; concept 1 "no branding" is a legitimate destination, not a failure). Distinct from the sibling `BRAND1_Brand_Constitution` (that = the governing *enduring-identity* doc; this = how identity *expresses in the room*). ✅ No code/schema/migration/tests. Throwaway `client/public/_brand1-apple.png` (apple-icon copy for injection; nothing imports it). Rollback branch+tag at HEAD before work. Report: `docs/implementation/BRAND1_ARCHITECTURAL_BRANDING.md`. | [`runs/BRAND1_ARCHITECTURAL_BRANDING.md`](./runs/BRAND1_ARCHITECTURAL_BRANDING.md) |
| `ARRIVAL1_Definitive_Home` | Awaiting review | `rollback/ARRIVAL1-definitive-home-20260717` → `10573dd2` (WIP tag: `arrival1-wip-snapshot-10573dd2`) | Awaiting review. **`/home` IMPLEMENTED as NORTH5's definitive Home — the archway built.** Re-composed as a room: **one continuous plaster wall → a bounded oak console → a visible stone floor**, blending V2's orchard connection · V1's calm restraint · V3's oak materiality. 🔴 **Governance FIRST, as mandated: the smallest explicit amendment before code** — Blueprint § 6.2 **rule 4** admits a **plaster architectural aperture (the arch)** for Home's E3, distinguishing an *opening in the wall* (the orchard seen THROUGH the architecture) from the *decorative picture-frame* E2's *"never by a drawn frame"* forbids (the tension NORTH4 § 4.1 named, resolved in the orchard's favour, Home only; § 7/the sun needed NO amendment — the arch's crop keeps the flare out of frame). **The REAL orchard is adopted, not authored:** `ORCHARD.png` (the owner-supplied v2 North Star asset — apple trees, blossom, the oak gate) converted to a served `orchard.webp` (**3.1MB → 393KB** via ImageMagick), mounted through the ONE owner (`orchard-backdrop.tsx`) as a third governed shape `OrchardArch` — honouring Colin Clapson's 2026-07-17 ruling (*orchard is a brand asset, supplied separately & adopted through the owner*; no substitute authored, only a format conversion). Blossom crosses the arch reveal; a soft petal-light softens inside/outside (not a fake cut-out branch — no fabricated content). **The day's three facts (meals · shopping · plants) rest as ivory objects on lit oak**, each with its own contact shadow (V3 materiality); the one resolver-aimed action sits on the console; **the four doors sit on the stone floor**. 🟢 **wall → furniture → floor passes the hand test** — the plaster wall is continuous behind BOTH the arch and the console (cover the furniture, the wall is still there); the console is bounded (plaster visible L+R, even on mobile — 16px + rounding + shadow, so it reads as furniture, NOT a card stack and NOT a full-width band — the exact NORTH4-C portrait-death avoided). **Orchard MATERIAL palette (plaster/stone/oak/ivory + Home's green = leaf-in-shade hue 74) is SCOPED to `.home-arrival`** — no other room changes; the global `--primary` (hue 132) is byte-untouched (NORTH4 D1's platform-wide swap deliberately NOT taken — "no unrelated refactoring / changes meaning: no"). **ALL existing data/hooks/routes/APIs/behaviour + every state preserved** (populated · quiet/**unanchored** · loading · error · companion · primary action · nav) — presentation only, no server/`shared`/API/schema/migration touched. **Verified in the REAL authenticated /home** across desktop/tablet/mobile × populated/quiet/loading/error (12+ renders in `docs/ui-audit/arrival1-home/`); the unanchored default (192/195 households) reads as a calm counter, intentional & complete. ✅ **HOME2 resolver test 47/47** (in main suite; the only Home test, none excluded); **build clean**; adoption **80·2 — both failures PRE-EXISTING/sibling, provably not mine** (my 2 files add **0 raw `<button>`**; orphan `HouseholdNutritionPanel.tsx` is a sibling-session file). ⚠️ **Honest gaps:** two orchards in the product (arrival + 5 dialog/list `url()` bypasses still show the meadow — platform-wide convergence out of Home's scope); `OrchardOpenView` now dead (left in place, no churn); NORTH3 opens (header "Home", door/nav duplication, Caveat signature) unchanged. Rollback branch+tag at HEAD before work. Report: `docs/implementation/ARRIVAL1_DEFINITIVE_HOME.md`. | [`runs/ARRIVAL1_Definitive_Home.md`](./runs/ARRIVAL1_Definitive_Home.md) |
| `NORTH5_Home_Refinement` | Waiting for User | `rollback/NORTH5-home-refinement-20260717` → `10573dd2` (WIP tag: `north5-wip-snapshot-10573dd2`) | Awaiting owner decision. **Design exploration only — NOTHING IMPLEMENTED, by instruction; product source byte-untouched (`git status` modified-list identical to session start), no CSS written, no renders (NORTH4 rendered; this brief forbids CSS).** The direction is now **chosen, not searched**: the **archway** is Home's canonical architectural signature and the **orchard-derived palette** the correct material direction. NORTH5 refines that ONE vision into **three arrangements of the same room** — **V1 Elegant Entrance Hall** (calmest; tall slender arch, hall console; best on mobile — a hall is a portrait shape) · **V2 Connected Living Space** (wide arch; the floor runs THROUGH the arch and becomes the orchard's grass — a THRESHOLD, the orchard as the *next room*, "almost tangible" built) · **V3 The Family Hearth** (Concept C's materiality kept, its bands killed; a deep recessed hearth-arch glowing warm, a substantial oak island you stand at, the day as objects-on-oak). 🟢 **They are provably ONE family: eight shared elements** (the arch · one continuous plaster wall · the furniture principle · the floor · one-arch light · the orchard→interior material flow · the palette · the blossom threshold) inherited by all three; they differ on exactly ONE axis — *what the furniture is, and so what the room is for* (console/sideboard/island → light/next-room/warmth). 🔴 **The one new rule the mission demanded, made checkable: `wall → furniture → floor`, NEVER `upper-band → lower-band`** — the **hand test**: *cover the furniture; is the plaster wall continuous behind where it was?* If the top of the page is one material and the bottom another, it's a band and it failed. This kills NORTH4-B's forbidden bands while keeping NORTH4-C's materiality (real oak furniture holding objects with contact shadows, not a coloured lower rectangle). Every variation keeps the arch as the emotional focal point and nothing is ever placed ON the orchard. ⚠️ **Portrait is the decisive test** (NORTH4-C *died* on mobile — its counter "became B's idea"): each variation keeps ONE narrowed piece of furniture reading AS furniture (wall visible L+R, shadow up onto wall, base on floor) rather than a card stack; **V3's constraint is absolute — if the oak ever reaches both phone edges it has become C and failed.** **Governance carried from NORTH4 as the PATH TO BUILD, not re-litigated:** 🔴 **G1 — the arch is a drawn frame; Blueprint § 6 fixes Home at E3 "framed by composition, NEVER by a drawn frame" — choosing the arch canonical is NOT yet the § 6 amendment it needs (NORTH4 D2); required before any CSS.** G2 — the sun is upper-right vs § 7 (V1 crops it out/safest, V2's wide arch most exposed, V3 absorbs it as hearth-glow). G3 — the palette (0.01%-hue-band `--primary`, platform-wide, the strongest single move, assumed). G4 — serif vs Caveat signature. **Recommends V2** (delivers the orchard as architecture most completely — the threshold) **carrying V1's discipline (owns the quiet day — 192/195 unanchored) + V3's material honesty.** ✅ No code, no schema, no migration, no gates. Rollback branch+tag at HEAD before work. Report: `docs/implementation/NORTH5_HOME_REFINEMENT.md`. | [`runs/NORTH5_Home_Refinement.md`](./runs/NORTH5_Home_Refinement.md) |
| `P0_Food_Intelligence_Recovery` | Complete | `rollback/P0-food-intelligence-recovery-20260717` → `10573dd2` | None — complete; awaiting review. **FI18's P0.1–P0.5 implemented. 15 files · +297 / −2,092 — a recovery that deletes ~1,800 net lines and adds no capability.** 🔴 **THE HEADLINE: the mission's own constraints forbade the mission's own instruction, and that IS the finding.** "Repair" meant **build the feature** for both paths — the gateway is not 3 missing imports but a **closed 8-category taxonomy scoped for 4 categories that don't exist** (repair = NTC-P2: +4 categories, +3 producers, +3 fact kinds, +4 phrasing cases, refused by Notice Engine §9's *"any new notice category without a registered owner — stop"*); HHP2's repair = **register a capability + enrol a producer**, both explicitly forbidden. **STOPPED and escalated before touching code** (Bootstrap requirement); user approved **retire both**. **Nothing was protecting either** — gateway had **zero runtime importers**, `bindHouseholdHealthCapability` **never called**, consumer panel **zero importers**: **no user-facing change, because no household was receiving anything.** 🔴 **THE FINDING THAT OUTRANKS THE MISSION: `npm test` was ALREADY RED and dying at link 9 of an `&&` chain — 132 gates never executed. Before this session: 81 of 90. The platform's effective test suite was EIGHT TESTS.** Cause: **RM3 correctly retired a route (2026-07-15) → broke a stale text grep in TRUST1-S3A → killed the chain → silently disabled everything downstream for two days**, because a chain that stops early exits non-zero exactly like one that ran. **That is the mechanism behind every other finding here** — HHP2 could be "Complete" and wired to nothing because *the suite that would have objected was not running*. **Fixed — `npm test` now EXIT 0 with 141/141 executed and 0 failed; the suite went from an effective 8 tests to 141.** 🔴 **P0.3 was 20× larger than recorded: not 3 unregistered tests but 61 of 151 (40%) — including dietary restriction & allergy SAFETY gates** (`restriction-safety`, `vegan-vegetarian-hard-enforcement`, `profile-dietary-title-safety`, `plant-milk-vegan`). They pass; **nothing was checking that they still did**. **51 registered (chain 90 → 141).** 🔴 **P0.4 worse than recorded — the check was VACUOUS**: greped `notice-gateway.ts` for `cookbook-opportunity`, **absent from both files**, so it passed *by not looking*, over a module throwing `SyntaxError`. **CONV1 P10's "the gate is a text grep" at a second gate.** Removed, not replaced; NTC-P2 now an honest `knownGap`. 🟠 **P0.5 §13 corrected (status only, NO rule changed): *THA did not fail to build its Domain Intelligence layer — it built it, didn't connect it, then wrote down that it had never been built.*** 🔴 **Two FALSE claims in shipped source corrected** — `household-nutrition.ts` asserted *"HHP2 MADE THAT TRUE… registered in `OPPORTUNITY_SOURCES`"* **while diagnosing its own failure mode one paragraph earlier** (*"the comment asserting it had shipped is exactly what stops the next reader from checking"*); **the same false claim was made twice, five months apart, both present-tense**. ⚠️ **3 deviations declared, none silent:** panel **NOT deleted** (it's HNP1's, not the activation path, and the only consumer of a live route — deleting it = scope creep); the 3 named tests deleted not registered (subjects retired); **a SECURITY test edited** — flagged loudest, **provably neutral**: both live routes keep `assertAdmin`, the dropped clause guarded an **RM3-retired route**, and **a new assertion was ADDED** so it can't silently return unguarded. ✅ **typecheck 32 → 27 (five removed, ZERO introduced)**; `verify:publication` **Companion/Notice 🟢**; adoption **2 failures — both proven pre-existing on a clean worktree**. **Every "pre-existing" claim proven on a clean `git worktree` at the tag, never by mutating the dirty tree; sibling work (NORTH3/4, CONV1 P10) untouched.** ✅ No schema, no capability, no ownership, no feature. **Blockers: 7 tests still unregisterable (4 = the SAME `SyntaxError` class — CBK2/PANTRY1/PLAN2/SHOP1 all half-landed; 3 = live regressions invisible because they never ran) · the SERVER HAS NO ADOPTION REGISTER, so nothing prevents the next HHP2 · FI still mute (600/610 foods uncitable — editorial, not engineering).** Report: `docs/implementation/intelligence/P0_FOOD_INTELLIGENCE_RECOVERY.md`. | [`runs/P0_Food_Intelligence_Recovery.md`](./runs/P0_Food_Intelligence_Recovery.md) |
| `FI18_Household_Food_Intelligence_Experiences` | Waiting for User | `rollback/FI18-household-food-intelligence-experiences-20260717` → `a9116faa` | Awaiting owner decision. **Investigation only — NOTHING IMPLEMENTED, by instruction; product source byte-untouched (`git status` modified-list identical to session start).** Defines how existing Food Intelligence is surfaced across the eight named surfaces. 🔴 **THE HEADLINE: THA's entire ambient intelligence is THREE opportunity types from ONE producer, and two of the three are absences.** `OPPORTUNITY_SOURCES` (`framework.ts:285-287`) holds exactly `{food-intelligence}`; its types are `planner-empty-day`, `pantry-item-unused-in-plan` (both *"you haven't done something"*) and `shopping-restriction-conflict` (the sole `critical`). **Food Intelligence is not one contributor to THA's proactive layer — it IS the whole of it.** 🔴 **The mission's first word names the wrong engine:** the **Observation** Engine is operator telemetry and §7 forbids user-facing reads (*"no observation is surfaced to a user"*); the owner of *"a true fact about your own data you didn't ask for"* is the **Notice** Engine. Building "Dashboard observations" on OBS1 is a hard stop. 🔴 **THE REFRAME: the bottleneck is CONNECTION and KNOWLEDGE, not surfacing.** The knowledge is deep (**610 foods · 1,035 graduated links · 173 diversity groups**) and the engines are built — but **FI3's recommender is reachable by NO utterance and NO route** (the codebase's own words: *"the shipped, tested reasoning engine that no user utterance can reach"*), **COMP1's 712-line cited comparison engine has no UI at all**, and **HNP1's + PANTRY1's assemblers have zero importers**. 🔴 **4 live defects, all hidden:** (1) `notice-gateway.ts` **cannot load** — imports 3 exports `notice-engine.ts` does not have; **executed, not inferred** (`SyntaxError`); no runtime importer — NTC-P2 half-landed; (2) **HHP2 is recorded "Status: Complete" and is wired to NOTHING** — capability absent from the registry entirely, binding never called, zero `OPPORTUNITY_SOURCES` entry; **HHP3 then deleted a live path on the strength of a producer path never connected**, and `HouseholdNutritionPanel.tsx:71-73` asserts that dead path is live; (3) **`hhp2`/`hhp3`/`notice-convergence` are not in `npm test`** — the gate that would have caught it never runs; (4) **the publication register greps a module that cannot load** (`publication-register.ts:993,1018`) — a green check over a `SyntaxError`, the same "gate is a text grep" class as CONV1 P10. 🟠 **The mandatory Bootstrap is materially FALSE about this domain** — FI arch §13 says *"no Food Intelligence Engine exists yet · 0% build-out · Phase 1 = 2027 H1"* while `engine.ts` cites that very document as its governing architecture (`DOC-4`/`KC14` again). 🔴 **The real ceiling is EDITORIAL, not engineering: `NUTRITION_CONTEXT` holds 10 foods of 610** (`engine.ts:217`), and **Rule E1 (*no citation, no card*) means 600 foods cannot render a card at all** — no UI work raises it. **Design = two doors already open, neither creating ownership: `OPPORTUNITY_SOURCES` enrolment (DEC1 §7, *"zero new suppress/rank/budget code"*) + the one `<AmbientIntelligence>` mount.** **Cap is 2 notices/moment and fixed on purpose** (*"improve the producers, not the volume"*) — so 8 surfaces over 3 facts is the same 3 facts, 8 times. **QW1 = mount `<AmbientIntelligence>` on Home** — the only major room without one, and it **already fetches every opportunity and renders none** (`home-experience-page.tsx:337` → used only at `:481` for the door's criticals). ⚠️ Repo-structure gate **3 FAILs — proven NOT mine** (identical with/without my file; sibling NORTH1–4 reports sit at `docs/implementation/` root). ✅ No code, no schema, no migration, no gates. **1 owner decision blocks the ranking — is the 10-food evidence ceiling deliberate?** Report: `docs/investigations/intelligence/FI18_HOUSEHOLD_FOOD_INTELLIGENCE_EXPERIENCES.md`. | [`runs/FI18_Household_Food_Intelligence_Experiences.md`](./runs/FI18_Household_Food_Intelligence_Experiences.md) |
| `NORTH4_Home_Concept_Exploration` | Waiting for User | `rollback/NORTH4-home-concept-exploration-20260717` → `a9116faa` (WIP tag: `north4-wip-snapshot-a9116faa`) | Awaiting owner decision. **Discovery only — NOTHING IMPLEMENTED, by instruction; product source byte-untouched (`git status` modified-list identical to session start).** Three entrance-hall concepts, palette derived from the new `ORCHARD.png`. 🔴 **THE FINDING, and it outranks all three concepts: decoded all 1,573,538 pixels — 94.37% of the orchard's chroma is hue 30–80°; the band `--primary: 132` / `--foreground: 120` / `--accent: 118` occupy is 0.01% of it.** Home's three most load-bearing colours are drawn from a hue band that is, to a rounding error, **absent from the artwork** — that is why it reads as a web application, and it is arithmetic, not taste. Platform-wide (every button in every room), and independent of which composition wins. **Derived, not invented:** THA's white is **`#fdf1da`** not `#ffffff`; its shadow is **olive** (`#292708`) never grey; its green is **hue 73** (`#39450d`, leaf-in-shade — the only foliage the golden hour doesn't overwhelm), **59° warmer than the 132 it ships**. Rule recorded: *hues are law, saturations are not* — transcribing the grass onto a button yields **mustard**. **Three concepts — the orchard is *light* (A · quiet hall, vertical/symmetric, best on mobile) / *the view* (B · panorama, mullions+sill load-bearing forever or it's a hero banner) / *a glimpse* (C · kitchen, oak counter, content as objects — a landscape idea that dies in portrait).** 🔴 **Two are UNLAWFUL as written: Blueprint § 6 fixes Home at E3 "the open view — Home only", and E2 reads "framed by composition, NEVER by a drawn frame" — A's arch and C's glazing bars are drawn frames. Only B is lawful today.** Named tension: the mission says *think in architecture*, and architecture frames views with drawn frames — that is what a window is. 🔴 **The new orchard's sun is upper-RIGHT** (measured: top-left 91, top-right **165**; flare at x≈93%) vs § 7's *"upper-left, **forever**"* — NORTH3 found this on the old asset and the new one inherits it; **the crop is the compliance mechanism and each concept crops differently** (A puts it out of frame; B and C show it). 🟠 The North Star's greeting is a **serif**; Home ships **Caveat** — the artwork and § 5.1's *"greeting in THA's hand"* have disagreed since NORTH1. 🟢 **The quiet day is the DEFAULT day** (CONV1 P8: **192/195 unanchored forever**) — a dashboard of zeroes reads *broken*; a hall with nothing on the table reads *a quiet morning*. **Every mockup renders the EMPTY state, not a showreel.** ⚠️ **4 of my own defects caught by measuring** — three vertical-rhythm collisions hidden behind absolutely-positioned layouts reporting `(fits)`, incl. C's primary action **invisible behind the doors** on mobile; 9/9 now collision-clear programmatically. Decoder **proven not trusted** (re-encoded and looked at it); first-pass region *means* discarded as mud. Orchard asset **byte-untouched**, no substitute authored. **Recommends B** (only lawful; only one doing what the mission asked) **carrying A's answer to the empty room** — but **the strongest move is none of the three: it is the palette.** ✅ No migration, no gates (no product code). **5 owner decisions block any build — D1 palette · D2 Exposure Scale · D3 the sun · D4 the signature · D5 which concept.** Report: `docs/implementation/NORTH4_HOME_CONCEPT_EXPLORATION.md`. | [`runs/NORTH4_Home_Concept_Exploration.md`](./runs/NORTH4_Home_Concept_Exploration.md) |
| `NORTH3_Home_Reimagined` | Waiting for User | `rollback/NORTH3-home-reimagined-20260717` → `f62b5839` (WIP tag: `north3-wip-snapshot`) | Awaiting review. **Home as the entrance hall — the room built, not the view.** 🔴 **OWNER RULING MID-SESSION: the orchard is a core THA BRAND ASSET with its own canonical ownership, supplied separately.** NORTH3 was instructed to author no substitute and did not — `orchard-bg.webp` is **byte-untouched**, and the ruling is recorded in the register (`orchard-environment.externalDependency`), not just in a report. **Three moves, no new law, NO VALUE SHIPPED, no behaviour changed:** (1) **the type roles were freed** — `.title-*`/`.text-signature` moved `@layer utilities` → `components`; they sat *after* `@tailwind utilities`, so the role silently beat every utility aimed at it, and **two of Home's shipped intentions had never actually happened**: NORTH2's `title-page font-normal` salutation rendered at **600** (the exact thing that change was written to stop — the comment outlived the fix) and NORTH1's `leading-[1.02]` signature at **1.15**. Both now live (600→**400**; 101px→**90px**), verified in the **compiled production CSS**; blast radius measured at **one surface** and proven by 4 byte-identical rooms. (2) **the doors became doors** — a 164px feature-card (icon/title/tagline/CTA disc, ~40% dead space) → a 58px name-and-handle; `DOOR_LINES` **retired**, four marketing lines NORTH1 §5.7 had already refused once by name. (3) **the room fits the house** — doors ended at y=936 with the nav at **y=839**, so the bottom 60% of every door was behind the toolbar (NORTH2 fixed this for the ONE door and never looked 100px lower); now **y=787**. `lg:pt-8` dropped → greeting and Companion share one shelf at y=89 (the household's name had been **32px below a software notice**). **NORTH2's `lg:mt-16` pause byte-untouched** — all air reclaimed was dead space above the first word. 🔴 **Findings:** the two `orchard_background_concept_*.png` NORTH2 named by path are **the same meadow** — *nothing in this repo has ever depicted an apple orchard*; **two further breaches no grade can fix** — **fog** across the bottom third (Blueprint §16 names fog as *the second sun*) and **the sun on the horizon, not upper-left** (§7, "forever") — *the one-morning law is currently kept by a CROP, not by the picture*. ⚠️ **A sibling session's work landed in NORTH3's evidence**: CONV1 P9 was live throughout (HEAD moved twice), the dashboard drifted and looked like NORTH3's CSS — **proven not**: CSS reverted → still differs; CSS applied vs reverted → **byte-identical**. Gate **80·0·2** (+1 = the `type-roles` row NORTH3 created; same 2 pre-existing failures, **proven not mine**, ceiling not raised); typecheck **32 — identical set, all in `server/`, zero in `client/src`**; build clean. ✅ **Code + generated register doc only — no migration.** **Open, reported NOT fixed:** Home's header still says **"Home"** above "Welcome home, Chloe" (Blueprint §18 item 2 — two lawful closures, both the owner's; picking by taste = a third treatment); the signature is a **marker pen** (Caveat, ADOPT-ruled — worth re-examining); the doors sit 60px above the same four in the nav. Report: `docs/implementation/NORTH3_HOME_REIMAGINED.md`. | [`runs/NORTH3_Home_Reimagined.md`](./runs/NORTH3_Home_Reimagined.md) |
| `NORTH2_Home_Refinement` | Waiting for User | `rollback/NORTH2-home-refinement-20260717` → `8fcb3d72` | Awaiting review. **Art direction on Home, executed as craft — no law amended, no behaviour changed, no new architecture.** Every pure-white surface → warm plaster (`--card` 0 0% 100% → 42 46% 98%, platform-wide on owner ruling; UIA §7 fixes structure, not values); `--shadow-support` **inverted** so the doors are etched INTO the counter rather than stacked on it; the welcome re-voiced (`title-page font-normal` — UIA §8's own "shouting in bold" rule) with the day moved below the name; the orchard **graded into the asset** (56,986 → **51,722 bytes** — PX1-W3's budget improved). 🔴 **Two findings, both pre-existing:** (1) **the orchard asset is not an orchard** — all 4 copies byte-identical (md5 332f82…), webp indistinguishable from the 2MB PNG, so *nothing was lost and nothing can be restored*; it is a watercolour MEADOW with no apple trees, no rows, no blossom. **No CSS closes this.** Full replacement spec in report §5 — **drops in with no code change**. (2) **five second owners of the orchard asset** (`dialog.tsx:48` behind EVERY dialog, `list-page.tsx:417`, `shopping-list-page.tsx:3014`, `onboarding-page.tsx:454`, `shopping-workspace-page.tsx:2328`) — the register's "nothing else" is **false**, and **the gate is enforced on the wrong noun** (it guards the export; these use the asset path, so it reports green while the concern is violated). Baking the grade made the fork **moot, not fixed**; recorded as `orchard-environment.openMigration`. ⚠️ Caught by looking: `lg:mt-24` pushed **the one door behind the bottom nav** at 1440×900 — air reclaimed from above the greeting instead. Gate **76·0·2** — identical to NORTH1's baseline, same 2 pre-existing failures, **not masked**; client typecheck + build clean; compiled CSS verified. **Platform change verified across 5 other rooms**; dark mode byte-untouched. ✅ Code + one binary asset — no migration. Report: `docs/implementation/NORTH2_HOME_REFINEMENT.md`. | [`runs/NORTH2_Home_Refinement.md`](./runs/NORTH2_Home_Refinement.md) |
| `NORTH1_Home_Implementation` | Waiting for User | `rollback/NORTH1-home-implementation-20260717` → `1648fc46` | Awaiting review. **Home stands at E3 — the North Star built.** The open view (window ≥`lg`, band below), the ODL2 material vocabulary, the signature greeting (**ADOPT executed on `/home`** — Caveat now ships), the 3-column glance + ring, and the one door the render lacks. `depth-light-ground` **CLOSED** (Study A deleted on its own trigger). Gate **76·0·2** — same 2 pre-existing failures as ODL2's baseline, not masked; client typecheck + build clean. ⚠️ **DEFERRED BY DECISION:** the 8 arrival prototypes are NOT deleted. P6 landed mid-session, so the files are free — but its `time-of-day-greeting` row has `adoptionFloor: 4` and 2 of those importers ARE prototypes, so deleting them needs the floor dropped 4→2 in the same change (a sibling row edit, while P7 is live). Owner ruled: leave them. `signature-typography` stays open on purpose. Report: `docs/implementation/NORTH1_HOME_IMPLEMENTATION.md`. | [`runs/NORTH1_Home_Implementation.md`](./runs/NORTH1_Home_Implementation.md) |
| `CONV1_Phase_P10_Schema_Coverage` | Complete | `rollback/CONV1-phase-p10-schema-coverage-20260717` → `a9116faa` | None — complete. **`SCH-3` closed; CONV1's engineering programme is FINISHED (P0–P10).** Coverage **51% → 100%** (91/91, `--strict` exits 0); **platform reds 6 → 5** — *"Every declared table is created by a reviewed migration"* (CPI1 §4.1) is **green for the first time since it was built**. **THE FINDING: the gate is a text grep and I walked into its trap** — the same 78 statements APPENDED report 100% while the rebuild fails **17/93 migrations · 65/91 tables**; at the **HEAD** → **93/93 · 91/91**. Same statements, only position. So the runner's append-only rule was **amended by governed exception** (one schema baseline at [0], the only entry that may sit there) — P8's §13.1 precedent. DDL **introspected from the live shape**, never the declaration (`SEC-3`'s lesson). **Proven twice**: builds from nothing (93/93 · 91/91, pinned connection, 45/45 column-identical) AND a **byte-identical no-op** on the live DB (983→983 columns). **3 of my own defects caught by measuring**, incl. accidentally running the full migration list against the live DB (pooled `SET search_path` doesn't hold) — schema verified intact. ⚠️ Migration applied (as a no-op); one cosmetic boot parity WARN on already-migrated DBs, self-resolving; `verify-prod` immune (set-based); REL3 39/39. **CONV1 assessment: complete as engineering; its household-visible value is ~zero until the OFFERED ANCHOR — a product decision.** Report: `docs/implementation/governance/CONV1_PHASE_P10_COMPLETION.md`. | [`runs/CONV1_Phase_P10_Schema_Coverage.md`](./runs/CONV1_Phase_P10_Schema_Coverage.md) |
| `CONV1_Phase_P9_Retire_The_Fabricator` | Complete | `rollback/CONV1-phase-p9-retire-the-fabricator-20260717` → `f62b5839` | None — complete. **`BEH-5` closed: `approxDate` deleted in BOTH copies** (§14 target 5, "2 → 0" — a private closure in `routes.ts` + the module extracted to prevent exactly that duplication). **Measured: the weekday Stories reported WAS the day you opened the app** (`(now.getDay() + dayOfWeek + 1) mod 7`) — one unchanged plan produced seven different "traditions" across seven days; right only on Saturdays, by coincidence. **CONV1's "inverted convention" grade is wrong** — DAY_NAMES and getDay() agree; the defect is that the date was never a date, and "fixing the index" would have made a fabricated date precisely wrong. Now a **lookup** over the anchor + `MONDAY_FIRST_ORDER`, reading **no clock**, or `null`; `MealEntry.date` nullable + `isDated` at both engines' seams. **`discover()`/Looking Ahead preserved** (food identity needs no calendar). **P8's gate excision DELETED** as instructed — `routes.ts` held to `ht-one-planner-week-owner` again, and passes. `household-time` 🟢 **17/17** (+2 ratchets, **6/6 mutations caught**); platform 75→77, reds unchanged at 4; typecheck **32 — identical set**; adoption 79·0·2; 9 suites green. **Live on a real 337-entry household: unanchored → 0 stories; the SAME plan anchored → 4 sections + a TRUE "Sunday became thai prawn curry night"**; nothing back-filled (verified). ✅ **Code-only.** **Recommends P10 (`SCH-3`)** — but the higher-value ask is **the OFFERED ANCHOR** (a product decision; Stories is silent for 192/195). Report: `docs/implementation/governance/CONV1_PHASE_P9_COMPLETION.md`. | [`runs/CONV1_Phase_P9_Retire_The_Fabricator.md`](./runs/CONV1_Phase_P9_Retire_The_Fabricator.md) |
| `CONV1_Phase_P8_T5_Convergence` | Complete | `rollback/CONV1-phase-p8-t5-convergence-20260717` → `5c4611e8` | None — complete. **4 of 5 closed** (`READ-3`·`OWN-6`·`BEH-3`·`BEH-9`); **`OWN-2` REFUSED with evidence** — it is behind the same legal gate as `SEC-5` (LIFE1 Step 3 needs Step 2 → § 12.1 legal review; `household_eaters` carries no age fact; `kind` means *has no account* and BEH-1 already renamed it; 166 rows hold live counts). **CONV1's OWN-2 row is mis-graded and must be corrected.** **The five rival "current weeks" are one** — three server `max(weekNumber)` ≡ **the constant 6**, `plannerFull[0]` ≡ 1, `localStorage` ≡ 1 → `server/lib/household-planner-week.ts` + `GET /api/planner/current-week` + one hook. **`BEH-9` re-graded**: Home HAD a door (NORTH1's, aimed by `localStorage` — what HOME3 §4 refused), so it was a convergence onto HOME2's resolver — **its first production consumer ever**. Also fixed the dashboard's **live day rotation** (Sunday rendered under "Mon", TIME1 §3.2). **HEADLINE: 192 of 195 households are unanchored FOREVER (HT7)** — user decided **Option C, the "Unanchored Home"**: no invented/floored week, every card from one canonical state, honest absence. **TIME3 § 13.1 AMENDED** (governed — P8 made it false). `household-time` 🟢 **15/15** (+3 ratchets, **6/6 mutations caught**); platform 72→75, reds unchanged at 4; typecheck **32 — identical set**; adoption 79·0·2; 13 suites green. ✅ **Code-only — no migration.** **Recommends the OFFERED ANCHOR (TIME1 §6.2) — a product decision, now the programme's bottleneck**; else `P9` (`BEH-5`). Report: `docs/implementation/governance/CONV1_PHASE_P8_COMPLETION.md`. | [`runs/CONV1_Phase_P8_T5_Convergence.md`](./runs/CONV1_Phase_P8_T5_Convergence.md) |
| `CONV1_Phase_P7_Planner_Week_Anchor` | Complete | `rollback/CONV1-phase-p7-planner-week-anchor-20260717` → `8fcb3d72` | None — complete. `SCH-2` closed: **both of Household Time's two facts now exist and the declaration is fully discharged.** `planner_weeks.weekStartDate` — nullable, additive, **no SQL default, no row rewritten**; written only by the existing funnel `storage.createPlannerWeeks` as `mondayOf(householdToday(now, zone)) + 7 × (N−1)`, legitimate there and nowhere else (TIME1 § 6.2). **Live-proven no back-fill: 1,152 pre-existing rows, 0 anchored**; an existing household re-calling the funnel got six NULLs back. `household-time` 🟢 **12/12** (+3 ratchets, **7/7 mutations caught** — incl. `ADD COLUMN … DEFAULT`, the back-fill wearing an ALTER's clothes). Platform 69→72 checks, reds unchanged at 4; typecheck **32 — identical set**, diffed at the tag; adoption 76·0·2 unchanged. **§ 14's targets 1 and 5 are no longer gated — the anchor's gate is open.** ⚠️ **Migration applied** (revert needs `ALTER TABLE planner_weeks DROP COLUMN week_start_date`). **Recommends `CONV1 P8`** (T5: `READ-3` → `OWN-6` → `BEH-3` → `OWN-2` → `BEH-9`); risk to carry: **`R3` — P7 built a FACT with no consumers, as P5 built a MODULE with no consumers.** Report: `docs/implementation/governance/CONV1_PHASE_P7_COMPLETION.md`. | [`runs/CONV1_Phase_P7_Planner_Week_Anchor.md`](./runs/CONV1_Phase_P7_Planner_Week_Anchor.md) |
| `CONV1_Phase_P6_Household_Time_T2_T3` | Complete | `rollback/CONV1-phase-p6-household-time-t2-t3-20260717` → `1648fc46` | None — complete. All four workstreams closed (`READ-4` → `BEH-6` → `SCH-4` → greeting ×4). **P5's owner had zero consumers; it has consumers now — `R3` closed.** `household-time` 🟢 **9/9** (2 new ratchets, mutation-tested). Greeting **4 → 1** (clock converged; words left to INT21/`CP3` — user-decided). **Live-verified against Pacific/Midway**: anchor `2026-07-16` vs UTC's `2026-07-17`; a lying client's `frozenDate` dropped. **`BEH-6` re-graded latent, not live** — `expiryDate` has no writer and 0/11 rows hold one (CP11). 14 suites green; 0 typecheck/adoption added; reds unchanged at 4. ✅ **Code-only — no migration.** Recommends **CONV1 P7** (`SCH-2`, the anchor); name **`R5`** (no back-fill). Report: `docs/implementation/governance/CONV1_PHASE_P6_COMPLETION.md`. | [`runs/CONV1_Phase_P6_Household_Time_T2_T3.md`](./runs/CONV1_Phase_P6_Household_Time_T2_T3.md) |
| `CONV1_Phase_P5_Household_Time` | Complete | `rollback/CONV1-phase-p5-household-time-20260716` → `fed6dcaf` | None — complete. All three workstreams closed; **HT18 gate met** (`household-time` 🟢 7/7, ratchets mutation-tested); `shared/time/household-time.ts` built, season 3→1, `households.timeZone` live (nullable, no back-fill). 15 suites green; 0 typecheck/adoption added. Recommends **CONV1 P6** (`READ-4` → `BEH-6` → `SCH-4` → greeting ×4). Report: `docs/implementation/governance/CONV1_PHASE_P5_COMPLETION.md`. ⚠️ Migration applied. | [`runs/CONV1_Phase_P5_Household_Time.md`](./runs/CONV1_Phase_P5_Household_Time.md) |
| `ODL2_Visual_Language_Foundation` | Waiting for User | `rollback/ODL2-visual-language-foundation-20260716` → `35d84533` | Delivered in 2 phases — **no governance blockers remain; North Star Home is unblocked**. P1 (law): UIA §4 amended (ground plane, one light, warm shadow, penumbra); Blueprint yielded ownership; orchard environment named; signature **ADOPT** recorded. P2 (values): **Study A** selected on law (B/C set type on the orchard — Blueprint §6.1) and deleted; exposure E3=0.90/E2=0.55/E1=E0=0; depth/light/ground valued from A; **dark completely resolved**; gradient-contrast rule →UIA §15; nested-radius rule →UIA §4; 3 superseded orchard tokens retired. Blueprint §18 items 1,3,4,5 closed. Gate 71·0·2 vs baseline 65·1·2 — same 2 pre-existing failures, unmasked. **UI provably unchanged** (E3 resolves to the identical 0.90). Await review. Report: `docs/implementation/ODL2_VISUAL_LANGUAGE_FOUNDATION.md`. | [`runs/ODL2_Visual_Language_Foundation.md`](./runs/ODL2_Visual_Language_Foundation.md) |
| `DEV1_Diet_Pattern_Launch_Recovery` | Complete | `rollback/DEV1-diet-pattern-launch-recovery-20260716` → `9900e405` | None — complete. DEV restored (stale pre-P4 process restarted onto the post-P4 tree after CONV1 P4's OWN-1 drop landed mid-boot). Next deployment must build from `9900e405`+. | [`runs/DEV1_Diet_Pattern_Launch_Recovery.md`](./runs/DEV1_Diet_Pattern_Launch_Recovery.md) |
| `CONV1_Phase_P4_Household_Person` | Complete | `rollback/CONV1-phase-p4-household-person-20260716` → `7d1dd2ce` | None — complete. All six workstreams closed; Household Dietary Preference 🔴→🟢; 26 suites green. Recommends **CONV1 P5** (`OWN-4` → `OWN-3` → `SCH-1`) next. Report: `docs/implementation/governance/CONV1_PHASE_P4_COMPLETION.md`. | [`runs/CONV1_Phase_P4_Household_Person.md`](./runs/CONV1_Phase_P4_Household_Person.md) |
| `ODL1_Home_Orchard_Design_Language` | Waiting for User | `rollback/ODL1-home-orchard-design-language-20260716` → `7d1dd2ce` | Delivered — lawful North Star subset on `/home` (display-voice greeting, named type roles, honest plant absence, Companion beat). Gated elements (orchard light, signature hand, depth) named with unlock paths in the report. Await review; ADOPT on signature typography unlocks the hand-written greeting. | [`runs/ODL1_Home_Orchard_Design_Language.md`](./runs/ODL1_Home_Orchard_Design_Language.md) |
| `DCA1_User_Visible_Data_Coverage_Audit` | Waiting for User | `rollback/DCA1-user-visible-data-coverage-audit-20260714` → `f9c23c97` | `SURF1A` (W1) delivered. Await direction on the remaining `SURF1` phases — W0 (household safety) is the highest. | [`runs/DCA1_User_Visible_Data_Coverage_Audit.md`](./runs/DCA1_User_Visible_Data_Coverage_Audit.md) |
| `SURF1A_Existing_Data_Surfacing` | Complete | `rollback/SURF1A-existing-data-surfacing-20260714` → `f9c23c97` | None — complete. Recommends DCA1 gap #1 (`dietRestrictions` never reach the Companion) next. | [`runs/SURF1A_Existing_Data_Surfacing.md`](./runs/SURF1A_Existing_Data_Surfacing.md) |
| `SURF1B3_Onboarding_Allergy_Safety_Routing` | Complete | `rollback/SURF1B3-onboarding-allergy-safety-routing-20260714` → `194f7af2` | None — complete. Recommends SURF1B2 limitation #1 next: the Vegan/Vegetarian **pattern** path still has meat-keyword holes the restriction path no longer has. | [`runs/SURF1B3_Onboarding_Allergy_Safety_Routing.md`](./runs/SURF1B3_Onboarding_Allergy_Safety_Routing.md) |
| `SURF1B5_Starter_Meal_Safety_Convergence` | Complete | `rollback/SURF1B5-starter-meal-safety-convergence-20260714` → `933dfabe` | None — complete. Its limitation #1 was closed by `SURF1C1` (below), which found the cookbook was never thin — only unlabelled. | [`runs/SURF1B5_Starter_Meal_Safety_Convergence.md`](./runs/SURF1B5_Starter_Meal_Safety_Convergence.md) |
| `SURF1C1_Starter_Cookbook_Diet_Classification` | Complete | `rollback/SURF1C1-starter-cookbook-diet-classification-20260714` → `e2fa1fbc` | None — complete. Its top gap, the **asparagus/`ragu` gate defect**, was fixed by `SURF1C2` (below). | [`runs/SURF1C1_Starter_Cookbook_Diet_Classification.md`](./runs/SURF1C1_Starter_Cookbook_Diet_Classification.md) |
| `SURF1C2_Canonical_Restriction_Matcher_Boundary_Safety` | Complete | `rollback/SURF1C2-canonical-restriction-matcher-boundary-safety-20260714` → `9e0b831d` | None — complete. Fixed the resolver so derived/hidden terms match only at a word boundary (`ragu` ⊄ `asparagus`); 0 matches added across the corpus, 8 fail-closed false positives removed, 52 asparagus recipes relabelled. Possible follow-up: a linter for short (<5 char) library terms. | [`runs/SURF1C2_Canonical_Restriction_Matcher_Boundary_Safety.md`](./runs/SURF1C2_Canonical_Restriction_Matcher_Boundary_Safety.md) |
| `ARRIVAL1_Arrival_Experience_Prototype` | Waiting for User | `rollback/ARRIVAL1-refine-welcome-header-sheen-20260715` → `3176c62d` (orig: `rollback/ARRIVAL1-arrival-experience-prototype-20260715`) | Delivered + **refined**. `/dev/arrival` — dev-only Arrival Experience (cream → deep-green "Welcome home" held then slowly faded → orchard emerges → canonical header parity → one sunrise sheen across the long logo → gentle glide into a one-viewport Home workspace). Live Home untouched. Screenshots + close-ups + recording in `docs/ui-audit/arrival-experience/`. Awaiting ADOPT / REJECT. | [`runs/ARRIVAL1_Arrival_Experience_Prototype.md`](./runs/ARRIVAL1_Arrival_Experience_Prototype.md) |
| `RM1_Canonical_Ready_Meal_Role` | Waiting for User | `rollback/RM1-canonical-ready-meal-role-20260715` → `0f0615aa` | Investigation delivered (`docs/investigations/platform/RM1_CANONICAL_READY_MEAL_ROLE.md`). Recommends: keep ready meals as `meals` identity (not a new Product entity), Analyser as read lens, Planner stores `mealId` (option c). Next workstream **RM2** (Analyser→Planner "Add to Planner" journey, no schema change); follow-on **RM3** retires dead `meal_template_products`. Nothing implemented/migrated. | [`runs/RM1_Canonical_Ready_Meal_Role.md`](./runs/RM1_Canonical_Ready_Meal_Role.md) |
| `RM2A_Analyser_To_Planner_Journey` | Complete | `rollback/RM2A-analyser-to-planner-journey-20260715` → `0f0615aa` | None — complete. Delivered the Analyser→Planner journey: idempotent resolve-or-create meal by barcode (`storage.resolveOrCreateProductMeal`) wired into both product routes, plus a direct "Add to Planner" action on the in-Planner Analyser reusing `AddToWeekModal`. No schema change, no new entity. 14/14 RM2A test; analyser/planner/meals/dedup regressions green. Next: **RM3** (retire `meal_template_products`, RM1 §8). | [`runs/RM2A_Analyser_To_Planner_Journey.md`](./runs/RM2A_Analyser_To_Planner_Journey.md) |
| `EXP2_Arrival_Experience_Exploration` | Waiting for User | `rollback/EXP2-arrival-exploration-20260715` → `74aaa8da` | Delivered. Five dev-only Arrival prototypes — `/dev/arrival-a-welcome` (greeting + pause) · `-b-orchard` (place + light) · `-c-workspace` (prepared kitchen) · `-d-quiet` (intelligence as preparation) · `-e-restraint` (subtraction) — each replayable via `?replay`. Evidence in `docs/ui-audit/exp2-arrival-explorations/`; report + § 7 carry-forward synthesis in `docs/implementation/ux/EXP2_ARRIVAL_EXPERIENCE_EXPLORATION.md`. Live Home + ARRIVAL1 untouched. Awaiting review of which ideas GRADUATE. | [`runs/EXP2_Arrival_Experience_Exploration.md`](./runs/EXP2_Arrival_Experience_Exploration.md) |
| `EXP3_Arrival_Synthesis_Prototypes` | Waiting for User | `rollback/EXP3-arrival-synthesis-prototypes-20260715` → `da368a39` | Delivered — the FINAL exploration before one canonical arrival. Two synthesis candidates: `/dev/arrival-s1-quiet` (A's greeting halved + B's shell-from-frame-one + C's state sentence + D's Companion manners + E's restraint) · `/dev/arrival-s2-walking-home` (walking into the orchard on still planes — no parallax, no moving trees). Evidence `docs/ui-audit/exp3-arrival-synthesis/`; report + § 5 recommendation (**adopt S1 as the canonical shape**) in `docs/implementation/ux/EXP3_ARRIVAL_SYNTHESIS_PROTOTYPES.md`. Live Home, ARRIVAL1, EXP2 untouched. Awaiting the selection. | [`runs/EXP3_Arrival_Synthesis_Prototypes.md`](./runs/EXP3_Arrival_Synthesis_Prototypes.md) |
| `EXPLANG1A_Experience_Language_Enhancements` | Complete | `rollback/EXPLANG1A-experience-language-enhancements-20260715` → `b7ddc442` | None — complete. Enhanced the governing THA Experience Language with **§ 4A — eight Place Principles** discovered in the ARRIVAL1 prototype (The THA Promise · THA is a place · Arrival before work · Walking not scrolling · Shell is constant · Light has meaning · Home is not the dashboard · Premium through restraint); extended § 6 / § 7 only where genuinely required. Enhancement only — thirteen § 4 principles unchanged and unrenumbered, no duplicated guidance, no ownership overlap. Docs only. Report: `docs/implementation/governance/EXPLANG1A_THA_EXPERIENCE_LANGUAGE_ENHANCEMENTS.md`. | [`runs/EXPLANG1A_Experience_Language_Enhancements.md`](./runs/EXPLANG1A_Experience_Language_Enhancements.md) |
| `EXPLANG1B_Emotional_Palette` | Complete | `rollback/EXPLANG1B-emotional-palette-20260715` → `b3c650cd` | None — complete. Enhanced the governing Experience Language with **§ 3A — The Emotional Palette of THA** from the Arrival prototype reviews: eight never-feel temperatures, the seven-note always-feel palette (calm · warm · energised · thoughtful · comforting · decisive · curious), the orchard fixed as *life*, and **"Calm must never become lifeless"**; § 6 +4 Warmth & Life questions, § 7 +5 anti-patterns. Enhancement only — nothing rewritten or renumbered, no duplication, no ownership overlap. Docs only. Report: `docs/implementation/governance/EXPLANG1B_EMOTIONAL_PALETTE.md`. | [`runs/EXPLANG1B_Emotional_Palette.md`](./runs/EXPLANG1B_Emotional_Palette.md) |
| `EXP4_Materiality_And_Depth` | Waiting for User | `rollback/EXP4-materiality-and-depth-20260715` → `b3c650cd` | Delivered. Three dev-only Materiality & Depth studies — `/dev/material-a-warm-layers` (the ground plane: workspace as a prepared counter) · `-b-atmosphere` (one morning light; hierarchy by illumination) · `-c-restraint` (one object; air as material; the flat-law control) — same room, only the material varies. Evidence in `docs/ui-audit/exp4-materiality-depth/`; report + § 5 synthesis (**A's ground + B's light + C's air**) in `docs/implementation/ux/EXP4_MATERIALITY_AND_DEPTH.md`. Live Home + all arrival prototypes untouched. Awaiting GRADUATE / REJECT. | [`runs/EXP4_Materiality_And_Depth.md`](./runs/EXP4_Materiality_And_Depth.md) |
| `EXP5_One_Home_Many_Places` | Waiting for User | `rollback/EXP5-one-home-many-places-20260715` → `b3c650cd` | Delivered — investigation only, no code touched. `docs/investigations/ux/EXP5_ONE_HOME_MANY_PLACES.md`: the "One Home, Many Places" governing concept (one orchard, many windows), 11-domain place & art-direction map, shared material/light/depth vocabulary, orchard-exposure scale, living-details library (max one per domain, data-borne only), supports/conflicts inventories. Recommends dev-only prototypes for **Home → Cookbook → Planner**, first workstream EXP5-P1 (Home). Awaiting review. | [`runs/EXP5_One_Home_Many_Places.md`](./runs/EXP5_One_Home_Many_Places.md) |
| `EXPBLUE1_THA_Experience_Blueprint` | Complete | `rollback/EXPBLUE1-tha-experience-blueprint-20260715` → `b3c650cd` | None — complete. Created the governing **THA Experience Blueprint** (`docs/architecture/THA_EXPERIENCE_BLUEPRINT.md`): the unifying source of truth for how THA feels, looks and behaves — Vision, One Home Many Places (11-room map), Orchard Exposure Scale (E0–E3), one-morning light, three-ground materials, Living Details library, Companion's place, Blueprint Checks (§ 15.2, wired into the workflow gate), spatial anti-patterns, Design North Star. Cites owners throughout; restates nothing; the three sibling documents untouched. Report: `docs/implementation/architecture/EXPBLUE1_THA_EXPERIENCE_BLUEPRINT.md`. Open items in Blueprint § 18 (orchard owner · Home header · UIA § 4 amendment · dark mode). | [`runs/EXPBLUE1_THA_Experience_Blueprint.md`](./runs/EXPBLUE1_THA_Experience_Blueprint.md) |
| `RM4_Planner_Ready_Meal_Library` | Complete | `rollback/RM4-planner-ready-meal-library-20260715` → `da368a39` | None — complete. Delivered the Planner Ready Meal Library: read-only `GET /api/planner/ready-meal-library` over the member's existing `meals` identities (recency → frequency ordering, Apple Score from own `product_history`) + a "Previously Added Ready Meals" section in the picker's Packaged source with one-tap reuse via the normal add-entry path. No new entity, no schema change. 21/21 RM4 tests; rm2a/planner/meals/dedup/compliance regressions green. | [runs/RM4_Planner_Ready_Meal_Library.md](./runs/RM4_Planner_Ready_Meal_Library.md) |
| `EXPBLUE2_Modern_Home_Ancient_Orchard` | Waiting for User | `rollback/EXPBLUE2-modern-home-ancient-orchard-20260715` → `b3c650cd` | Delivered. Extended the governing Experience Blueprint in place: § 1.4 **"A modern home in an ancient orchard"** (modern intelligence for timeless values), § 1.5 the **Technology Principle** (*technology should quietly disappear; the household should always feel present*), § 4.1 the **digital-home** reading of the rooms, § 6.0 the **ancient orchard**, § 15.3 a **mandatory Experience Test** (which room · how should someone feel · the one thing it helps them do — wired into the Experience & UI gate), and the § 17 **Design North Star headline**. Extension only — no new document, no UI, no rule duplicated; three sibling docs byte-untouched. Awaiting review/adoption. | [runs/EXPBLUE2_Modern_Home_Ancient_Orchard.md](./runs/EXPBLUE2_Modern_Home_Ancient_Orchard.md) |
| `OLB1_Orchard_Living_Book` | Complete | `rollback/OLB1-orchard-living-book-20260715` → `b3c650cd` | None — complete. Created the canonical **Orchard Living Book** (`docs/architecture/THA_ORCHARD_LIVING_BOOK.md`) — the lived, felt account of the house, written to be *read*, not checked. Sixteen chapters of ordinary moments (First Light → Evening In The Orchard) each covering where you are · what's happening · atmosphere · light · orchard · materials · Companion · what should never happen · lasting feeling, closing with **The Orchard Promise**. Extends the Experience Blueprint (place) and Orchard House Design Blueprint (design character) by rendering them as lived experience; restates no rule, adds no gate/check; upholds the one-morning/one-season law by placing all rain/seasons/evening in the *household's* life, never the house. Docs only; both blueprints + three siblings byte-untouched. Report: `docs/implementation/architecture/OLB1_ORCHARD_LIVING_BOOK.md`. | [runs/OLB1_Orchard_Living_Book.md](./runs/OLB1_Orchard_Living_Book.md) |
| `ORCHARD2_First_Light` | Waiting for User | `rollback/ORCHARD2-first-light-20260715` → `b3c650cd` | Delivered — the First Light arrival design (`docs/implementation/ux/ORCHARD2_FIRST_LIGHT.md`), the first practical design built on the Experience Blueprint, Orchard House Design Blueprint, and Orchard Living Book. Design only, no code: ten deliverables (narrative · spatial layout w/ ASCII sketches · composition · hierarchy · motion · Companion · light · 30-sec journey · ten First Light principles · 14 decision-reasons) + gold-standard room grammar + deliberate absences. Restates no rule, cites owners, creates no governing law (README untouched), declares the §0.3 governance path rather than jumping it. Report: `docs/implementation/architecture/ORCHARD2_FIRST_LIGHT.md`. Awaiting review/adoption. Next (separate workstream): dev-only Home prototype, only after the orchard-owner + Home-header open items and the UIA §4 amendment land. | [`runs/ORCHARD2_First_Light.md`](./runs/ORCHARD2_First_Light.md) |
| `ORCHARD3_Visual_Concept_Exploration` | Waiting for User | `rollback/ORCHARD3-visual-concept-exploration-20260715` → `b3c650cd` | Delivered — the discovery of THA's **visual soul**. Reference world of real timeless environments (architecture · interiors · hospitality · galleries/museums · premium retail · residential); nutrition apps/Apple/Notion/Linear/trends excluded. Three complete named concepts — **A The Glass Pavilion** · **B The Kept Room** · **C The Garden Room** — each × 14 dimensions, critically evaluated. Recommends **Concept B — The Kept Room** (warm minimalism), grafting A's disciplined view + C's warm orchard light. Design only — no code/tokens/values; restates no rule; README + governing docs byte-untouched; names the § 2.4 graduation path. `docs/implementation/ux/ORCHARD3_VISUAL_CONCEPT_EXPLORATION.md` + report in `docs/implementation/architecture/`. Awaiting decision; if accepted, next is a *governance amendment* (not implementation). | [`runs/ORCHARD3_Visual_Concept_Exploration.md`](./runs/ORCHARD3_Visual_Concept_Exploration.md) |
| `OHDB1_Orchard_House_Design_Blueprint` | Complete | `rollback/OHDB1-orchard-house-design-blueprint-20260715` → `b3c650cd` | None — complete. Created the governing **Orchard House Design Blueprint** (`docs/architecture/THA_ORCHARD_HOUSE_DESIGN_BLUEPRINT.md`) — the design-language face of the Experience Blueprint. Owns only the unowned: the house's **architectural character** (§ 3), **interior philosophy** (§ 4), the **per-room design reading** of all nine named rooms (§ 13), **"Timeless, not fashionable"** (§ 14), a ten-line **Design Manifesto** (§ 15), and the **Design Character Check** (§ 16.2). Cites owners for material/light/colour/type/depth/orchard/seasonal/atmosphere; restates no rule; the Experience Blueprint and three sibling docs byte-untouched. Docs only. Report: `docs/implementation/architecture/OHDB1_ORCHARD_HOUSE_DESIGN_BLUEPRINT.md`. | [runs/OHDB1_Orchard_House_Design_Blueprint.md](./runs/OHDB1_Orchard_House_Design_Blueprint.md) |
| `TRANSLATION1_Kept_Room_Into_UI_Language` | Complete | `rollback/TRANSLATION1-the-kept-room-into-ui-language-20260715` → `b3c650cd` | None — complete. Created the governing **Kept Room Translation** (`docs/architecture/THA_KEPT_ROOM_TRANSLATION.md`) — the bridge between the Orchard House architecture and the software interface: the 9-facet translation schema, 20 architectural characteristics each translated across behaviour · layout · spacing · typography · motion · interaction · what-must-never-happen (Light · Space · Thresholds · Rooms · Windows · Orchard views · Kitchen island · Oak · Stone · Linen · Handmade ceramics · Shelving · Quiet corners · Morning rhythm · Patina · Permanence · Calm · Warmth · Household presence · Companion presence), the 9 governing UI translation principles (space before decoration … technology should quietly disappear · the household should always feel present), and the complete visual language stated whole (no screen named). Cites owners; restates no rule; sets no value; adds no gate; jumps no governance path. Five translated docs byte-untouched; inherits the Blueprint's four open items. Docs only. Report: `docs/implementation/architecture/TRANSLATION1_KEPT_ROOM_TRANSLATION.md`. | [`runs/TRANSLATION1_Kept_Room_Into_UI_Language.md`](./runs/TRANSLATION1_Kept_Room_Into_UI_Language.md) |
| `DESIGN1_Home_Visual_Design` | Waiting for User | `rollback/DESIGN1-home-visual-design-20260715` → `b3c650cd` | Delivered — the definitive **visual** design of the permanent Home (the first visual product-design work), building on EXPBLUE · OHDB · OLB · ORCHARD2 · ORCHARD3 · HOUSE1 · TRANSLATION1. Primary deliverable is visual: `docs/ui-audit/design1-home/DESIGN1_home_visual_design.html` — eleven annotated design plates (Desktop · Tablet · Mobile concepts · annotated layout · visual hierarchy · typography · spacing · Companion · motion · empty state · returning Home) + permanence-contract & gold-standard tables; also published as a shareable Artifact. Home drawn as **The Kept Room**; plates keep one warm morning even in a dark frame (demonstrates the one-morning law). Design-spec `docs/implementation/ux/DESIGN1_HOME_VISUAL_DESIGN.md`; report `docs/implementation/architecture/DESIGN1_HOME_VISUAL_DESIGN.md`. Design/docs only — no code, no components, no tokens; README + all governing docs byte-untouched; every value remains UIA's; governance path (§0.3/§2.4) declared not jumped; passes the Experience Test, Blueprint Checks, Design Character Check read against TRANSLATION1. Awaiting review/adoption. | [`runs/DESIGN1_Home_Visual_Design.md`](./runs/DESIGN1_Home_Visual_Design.md) |
| `NORTH2_Experience_Architecture_Refinement` | Waiting for User | `rollback/NORTH2-experience-architecture-refinement-20260716` → `7d1dd2ce` | Delivered — the Architecture Refinement Report (`docs/investigations/ux/NORTH2_EXPERIENCE_ARCHITECTURE_REFINEMENT.md`), assessing NORTH1's five proposed principles as an *evaluation document, never architecture*. **1 of 5 adopted.** **Aperture — ADOPT (narrowed)**: Experience Blueprint § 6.1, **+1 bullet**; genuinely new content is only that boundedness binds at **E3** (a full-bleed orchard at Home is *currently legal* — the loophole the live wallpaper defect sits in) plus plurality/the load-bearing ground between openings; must be narrowed to E2–E3 and worded *bounded* not *separated*. **Truthful Objects — NO** (owned 3×: Blueprint § 12.1 r2 · OHDB § 4 · TRANSLATION1 *Patina*). **Presence — NO** (owned by Experience Language § 3A.2/§ 3A.4 + OHDB § 4; all three contrasts already drawn — EXPLANG1B closed this gap in July). **Interface as Guest — NO** (sound half owned § 8.2; novel half **conflicts** — *"the room always remains primary"* contradicts § 6.2's inverse law + § 16 *metaphor taxing function*). **Time — REJECT** (**conflicts** with Blueprint § 7, § 16 *the second sun*, OHDB § 11, TRANSLATION1 *Morning Rhythm* § 9 which forbids it by name; time is *already* recognised — through the household's data, never the house's light). **Headline: the Experience Architecture is the wrong document for all five and stays byte-untouched** (§ 1/§ 2.1 — technology-independent, explicitly not a visual style guide); NORTH1 produced zero behavioural findings not already law, and its big findings are **conformance defects, not architecture gaps**. **No architecture modified, no UI, no second visual language.** Awaiting instruction on the § 6.1 bullet (note the named cost: DESIGN1 § 4 / HOUSE1 § 2.2's horizon is not an aperture). | [`runs/NORTH2_Experience_Architecture_Refinement.md`](./runs/NORTH2_Experience_Architecture_Refinement.md) |
| `NORTH1_The_Visual_North_Star` | Waiting for User | `rollback/NORTH1-visual-north-star-20260716` → `7d1dd2ce` | Delivered — the evaluation of the North Star reference images (`attached_assets/North Star/`) against the canon (EXPBLUE · OHDB · OLB · TRANSLATION1 · DESIGN1). Verdict: **the strongest artefact the visual programme has produced, and it must never be built** — it proves the Kept Room is warm (settling ORCHARD3's bet and the cold-calm risk), but earns that warmth by drawn kitchen, painted props and a wall aphorism, i.e. *the theme park* (§ 16) and *fabricated feeling* (§ 12). Extracts six permanent principles — incl. the genuine find, **the aperture**: the orchard reads as a view *because of the solid wall between the openings* (proposed addition to Blueprint § 6, not written as law). Names what must not be copied (the room · props · six-tile launcher · glance-card-with-no-door · the Cookbook plate, which disproves itself · frosted glass · wall text/tagline · fixed perspective). Fact-checked against code: **the live product already ships the wallpaper + parallax anti-patterns** (`orchard-backdrop.tsx:9`, opacity 0.90 behind every room), and `--card: 0 0% 100%` is the one cold plane, masked by an opacity crutch. First changes are **deletions and conformance fixes**, all deliverable before the UIA § 4 amendment. Report: `docs/implementation/ux/NORTH1_THE_VISUAL_NORTH_STAR.md`. **No UI implemented; no governing doc amended.** Awaiting (a) adoption of the § 7 status line, (b) go/no-go on NORTH2 (retire the backdrop). | [`runs/NORTH1_The_Visual_North_Star.md`](./runs/NORTH1_The_Visual_North_Star.md) |
| `EXPCOMP1_Experience_Compliance_Standard` | Waiting for User | `rollback/EXPCOMP1-experience-compliance-standard-20260716` → `7d1dd2ce` | Delivered — the governing **Experience Compliance Standard** (`docs/investigations/ux/EXPCOMP1_THA_EXPERIENCE_COMPLIANCE_STANDARD.md`) + **Audit Template** (`…_AUDIT_TEMPLATE.md`). **19 compliance areas, 19 owners, ZERO rules created** — the standard owns only the *instrument* (audit method · PASS/WARNING/FAIL scale · evidence standard · report shape); every area routes to its owning document, with a yield clause making any rule found in it a defect. **The gap it fills:** the canon's six gates (UX Checklist · UI Checklist · Experience Review Questions · Experience Test · Blueprint Checks · Design Character Check) are all **per-change and binary** — nothing verifies a *surface as a whole*, grades it, permits comparison, or leaves a retained artefact. **Precedence hazard resolved:** the audit *measures surfaces*, never *governs changes* — a gate's STOP overrides any grade; WARNING is never a licence. **WARNING is the load-bearing grade** (correct today, correct for the wrong reason, one innocent change from wrong — e.g. NORTH1 § 8.2's opacity crutch). **Two findings, reported not fixed:** (1) `ENGINEERING_WORKFLOW.md` references **none** of OHDB / TRANSLATION1 / OLB — so **OHDB § 16.2's Design Character Check is unreachable** by anyone following the workflow, the exact `ARCH-VERIFY1` gap reopened on 2026-07-15; (2) **Performance & Responsiveness has no canonical owner** — recorded as an open item, *not* invented. **Placement recommended:** the *obligation* → Experience Blueprint **§ 15.4** (one subsection, by governed amendment — § 15 already holds the gate system as a whole); the *standard + template* → **operational**, `docs/implementation/ux/`, beside `ADOPTION_REGISTER.md` (the exact UIA § 17 precedent). Experience Architecture stays **byte-untouched** (NORTH2 § 1's reasoning). **No architecture modified, no screen audited, no UI, no mock-up, no principle created.** First audit subject recommended: **Home**. | [`runs/EXPCOMP1_Experience_Compliance_Standard.md`](./runs/EXPCOMP1_Experience_Compliance_Standard.md) |
| `HOUSE1_The_Entrance_Hall` | Waiting for User | `rollback/HOUSE1-the-entrance-hall-20260715` → `b3c650cd` | Delivered — the definitive design of the **permanent Home experience** (`docs/implementation/ux/HOUSE1_THE_ENTRANCE_HALL.md`), rendered as **The Kept Room** (ORCHARD3 § 5, taken as given — no alternatives explored). 26 sections covering every mission element (spatial composition · visual hierarchy · arrival · daily rhythm · permanent layout · what changes/remains · Companion · orchard · info hierarchy · motion · material · light · depth · empty state · returning · progressive disclosure · interaction · accessibility · mobile/tablet/desktop) with low-fi ASCII sketches, a full-day (morning/midday/evening + returns) narrative, and per-element reasoning. Central idea: **one unchanging morning; the household's day shows through the data, never the house's light** — reconciles the daily rhythm with the one-morning law. First Light cited as the arrival beat (not restated); Home fixed as the **gold standard** every future room inherits. Design only — no code/components/tokens; restates no rule; cites owners; declares the §0.4 governance path; README + all six source docs byte-untouched. Passes the Experience Test, Blueprint Checks, Design Character Check. Report: `docs/implementation/architecture/HOUSE1_THE_ENTRANCE_HALL.md`. Awaiting review/adoption. | [runs/HOUSE1_The_Entrance_Hall.md](./runs/HOUSE1_The_Entrance_Hall.md) |
| `HOME1_Arrival_Behaviour_Investigation` | Waiting for User | `rollback/HOME1-arrival-behaviour-investigation-20260716` → `7d1dd2ce` | Delivered — the Arrival Behaviour Investigation (`docs/investigations/ux/HOME1_ARRIVAL_BEHAVIOUR_INVESTIGATION.md`). **The hypothesis is two claims with opposite verdicts.** **"Home is the household's Arrival room" — TRUE but already law**, in the canon's own words: Home is *"the threshold and the heart"* (Blueprint § 5.1; OHDB § 13.1), governed by EXPLANG Principles 1/C/G, verb fixed by DESIGN1 § 0.1. A paraphrase, not an interpretation — adopting it creates the second owner § 18 forbids. **"…containing an ambient Household Display" — REFUSED, in both readings.** As the room's *nature* it **conflicts 4×**: UIA § 5 (the two-second rule needs **three** answers — orientation, state, **and the primary action**; a display yields two) · Blueprint § 15.3 Q3 (**no verb = decoration**, instructs STOP) · EXP ARCH Principle 4/§ 7 · **dwell vs departure** (EXP ARCH § 11/§ 17.11, Blueprint § 1.5/§ 1.1 — *a display succeeds when you look at it; THA succeeds when you leave*). As a *component* it is **owned with no remainder** (content § 4 · hierarchy § 9/UIA § 5 · feeling § 3A · Home's one Living Detail already spent on the greeting). Inherits NORTH2 § 3.4's refusal. **The 7 behavioural characteristics: 7/7 already owned** — ambient info · glanceability · calm presence · always useful · non-demanding · today's context · low cognitive load. Nothing to adopt. **"Always useful" is the trap**: the display means *never blank*, THA means *never wasted* (EXP ARCH § 6). **Headline hazard:** the hypothesis is the most convincing argument yet for **never giving Home a door** — it would convert NORTH1 § 8.3's live defect (Home has no primary action) into a design intent. *"Absence of a primary action looks exactly like restraint"* (EXPCOMP1 AREA 1). **"Retire Dashboard → Arrival": NO — category error.** *Arrival* is a **beat** (EXPLANG § 5) running in every room; *Dashboard* a **room** (Principle G); *Home* the **place**. Breaks EXP ARCH § 13 (one name per concept); changes nothing user-facing (Home is already "Home" everywhere; "Arrival" is **zero** user-facing); Blueprint § 4: *"the home is never renamed."* **Genuine new find (§ 8.2), reported not fixed: `/dashboard` is a live household-reachable page with NO row in the Blueprint § 5.1 map** — `<h1>Dashboard</h1>` (`dashboard.tsx:227`), 923 lines, charts + THA Score + 2 logging forms, not in nav, **its only live door is Home's muted footer link**, actively maintained, sanctioned in-file by PHASE5C. Blueprint § 15.3 Q1: *a placeless surface cannot be one home*. Principle G's *"the dashboard"* is **undefined** (Home's workspace, or this page?) — ambiguity recorded, not resolved. **Zero amendments, zero principles.** All seven governing docs byte-untouched. No UI, no redesign, no audit. **Next: (a) fix Home's door — must be a *departure*, not a fourth card; (b) decide what `/dashboard` is.** | [`runs/HOME1_Arrival_Behaviour_Investigation.md`](./runs/HOME1_Arrival_Behaviour_Investigation.md) |
| `EXPCOMP2_Home_Experience_Compliance_Audit` | Waiting for User | `rollback/EXPCOMP2-home-experience-compliance-audit-20260716` → `7d1dd2ce` | Delivered — **the first Experience Compliance Audit**, on the subject EXPCOMP1 § 7.4 nominated (`docs/investigations/ux/HOME_COMPLIANCE_AUDIT_20260716.md`). **Home: 3 PASS · 9 WARNING · 7 FAIL → NON-COMPLIANT (3/19)**, band set by the worst grade present. All 19 areas graded (the mission's 16 mapped in, none dropped; the 4 it didn't name include 2 FAILs). **Headline: Home has no door** — zero `<Button>`, three identically-styled `<Link><Card hover-elevate border-border/40>` with three identical chevrons, and a subtitle that *asks* the household to prioritise (`"How can I help your family today?"`, `:199`). The room built to know what they need is asking them. **Two live defects new to the canon:** (1) **`:160` `plantCount ?? 0` → "0 of 30 plants"** where the server deliberately returns `null` for *no validated data* (`routes.ts:11486-87`) — a measurement never taken, and the same codebase refuses this exact coercion one route away (`:11717-18`); (2) **"Today's Meals" is unfalsifiable** — derived from `localStorage` active-week against a schema with **no calendar anchor** (`schema.ts:420-454`), rendered beneath a *real* date. **The two most valuable insights are WARNINGs** (the standard's bet paid): **Home is at E3 only because every other room is wrongly at E3** — `orchard-backdrop.tsx:18` `opacity: 0.90` hardcoded, no exposure scale exists (`grep -i exposure client/` → 0), `--orchard-opacity: 0.72` orphaned; any fix aimed at the Planner silently drops Home below its mapped level. And **every card is warm only by an 18% cream bleed** (`--card: 0 0% 100%` + `bg-card/82`) — with `index.css:588-93` already reverting it in dialogs, proving it a crutch. **Contrast measured, not eyeballed:** date label **3.40:1** (floor 4.5), focus ring **1.51:1** (floor 3, product-wide). **6 architectural conflicts routed, none resolved** — load-bearing: **the canonical container has no rung for Home's mapped ground posture** (`pageContainerClass` = 1536/1280; the map needs 768's *"compact counter — the view keeps its share"*), so the naive conformance fix **destroys the map**; also the realm tokens are **out of scope for every page body**, and "one breakpoint truth" is **three numbers inside the owner**. **3 stale canon claims corrected:** `hover-elevate` IS defined (PX1-W1.1 closed it); `PageHeader.tsx` was **deleted**, not dormant (but `PageContainer` has 0 consumers, unregistered); NORTH1 § 8.1 cites the wrong line and asserts a **parallax that does not exist**. **Nothing implemented, no architecture modified, no screen redesigned** — two new files, neither under `docs/architecture/`. Roadmap by value + order by dependency; **the top item is blocked by nothing** and the first four steps are deletions/one-liners. **Stated gap: the surface was never observed rendered** (static analysis). | [`runs/EXPCOMP2_Home_Experience_Compliance_Audit.md`](./runs/EXPCOMP2_Home_Experience_Compliance_Audit.md) |
| `HOME2_Canonical_Home_Decision_Model` | Waiting for User | `rollback/HOME2-canonical-home-decision-model-20260716` → `7d1dd2ce` | Delivered — the Canonical Home Decision Model (`docs/investigations/ux/HOME2_CANONICAL_HOME_DECISION_MODEL.md`). **Headline: the model is designable, total and calm — and 6 of the mission's 10 named inputs DO NOT EXIST.** **The planner has no calendar date field at all** (`schema.ts:420-454`; only `weekNumber`/`dayOfWeek`) and **the server has no timezone/local time** (no column on ~90 tables, no `Intl.` in `server/`) — the attention/decision stack is *deliberately* clock-free (`shared/attention/decision.ts:20-22`). **THA cannot answer "what is planned for today."** Absent: meal preparation · goals · journeys · pantry freshness · reminders · time. Forbidden: `platform_observations` (OBS § 7 — *"any behaviour that reads an observation — routing — stop"*). **Duplicate-engine hazard settled FIRST (§ 2):** the door is **neither a Decision nor a Selection** — it is a **total resolver**. Proof is structural: **DEC1's output may legitimately be empty; the door may never be** — *a total function cannot be implemented by a partial one*. Deeper cause: DEC1 ranks **THA's** judgement; Experience Principle 4 requires the door to be the **household's** most likely intent — so the top opportunity as door would **invert** the principle it serves. Class = the **Behaviour Engine's `resolveBehaviour`** (total, fail-safe, *"never to silence"*). Standing = **peer**, on the Silence Rules' exact precedent (DEC1 § 6). **The model:** pure, zero-I/O, deterministic, clock-free, learns nothing, persists nothing; **4-tier ladder + unconditional floor** — Safety (DEC1 `critical`, consumed as-is) → Week unplanned → Trip pending → **Floor: the week**. The ladder is **the household's own sequence** (EXP ARCH § 10), not an invented priority. **Ladder not score** (a score = 2nd ranker/oscillates/unexplainable/tunable). **Conflicts impossible by construction**; the one real tie reuses `orderByAttention`+`clampLimit(1)`. **Muting silences notices, never doors** (scope, not a new rule). **Reads 4 owners, creates 0.** **Calm is a theorem, not a rule:** *the door changes when the household's state changes, never when THA's opinion changes.* **Gaps recorded not filled:** (1) **`TRANSLATION1` *Morning Rhythm* § 8 is law and is currently UNIMPLEMENTABLE** — needs a clock **and** a planner→calendar anchor; the latter is the real blocker. (2) `buildHouseholdHistory` **fabricates dates** (`routes.ts:11370`) — live **Core Principle 6** defect existing only because of gap 1. (3) `/api/home/intelligence` is **chartered NTC-P2 debt** and its `opportunity` field is **not DEC1's** (the file says so in capitals) — call-site must be a decision, not an accident. (4) **Pantry has no SoT domain**; two docs mis-cite D8–D11 for it. **Zero amendments. No conflict found** — the unimplementable rule is a *platform gap*, not an architecture defect; amending it would ratify a gap as law. **Closes NORTH1 § 8.3 with no amendment, no new fact, no schema change, no new service — buildable today.** | [`runs/HOME2_Canonical_Home_Decision_Model.md`](./runs/HOME2_Canonical_Home_Decision_Model.md) |
| `TIME1_Household_Time_Foundation` | Waiting for User | `rollback/TIME1-household-time-foundation-20260716` → `7d1dd2ce` | Delivered — the Household Time Foundation (`docs/investigations/platform/TIME1_HOUSEHOLD_TIME_FOUNDATION.md`). **Headline: THA does not lack time — it already tells the household the time in SIX places, with six private definitions, owned by nobody, and at least three are wrong.** So the Foundation is a **convergence, not an addition**: duplicate ownership of time is *already live*, and Principle 2's fail test is met six times over. **The discovery that reshapes it: `max(weekNumber)` ≡ 6.** All six planner weeks are created **eagerly at first touch** (`storage.ts:1220-1226`) and `weekNumber` is bounded 1–6 (`routes.ts:7182`) — so "current week" at `routes.ts:11503` is *the constant six*, a computation-shaped costume; meanwhile `dashboard.tsx:191` calls **Week 1** "This Week" (`:426`). **Two live surfaces, two constants, both named "this week", neither consulting a calendar.** The planner is **not a calendar and never was**: six reusable slots, no date, no `createdAt`, verified against the DDL — in a schema with 99 `timestamptz` columns, so it is a *design*, not an oversight. **No timezone on any table; 0 `Intl.` in `server/`; `date-fns` declared and never imported.** THA stores **instants** correctly and **civil time** not at all. **The model:** `shared/time/household-time.ts` — one pure, zero-I/O vocabulary owning the **rules** of time and **none of its data**, in the exact class Appendix A already records for **ATTN1/DEC1** (Principle 5). **Two facts, both on owners that already exist:** `households.timeZone` (D16 — *a clock is a property of the home, not the device in your pocket*; per-user rejected as a split-brain over one shared plan) and `planner_weeks.weekStartDate` (D14 — written **only at creation**, the only moment THA can honestly know what a week means; **existing rows stay NULL forever** — a back-filled anchor is `approxDate` with a schema). **Zero new domains, zero new stores, zero new writers, no new dependency** (`Intl` suffices). **Retires** 6 clocks · 4 `getGreeting()`s (boundaries 17 *and* 18) · 3 rival day arrays · `approxDate` ×2 · 2 rival "current week"s. **Day-of-week is declared, not migrated** (0=Sunday — correct by *undocumented coincidence*; `routes.ts:11371`'s "0 = Monday" is **false** and sits on the fabricated-date line). **The one-morning law is the load-bearing check and it survives:** `TRANSLATION1` § 3/§ 6/§ 8 **already require** THA to know the hour — *the law is right, the platform is behind it* — while § 9 forbids time reaching the light, so the design imposes a **hard boundary on itself** (time aims words + doors, never colour/palette/motion). **NORTH2 § 3.5's rejection of visual time stands undisturbed.** **Architecture impact: ONE register (Appendix A row + 2 domain rows), due at implementation, not now** — every Experience/Intelligence doc byte-untouched. **Migration: 5 phases, old behaviour is the floor** — `anchored:false` → today's behaviour, so nothing regresses, ever. **Live defects reported not fixed:** dashboard week chart **rotated one day**; Home/dashboard contradict; **`approxDate` is a season filter + occasion key, not the recency hint its docblock claims** (summer memories become autumn memories as the clock moves — worse than HOME2 recorded); `buildHouseholdHistory` duplicated; **8 timezone-naive timestamps incl. 2 security token expiries**. **Top open item: what happens when the six-slot window expires — a Planner product decision, deliberately not answered.** **Zero amendments. No conflict. Nothing implemented, no schema, no Planner redesign.** | [`runs/TIME1_Household_Time_Foundation.md`](./runs/TIME1_Household_Time_Foundation.md) |
| `TIME2_Household_Time_Consumer_Audit` | Waiting for User | `rollback/TIME2-household-time-consumer-audit-20260716` → `7d1dd2ce` | Delivered — the Household Time Consumer Audit (`docs/investigations/platform/TIME2_HOUSEHOLD_TIME_CONSUMER_AUDIT.md`). **Headline: every INSTANT consumer in THA is correct; every CIVIL consumer is broken; and the line between them is exactly the one TIME1 drew** — the platform has been failing along it for months, so the Foundation is less a proposal than a diagnosis. **The instrument: the CIVIL/INSTANT test** — *"if this household moved to Tokyo tomorrow, would this value have to change?"* Trial countdown, the 90-day evidence window and every cache TTL are **flawless — because a duration needs no calendar**, so the missing zone costs them nothing; the Companion's `TODAY`, freezer expiry, the diary's "today", Home's week and all of Stories are wrong, **all the same way** (local frame → `.toISOString()`). **20 domains audited** (11 named + **9 discovered**: Streaks · Savings · Health Trends · Product/Analyser · Trial/Auth · Learning · Caching · Observation/Analytics · Admin): **12 consumers · 5 MUST-NOT · 3 correct-by-abstinence.** **The must-nots matter as much as the musts** — making the trial countdown household-aware would be a *new* defect wearing a canonical badge (§3.3), so the matrix records `MUST NOT` as a **permanent verdict, not a backlog**. **3 of the 5 mission confirmations REFUTED:** *duplicate "today"* — **5 rival current-weeks** (3× always Week 6, dashboard always Week 1, **Home's localStorage always 1** — so Home shows Week 6's plants above Week 1's dinners), **19 week-shape declarations across 16 files**, **5 live frames**; *duplicate season* — **3 implementations** (2 byte-identical), duplication **documented as a virtue** (*"three engines, one season truth"* — a comment, not an owner), and **the register's declared owner (D11 `seasonal/engine.ts`, private, 1 consumer) is NOT the actual owner** (D8's `discovery/seasonal-map.ts`, 5+ consumers) — a Rule 1 defect independent of time, fixable with no zone/anchor; *no schedulers* — **holds vacuously, none exists**; only *single owner* + *no duplicate events* hold, both conditionally. **3 CORRECTIONS TO TIME1:** (1) `dayOfWeek` is **not** "correct by coincidence + one false comment" — the server is **genuinely split** and the misreading is in **live code**: `storage.ts:3345-3348` seeds **every new household's starter meals onto Sunday–Wednesday while calling them Monday–Thursday**; Principle 1's fail test is met **inside one file** (`:3074` vs `:3345`); (2) TIME1 **undercounted** (3 day arrays → 19; 2 current-weeks → 5); (3) **TIME1 § 9's retirement list is incomplete** — must be amended before Step 0 or two-thirds converges and the rest looks sanctioned. **THE SEQUENCING FACT:** Stories says *"Friday became curry night"* — a claim about **a date that was never real**; **household time will NOT fix it** (no real date underneath), so Stories is an **anchor dependant, not a convergence target**, and "fixing its timezone" first would make a fabricated date *precisely* wrong. **Roadmap: Step 0 is GOVERNANCE, not code** — the register names no time owner and TIME1 is **not indexed as governing**, so nothing can converge onto it; **Step 2 (zone) unblocks 7 of 12 without touching the Planner** (the expensive anchor is *not* on the critical path for most harm); Step 4 (anchor) gates Steps 5–6. **14 live defects reported not fixed, 6 user-visible** — incl. **2 SECURITY, out of band, do NOT sequence behind Step 0**: `password_reset_expires` declared naive while the DB is `TIMESTAMPTZ` (wrong-instant expiry / extended security window if process TZ ≠ UTC), and `email_verification_expires` **has no migration at all** (push-only) — both INSTANT-domain, the Foundation will never touch them. **Two mission deviations, reported:** the specified path `docs/investigations/TIME2_…md` **violates governing architecture** and would **fail `repo-structure-verify.sh:52-54`** → filed under `platform/` (gate re-run: **PASS**); and *"governing"* + *"under docs/investigations/"* are **mutually exclusive** (README:4; CONVENTIONS:47/89-93) → written as an investigation, promotion path named (§13.2, recommended **against** — an audit measures, it does not govern; **TIME1** is the thing that should govern). **Architecture index update: NOT required** (architecture README indexes governing docs only; investigations README is a *workstream* index). **Next: TIME3 — the TIME1 register amendment (Step 0), docs only.** | [`runs/TIME2_Household_Time_Consumer_Audit.md`](./runs/TIME2_Household_Time_Consumer_Audit.md) |
| `HOME3_Home_Primary_Action_Implementation` | Waiting for User | `rollback/HOME3-home-primary-action-implementation-20260716` → `7d1dd2ce` | Delivered — **the first implementation in the North Star → Blueprint → Behaviour → Compliance chain**; every link above it was a document and nothing had touched the product. Builds `HOME2` § 4's **Home Primary Action Resolver** (`shared/home/home-primary-action.ts`): pure, zero-I/O, **total**, deterministic, clock-free; 4-tier ladder (Safety → week unplanned → trip pending) + **unconditional floor**; reads 4 owners, creates 0; consumes DEC1's governed bundle as-is at Tier 0 and reuses `orderByAttention` + `clampLimit(MAX_DOORS)` (no module-local sort/clamp — DEC1 § 4). **47/47 pass** · typecheck clean · dec1 49 / attn1 29 / behaviour 119 / OD1 60 / notice 65 all green. **One deviation from HOME2, on Principle 2:** `destination` is a **DOMAIN, not a route** — `domainLandingPath()` is already *"the single place ref/domain → THA path is decided"* (`companion-card.ts:162`), so the resolver returns the producers' own `FoodOpportunityDomain` vocabulary and the caller resolves the route through the existing owner. **Verification found its own bad test:** the 10 negative assertions were mutation-tested; 9 had teeth, and the "no user-facing prose" grep was **vacuous** — green, and blind to the exact hardcoded label it existed to catch. Replaced with a property (*every emitted string is contract vocabulary or carried verbatim from the input*) which **fails** on that mutation. **Findings reported not fixed:** (1) the **Adoption Register gate walks `client/src` only** (`adoption-register-gate.ts:47`,`:158`) — UIA § 17's *"authored-but-unadopted must be impossible to hide"* does **not** hold for `shared/`, and this resolver is itself unadopted with the gate green about it; (2) `adoption:check` **fails on main**, 2 pre-existing failures (button ceiling 539>538; `HouseholdNutritionPanel.tsx` orphan); (3) the **two "current week" definitions disagree** (server `max(weekNumber)` vs client `localStorage`, default 1) — a live input to the call-site decision. **No architecture modified, no schema, no route, no UI, no governing doc touched.** **Two user decisions taken:** call site → **a new dedicated route** (`GET /api/home/primary-action`, avoiding NTC-P2's debt entirely) — **not built**, blocked by the second; current week → **surface the disagreement first**. **The finding that blocks it** (`docs/investigations/platform/HOME3_THE_TWO_CURRENT_WEEKS.md`): **THA has two current weeks and Home already contradicts itself.** Server `max(weekNumber)` (`routes.ts:11503-11505`) ≡ **the constant 6** — six weeks created eagerly in one transaction at first touch (`storage.ts:1220`), ceiling 6 (`routes.ts:7182`), confirming `TIME1` independently. Client `localStorage["planner:active-week"]` ≡ **1** by default (`home-experience-page.tsx:54`) — **five readers, one writer** (`weekly-planner-page.tsx:276`, only on a manual pick), **never seeded**, device-local, therefore **not household state**. **Live defect new to the canon: Home renders Week 1's meals (`:139`) beside Week 6's plant count (`:160`) — one component, one screen, 21 lines apart.** Sharper than EXPCOMP2's *"unfalsifiable"*: Home doesn't merely fail against a calendar, **it fails to agree with itself, deterministically.** **Both candidates refused** — each falsifies `HOME2` § 6.1's theorem (`max` never moves; `localStorage` moves when you change **device**). **The resolver is correct; there is nothing correct to feed it.** Routes to `TIME1`'s `weekStartDate` (D14) + **`TIME2`, whose consumer audit the door is exactly an entry for.** **HOME STILL HAS NO DOOR: the function exists and nothing calls it** — and it is total without a week (`week: null` → floor), so the route is mechanical the moment a week means something. | [`runs/HOME3_Home_Primary_Action_Implementation.md`](./runs/HOME3_Home_Primary_Action_Implementation.md) |
| `TIME3_Household_Time_Architecture_Promotion` | Waiting for User | `rollback/TIME3-household-time-architecture-promotion-20260716` → `7d1dd2ce` | Delivered — **Household Time is now a recognised platform capability.** Created the governing **[`THA_HOUSEHOLD_TIME_ARCHITECTURE.md`](../../docs/architecture/THA_HOUSEHOLD_TIME_ARCHITECTURE.md)** (19 §§, rules **HT1–HT18**), promoted from TIME1 (the model) + TIME2 (the consumer findings). **This was TIME2's Step 0 — the governance act that had to precede all code.** **Recognition verified on 4 tests:** doc at canonical location · indexed in `docs/architecture/README.md` (table + blockquote + History) · **owner declared in the SoT Register's Appendix A** on ATTN1/DEC1's exact footing · **both facts declared with their existing owners** (`households.timeZone` → D16; `planner_weeks.weekStartDate` → D14). Also +1 row to `ARCHITECTURE_PRINCIPLES.md`'s Domain Ownership Quick Reference. **Canonical owner: `shared/time/household-time.ts` — owns the *rules* of household time and NONE of its data** (Principle 5; the class Appendix A already records for ATTN1/DEC1). **No new numbered domain, no new store, no new write funnel, no new dependency** (`Intl` suffices) — exactly as TIME1 §13 specified; **not redesigned**. **⚠️ DECLARED, NOT BUILT (§17)** — module + both columns do not exist; this is the **order CPuBA §Transition Rules *requires*** (*"every new domain must be declared before implementation"*), verified at `CANONICAL_PUBLICATION_ARCHITECTURE.md:462-465`. **What is in force is the LAW, not the code:** from 2026-07-16 an implementation that invents a "today", writes a fifth `getGreeting()`, or adds a sixth "current week" **violates governing architecture whether or not the module exists** — that is the whole value of declaring first: **it stops the duplication growing while the module is written**. **Incorporated from TIME2 verbatim, not re-derived:** the 21-domain consumer matrix (§8) · the **CIVIL/INSTANT model** (§7 — *"if this household moved to Tokyo tomorrow, would this value have to change?"*) · the **5 domains that MUST NEVER consume** (§8.1 — Trial/Auth · Learning · Caching · Observation *(forbidden outright by OBS §7)* · plus Shopping & Shared Plan acquiring no clock) — **a permanent verdict, not a backlog**, because *a duration is not a date* and converging them would be a **new defect wearing a canonical badge** · the dependency graph (§10). **Boundaries cite their owners, restate nothing:** time aims **words and doors, never light** (HT13 → TRANSLATION1 *Morning Rhythm* §9 — **the one-morning law and NORTH2 §3.5 both stand byte-untouched**) · time **triggers**, INT20 **decides** (HT14) · the model reads time **only** via an INT17 Context View (HT15) · the attention stack stays **clock-free** (DEC1) · **season is NOT Household Time** (HT17 — input, never answer). **§14 retirement targets** (Principle 8, in the doc that introduces the replacement): 5 rival current-weeks → 1 · **19 week-shapes across 16 files → 1** · 4 `getGreeting()` → 1 · 3 season impls → 1 · 2 fabricators → 0 · **5 live frames → 1**. **TIME1 + TIME2 retired as sources of rule** — banner only, **no analysis altered**; they stay at their paths as history holding the *reasoning* and the *evidence* (incl. 14 live defects) the architecture deliberately does not. **Decisions:** (a) bannered despite the unbannered DEC1 precedent — TIME1 §13 and TIME2 §10.2 both contained recommendations **this promotion completed**, so unmarked they'd cause the next reader to redo the work; (b) **no verification entry added** to `publication-register.ts` — application code (forbidden), and a check on a non-existent file fails by design → lands with Phase 1; (c) **Domain 11's season-ownership inversion NOT resolved** — that's Phase 1a, a workstream; TIME2 left it open and this promotion does not decide it. **No application code, no schema, no migration, no roadmap change, no new capability, no redesign.** `package.json`'s diff is a **concurrent HOME3 session's**, not TIME3's. **Next: TIME4 — Phase 1, the module** (pure, zero-I/O, zero consumers, reversible by deleting one file; **must land with its verification entry**). **Still out of band: the 2 security defects** (TIME2 §9.4 #11/#12) — Household Time will never touch them. | [`runs/TIME3_Household_Time_Architecture_Promotion.md`](./runs/TIME3_Household_Time_Architecture_Promotion.md) |
| `LIFE2_Remove_Fabricated_Age_Assumptions` | Waiting for User | `rollback/LIFE2-remove-fabricated-age-assumptions-20260716` → `7d1dd2ce` | Delivered — **LIFE1 Step 0: the fabricated age is gone.** `server/routes.ts:568`'s `const bmr = 10*weightKg + 6.25*heightCm - 5*30 - 78` is **deleted** — an age hardcoded to **30** and `-78`, the arithmetic midpoint of Mifflin–St Jeor's male (+5) and female (−161) constants, **a sex nobody is**. A 180 cm / 80 kg household was shown **2,630 kcal** captioned *"based on your weight, height, activity level and goal"* — a sentence **false about itself**. **Not a degradation but a REMOVAL:** the equation needs weight·height·**age**·**sex**; THA holds the first two and LIFE1 § 12.4 defers both of the last two, so the estimate is **impossible, for every household, always** → `calorieEstimate: {available:false, reason:"age-and-sex-unknown"}`, an explicit honest gap rather than a bare `null` (which reads as *"not computed yet"*). **Machine reason code, no prose** — the words stay client-side, because they are INT21's, never an API's. `calculatedCalories` **removed from the contract**, not left permanently-null (**Principle 8**: a field named *calculated* that can only ever be null is a claim about a calculation that no longer exists); safe because `buildProfileResponse` is a **non-exported closure** — no external consumer exists. `dailyCalories` is now **only** a target the household declared themselves. **THE LEAK THAT HAD TO BE STOPPED:** `storage.ts:3405` seeds `calorieTarget: 2000` **alongside `calorieMode:"auto"`** — reporting the stored target regardless of mode would have shown every seeded household **2,000 kcal they never chose**, swapping one fabrication for another; **Case C exists solely to prove it doesn't leak. It doesn't.** **THE COMPANION WAS NEVER AFFECTED — by design, not luck:** `profile-read-handler.ts:15-20` refuses to recompute *"the derived fields the route layer computes inline (BMI, calculated calories…)"* — **3 independent proofs**: structural (non-exported closure; only 2 `res.json` call sites), by projection (reads `calorieTarget`/`calorieMode` straight from the owner's prefs), by test (INT12 **50/50** before and after). ***The Intelligence Platform's delegation-only boundary quarantined this defect from the one surface that speaks — the architecture protected the Companion from the platform.*** **UI: the fabrication would have SURVIVED the server fix** — a radio labelled **"Auto calculate (recommended)"** that calculates nothing, marked *recommended* → **"No daily target"** / **"Set my own target"**, and the false *"Calculated: 2,630 kcal…"* → *"THA won't work a target out for you. Doing that honestly needs your age and sex, and THA doesn't hold either."* **No schema change** — `calorieMode` keeps its enum and values; **only the labels were lying.** **VERIFIED BY DRIVING IT**, live server on **5099** (5000 runs stale code — `tsx` doesn't hot-reload; the concurrent session's server left untouched): **A** auto+height+weight (*the exact fabricating state*) → `null` + honest gap, `bmi 24.7` intact ✅ · **B** manual 2200 → `2200` (theirs) ✅ · **C** back to auto → `null`, **no leak** ✅ · **D** no height/weight → `HTTP 200`, no throw ✅. **843 tests passed, 0 failed** (INT12 50 · dietary-safety 39 · context 166 · platform 33 · INT13 51 · companion-actions 62 · personality 323 · behaviour 119). **Typecheck 304 → 304 — identical**, measured by stashing *only these 2 files*; **0 errors in either.** **⚠️ HONEST EVIDENCE GAP: the rendered page was NOT observed** — Playwright installed but Chromium won't launch (`libglib-2.0.so.0` missing; the root `.glibcheck.txt`/`.libdirs_uxhome.txt` strays are a **concurrent session diagnosing this same library**). API behaviour is observed fact; the UI table is **read from the diff and type-checked, not seen** — stated per `EXPCOMP2`'s precedent, with a `data-testid` added to close it later. **2 NEW findings, reported not fixed:** (1) **BMI's category thresholds are adult-only** (`routes.ts:560-563`) — child BMI needs age-and-sex percentile charts; **the same class of defect as `- 5*30`, one order quieter — it fabricates no number, it fabricates a category boundary**; LIFE1 missed it; belongs to Step 2. (2) the manual box **pre-fills 2000** — a UI default, not a claim, but a household saving unedited gets a target **THA suggested**. **Scope lock held:** no Life Stage · no birth date · no schema/migration/column · no new calculation · no inference · **NK1's false claims deliberately NOT amended** (Step 0a — an implementation must not amend architecture as a side effect) · 2 pre-existing `adoption:check` failures **not absorbed** (raising a ceiling to pass a gate is the exact dishonesty this workstream removes). **Files: 2 application + 1 report** (`docs/implementation/platform/…` — the mission's root path violates STEP 5; filed by workstream, gate **PASS**). **Next: `LIFE3` — Step 0a, correct NK1's false inventory** (`:73`/`:139`/`:535` claim eater age is stored & *"Authoritative"*; the column doesn't exist — and `:418`'s *"no per-child signals"* cites a **§6.3 that does not exist**). **Do NOT proceed to Step 2 (the column): children's personal data has no governing owner, and unlike everything else it is not reversible by dropping a column.** | [`runs/LIFE2_Remove_Fabricated_Age_Assumptions.md`](./runs/LIFE2_Remove_Fabricated_Age_Assumptions.md) |
| `LIFE1_Life_Stage_Intelligence` | Waiting for User | `rollback/LIFE1-7d1dd2ce-20260716` → `7d1dd2ce` | Delivered — Life Stage Intelligence (`docs/investigations/platform/LIFE1_LIFE_STAGE_INTELLIGENCE.md`), built on `TIME3`'s governing Household Time Architecture. **HEADLINE: THA does not know how old anyone is — and in the one place it needs an age, it asserts everyone is thirty.** `routes.ts:568` — `- 5 * 30` — hardcodes age 30 (and `-78`, the **sexless midpoint** of Mifflin–St Jeor's male/female constants) for **every human being**, then surfaces it as a *personalised* calorie target: a 19- and a 68-year-old of equal height/weight get **byte-identical** numbers. **This INVERTS TIME1 rather than repeating it** — time was answered *six times* by nobody's authority; **age is answered once, by a constant, in silence.** So Life Stage has **nothing to converge**; its first act is a deletion of a **fabrication**, not of duplicates. **The mission's own noun is declined (§ 2):** Life Stage is **not a capability but a vocabulary** — TIME1 § 12.2's exact reasoning (*"time alone answers no household question"*); nobody opens THA to ask what life stage their child is in. Class = ATTN1/DEC1/**TIME3**, which Appendix A already records **three times**. **NEW DEFECT CLASS (§ 4):** `NK1:73`/`:139`/`:535` — **governing architecture** states eater **age** is stored and **"Authoritative"**; the column **does not exist** (`schema.ts:1158-1168`). Not a law ahead of its platform (TIME1), not a right canon wrongly rendered (NORTH2) — **a governing document factually wrong about the system it governs**, over-claiming *authority* on a **safety-relevant** field. *"A law ahead of its platform costs patience; a false inventory costs trust the moment somebody believes it"* — and `undefined` **fails open**. **`NK1:418` — the canon's ONLY per-child rule (*"no inferral, no per-child signals"*) cites a §6.3 that DOES NOT EXIST** (NK1 has **no numbered sections at all**; `6.3` occurs once — in the reference). Recorded, **not written**. **SCOPE DEFECT (§ 5):** `user_preferences.adultsCount/childrenCount/babiesCount` = a **Domain 16** fact held by **Domain 27** at **user** scope (a domain the Register already marks *Contested*) — two adults in one home can disagree, nothing reconciles them, **nothing computes with them**. *"A household's composition is a property of the home, not of whichever adult last opened the profile page"* — **the split-brain `HT4` refused for the time zone, in the same register domain, already live.** **SEVEREST (§ 7):** `babiesCount` reaches the model in **90 of 100 prompts** (`context-view.ts:189-192`) and **gates nothing**, while the infant-botulism rule is a **code comment** (`restriction-library.ts:1169`) above a resolver taking **only declared strings** (`:490-494`) — in the file whose own docblock says ***"a restriction the platform cannot enforce is worse than one it never accepted, because the household believes it is protected"*** (`:503-506`). **SURF1B2 closed this exact hole for `meat`/`fish`/`honey` and left it open on composition.** Stated honestly as a **structural hazard, not a demonstrated harm** — the Companion disclaims (*"I don't know the ages of your children"*), so **the platform is protected by the model's manners, not by a gate.** **`kind: "child"` means *has no account*** (`household-eater.ts:107`) — a grandparent is a "child", a 15-y-o with a login is a "user"; unread by prod, **but it reaches the model**: a loaded gun. **THE MODEL:** `shared/life-stage/life-stage.ts` (pure, zero-I/O, **total** — `known:false` first-class) + **ONE fact on an existing owner**: `household_eaters.birthYear`/`.birthMonth` (**D16**; HT2's shape — no new domain/store/funnel). **THE DECIDING ARGUMENT (§ 9.4): the fact must be a DATE, never a STAGE** — a declared stage is a **stored derivation** that rots silently as the child grows (**HT3**), i.e. ***`approxDate`'s exact defect wearing a kinder name***. *"The birth date is not the most precise form of this fact — it is the only form that stays true without anyone touching it."* **Month precision suffices for every boundary rule; day precision buys only birthdays — celebration, not safety.** **THE INSTRUMENT (§ 8) — the Boundary/Curve test:** **Boundary** (honey <12mo) = step function, fails **closed**, extends **Rule T0 + the resolver that already exist** → **RECOMMEND**; **Curve** (iron for a 4-y-o) = needs age-banded intakes → **REFUSED** (`household-nutrition.ts:59-65`: *"inventing one would be fabricating certainty"*; NK2's **household-level** unit `:179`/`:213`; Register Rule 8). **The cheap half IS the safety-critical half — nothing is traded away.** **TRUST (§ 10): declared → derived → NEVER inferred** — already law 3× (`HT16`; OBS § 7; `NK1:418`): *THA must never conclude a baby exists from a formula scan — that is surveillance of a family's table.* **PLACEMENT (§ 11): a T2-only consumer, `Owns: Nothing`, NOT blocked by Phase 2 (zone) or Phase 4 (★ anchor)** — CIVIL with a tolerance in **months**, consuming a value that errs by **hours**. **§ 11.1 — why it needed TIME3 first: life stage is THA's FIRST TIME-DERIVED HOUSEHOLD FACT** — the only fact whose *meaning changes while nobody touches it*; every other fact is time-invariant, so a platform with no owner of *today* could only hold a label and watch it rot. **GAP (top open item): children's personal data has NO governing owner** — no minors/consent/retention rule anywhere; THA demands *"legal/regulatory review"* for S-3 biomarkers, and **a child's DOB is at least that sensitive and is asked of every household**. Gates Step 2 — **not the module, not the retirements.** **Architecture impact: one doc CORRECTION, one register row, two fields — only the correction due now.** Household Time **NOT amended** (a consumer does not amend what it consumes); NK2 **NOT amended** (§ 8's refusal exists to keep it so); Experience canon **byte-untouched**. **Nothing implemented, no code, no schema, no architecture modified — one file created.** **Next: `LIFE2` — Step 0, delete `routes.ts:568`: one line, no schema, no Household Time dependency, blocked by nothing — the only thing here that is *wrong today* rather than *absent today*, and its fix is a deletion.** **Do NOT start with the column:** a birth date collected before the legal question is answered is **not reversible by dropping a column**. | [`runs/LIFE1_Life_Stage_Intelligence.md`](./runs/LIFE1_Life_Stage_Intelligence.md) |
| `ORCH1_Household_Journey_Blueprint` | Waiting for User | `rollback/ORCH1-household-journey-blueprint-20260716` → `7d1dd2ce` | Delivered — the Household Journey Blueprint (`docs/investigations/ux/ORCH1_HOUSEHOLD_JOURNEY_BLUEPRINT.md`), 11 stages × 6 questions. **HEADLINE: THA is a product about eating well together, and it can observe everything except eating.** It witnesses **intention** at high fidelity (every plan, list, scan) and **outcome** — cooked, eaten, shopped, enjoyed — **not at all**. Three proofs: `planner_entries` has **no completion column**; **"mark as cooked" writes to `localStorage` only** (`weekly-planner-page.tsx:280` — browser-scoped, so *the other adult in the household cannot see dinner was cooked*); and **`MealSource="logged"` has ZERO producers** though it is typed + documented (`shared/stories/types.ts:37`, `:20-24`) — *the honest branch was designed, typed, documented, never wired*. **And there is nowhere in ~90 tables for a family to say they liked their dinner** (no `meal_ratings`; the only human rating is 👍/👎 **on a Companion reply**). **THE CANON ALREADY KNEW:** `EXP ARCH §17.9` names the qualifying premium moments — *"a first plan made, a first list completed"* — and **does NOT name a first meal**; the trust model is written in the code; a test asserts *"the platform cannot date a milestone, and does not pretend to"*. **The horizon is deliberate, documented 4×, and test-enforced — protecting it is the blueprint's first job**, because the alternative to a horizon is surveillance of a family's table. **THE REAL DEFECT is subtler: the household is ALREADY TELLING THA what happened and nothing is listening** — the diary holds genuine `sourceType='manual'` "I ate this" evidence that `buildHouseholdHistory` **never queries**; `sourcePlannerEntryId` **exists and is set to `null`** in the one path that could close the loop; `stuckToPlan` + weight/mood/sleep are **read by no narrative**. **ZERO NEW DOMAINS REQUIRED** — the Diary (**Domain 21**) has been the outcome owner all along; **every piece exists. What's missing is a wire.** *(Third consecutive investigation to find THA's competence present, correct, and never connected.)* **THE MODEL: three zones** — what THA *sees* (intention) · what the household *tells* it (outcome, authored not observed) · what it *never knows* (Zone 3, correctly absent). **Trust = what the household chooses to TELL, never what THA widens its sight to see.** **T0–T5 model: the household is already at T3 (confiding weight/mood) while the platform behaves as if at T1.** **SEVEREST FINDING (Stage 8): the ONLY "elite" health-streak input is *tapping a product in the Analyser*** (`products-page.tsx:747,773`) — **THA's entire long-term health narrative rests on Analyser page-views**; a household cooking 5 whole-food meals a day that never opens the Analyser has a **permanent streak of zero**. **§17.9 audit: of the canon's 3 named premium moments THA can detect ONE** — and **the one milestone it DOES celebrate (7-day streak) fails §17.9's own test** ("a milestone the household would themselves recognise"). **Loyalty finding: every genuine loyalty moment THA has is a moment where THA DID LESS** — the list already right, the substitution remembered without mentioning, the allergy honoured unwitnessed, **the week it said nothing**. So **"indispensable" can never be pursued** — it's a residue, not a goal; resolved against `EXP ARCH §11` (*"never time in the product"*): **indispensable to the household's LIFE, never to their ATTENTION**. **THA's own declared success measure — *"the household ate better with less effort"* — is a sentence it cannot evaluate.** **Principles: 4 candidates, 2 REFUSED** under NORTH2's gate; the 2 survivors (**the horizon is a feature** · **the household tells, THA doesn't watch**) are both about the same unowned subject: **the boundary of what THA may know**. **Future architecture recommended: ONE amendment (EXP ARCH §12 Trust extension), ONE scope note (EXPLANG §5 — the Rhythm governs the visit, not the relationship), ONE future Blueprint §5.1 row (Community, before it's built) — and NO new documents**; explicitly recommends **against** promoting ORCH1 (a snapshot, stale the day Stage 3 lands). **Community: no code, no register domain, and NO ROOM in the map of the house** — the only domain that could be *born* correct. **No UI, no implementation, no feature list, no new domain, no architecture amended.** **Next: fix the live false claim** — `CookbookMealIntelligenceStrip.tsx:183` renders "Cooked N times" from planner counts, the **only** surface breaking the trust model the rest of the codebase defends. | [`runs/ORCH1_Household_Journey_Blueprint.md`](./runs/ORCH1_Household_Journey_Blueprint.md) |
| `PLAN1_Orchard_Planner_Blueprint` | Waiting for User | `rollback/PLAN1-orchard-planner-blueprint-20260716` → `7d1dd2ce` | Delivered — the Orchard Planner Blueprint (`docs/investigations/ux/PLAN1_ORCHARD_PLANNER_BLUEPRINT.md`), the Planner defined as a **place**, in principles, no screens. **THE MISSION'S NOUN IS REFUSED AND ITS INSTINCT IS CANON.** *"Define the Planner as **the kitchen**"* cannot be carried out as worded: the Planner is **already named 4×** — `BLUEPRINT:172` **"The family table"** · `:146` *"the family's planning table"* · `OHDB:230` · `OLB:61` — and renaming is forbidden **twice independently** (`EXP ARCH:211` one-name-per-concept; `BLUEPRINT:132` *"the home is never renamed"*). **"Kitchen" is the HOUSE, not a room** — spent 4× as the product's one-sentence feeling (`EXPLANG:36-38`), the **Companion's address** (`BLUEPRINT:325`, *"comfortable in every room"*), THA's voice (`EXP ARCH:208`,`:276`), and **the theme park's first noun** (`BLUEPRINT:439` *"illustrated kitchens"*); `NORTH1:99` — ***"There is no kitchen in the software. There never was."*** **But the canon already grants the intent, verbatim:** `EXP5:316` — *"Room / place analogy. **The kitchen table** with the week laid out on it."* → **the house is the kitchen; the Planner is the table in it.** **The mission is the first live instance of the risk NORTH1 predicted** (`:18` — *"so persuasive that a team will copy the picture instead of extracting the principle"*): the render's noun arriving in a brief **one day later**, attached to a room — recorded as evidence NORTH1 was right, not as criticism. **CREATES NO LAW, and that is the finding:** NORTH2's gate run on all 7 deliverables → **6 owned with no remainder** (philosophy · feeling · Companion · orchard · delight · anti-patterns), so the deliverable is **composition + citation**; NORTH2's verdict holds unmodified — the architecture ***"needs obeying"***. **THE ONE GAP (§6) is the word the canon uses most about this room: *together*** (`BLUEPRINT:146`,`OHDB:232`,`OLB:70`,`:21` — 4 uses, no owner). **D14 owns the week's state, D16 owns the people, NOTHING owns the relationship.** `planner_entries` records **no author** (`schema.ts:438-458`) while `shopping_list` records one **and surfaces it by name** (`:177`; `storage.ts:2814-2829`): ***"Dad added this" is already built — for the list by the door, not for the family table.*** **The plan knows every mouth at the table and no hand that set it.** Named + routed, **not filled** (PKR1 R7). Deeper: **the shared table draws from private books** — `meals.userId` + `routes.ts:6039-6042` mean **a member cannot put another member's recipe on the family week**, invisible in every single-member household; **"together" is last-write-wins** (no presence/history/realtime — `ws` is a Neon transitive dep imported by nothing); and **even `OLB:63-70` says "together" twice then addresses one pair of hands** (*"steady under **your** hand"*) — the gap showing up in the canon's own prose. **2 CORRECTIONS — EXP5 §7 is STALE IN THE HOUSEHOLD'S FAVOUR:** the grid is **NOT** *"a lattice of translucent cells floating on landscape"* — it is **one `Card` + one ruled CSS grid, `shadow-none`** (`:2316-2322`; `card.tsx:12`; `:2403`) → ***the Planner ALREADY IS the single solid ground plane the canon asks for***, the hardest posture in the canon, **and no document knows it**; and empty slots are **already bare table** — **zero `dashed` anywhere** (`:2636-2650`). **This inverts the work: the Planner needs CLEARING, not building.** Plus a **dead path** — `EXP5 §7.4`/`NORTH1 §8.1` cite `components/orchard-backdrop.tsx`, which **does not exist** (it is `components/layout/`), so a verifier finds nothing and may conclude it was fixed. **It was not.** **4 LIVE FINDINGS, reported not fixed:** (1) **the strip announces a zero before you have cooked anything** — `PlannerIntelligenceStrip.tsx:152-156` renders unconditionally, `plantCount` computes locally to `0` (`:120`) → **"🍎 This Week · 🌱 0/30"** on an empty week; **same coercion, same metric, second room** as EXPCOMP2's Home finding; (2) **the Planner has no Arrival beat — it has a lobby**: **5 always-on strips** between the room's edge and the first cell (2× `FirstVisitHint`, tabs, diets, **two stacked ungoverned notice channels**) in a **4,252-line** file — and **WX13 "fixed" this by *compressing* the panel, which is now item 5**; (3) **the wash is real, global, and not where EXP5 said** — `layout/orchard-backdrop.tsx:18` **hardcodes `opacity:0.90`** on every room and `card.tsx:12` `bg-card/82` bleeds **18% orchard up through the grid cells and behind the meal text** (`BLUEPRINT §6.2`: *"the orchard is never behind working text"*) — **and `--orchard-opacity` (`index.css:73`,`:155`) is defined and consumed 0×, so dark mode gets a 90% orchard it was designed not to have**; (4) **today is unknowable** — **zero `Date` in 4,252 lines**, no date column in `planner_weeks`/`planner_days` → ***the room built to hold the household's week does not know which day is today***, so **"the sun on today" — its ONE permitted Living Detail — is unimplementable, and the blocker is not visual**: it is `planner_weeks.weekStartDate` (D14/HT7, declared+unbuilt). **The Planner is NOT in TIME2's consumer matrix — it is the room the anchor exists for.** **Delight bounded by `EXP ARCH §17.9`, applied not invented:** the Planner has **exactly one** qualifying moment — *"the plan is made"* — whose register is **rest, not celebration**; **the streak is recorded as REFUSED** (`WX3:121-129` already lists "Family Meal Streaks" as unbuilt — documented, unbuilt, **forbidden**; without a recorded refusal a future workstream finds it in a suggestions list and builds it); and **the Planner may celebrate a plan, never a meal** (it is the source of `CookbookMealIntelligenceStrip`'s false *"Cooked N times"*). **2 of my own citations found wrong on verification and corrected before delivery.** **No screens, no code, no schema, no architecture modified — all 7 governing Experience docs byte-untouched; README untouched.** Gate: `docs/investigations/ has no loose files` **PASS**. **Next: (1) delete the fabricated zero (one guard — and it should be ONE workstream with EXPCOMP2's identical Home defect); (2) clear the lobby — REMOVAL, not compression; (3) NTC-P2 (already chartered); (4) PLAN2? — name & route co-authorship, the only item that is not a fix ("Shared Plan" is already taken).** **Explicitly NOT recommended: any Planner visual prototype** — `EXP5:951` fixed the sequence and **P1 (Home) has not run**, so the reference photograph every room is calibrated against does not exist; *"if the concept survives the Planner, it survives everywhere"* is a reason to go there **last**. | [`runs/PLAN1_Orchard_Planner_Blueprint.md`](./runs/PLAN1_Orchard_Planner_Blueprint.md) |
| `COOK1_Orchard_Cookbook_Blueprint` | Waiting for User | `rollback/COOK1-orchard-cookbook-blueprint-20260716` → `7d1dd2ce` | Delivered — the Orchard Cookbook Blueprint (`docs/investigations/ux/COOK1_ORCHARD_COOKBOOK_BLUEPRINT.md`), the Cookbook defined as **the family's cookbook**, in principles, no screens. **NO NOUN DEVIATION — AND THAT IS THE FINDING. It inverts PLAN1.** PLAN1's brief named its room wrongly and the canon was right; **this brief names the room exactly right** — *"the family's cookbook"* is `BLUEPRINT:147` verbatim (*"**The Cookbook is the family's living cookbook** — the used, well-thumbed book of what this family cooks, not a catalogue of what anyone could"*) — **and the PLATFORM is wrong.** **HEADLINE: the Cookbook is the only room in the house named for a family that does not have one — and its five stories are all written, all running, and all told in the Pantry.** **The room's name makes two claims and the platform backs neither:** ***family*** — `meals.userId` **notNull**, **no `householdId` on `meals` at all** (`schema.ts:88-137`), `getMeals` strictly user-scoped (`storage.ts:469-471`), no `getMealsForHousehold` anywhere → **a member cannot see another member's recipes; the family's cookbook is *N* private books**; ***living*/*"well-thumbed"*** — **no cook record exists in ninety tables** (no `cooked_at`/`cook_count`/`markAsCooked`) → **the room's one Living Detail is sourced from a fact THA does not have.** **AND IT WAS NEVER UNDISCOVERED — it is written down twice, in one directory, by two documents never read together:** `capabilities/meals.md:29-32` is **also governing architecture** and already says *"own-data is **USER-scoped** … **There is no household-level meal scoping anywhere in the owner**"* — Experience Governance vs Architecture→Capabilities, both current, both in `docs/architecture/`. **The gap is not a discovery; it is a directory that does not read itself.** **4 OF THE MISSION'S 9 DELIVERABLES WERE DESIGNED, TYPED, TESTED AND SHIPPED — INTO ANOTHER ROOM:** favourites · discovery · traditions · seasonal are **4 of the 5 WS10 story types** (`shared/stories/types.ts:27-32`; 752-line engine + test suite), whose **only** UI consumer is `PantryKnowledgeHub.tsx` over **`/api/pantry/stories`**; ***"Family favourites" is a Pantry Explore topic with a ❤️, filed between "gut health" and "seasonal foods"*** (`:121`). **The family's living cookbook has never been told the family's story.** **The engine refuses rankings in the canon's own words** (`types.ts:12-18` *"NO rankings, NO scores, NO streaks… only observations, only recognition"* ≡ `BLUEPRINT:312` *"never a badge, rank, or label"*) — **written independently, 18 months apart, never introduced.** **THE RICHER THE FAMILY'S HISTORY, THE FEWER STORIES THEY SEE:** `PantryKnowledgeHub.tsx:828` renders `.sections.slice(0, 2)`; sections emit in fixed order (`engine.ts:244/372/519/584/665`) and empty ones are never pushed (`:706`) ⇒ **traditions render only for a household with ≤1 of {favourites, discovery}** — ***"Friday became pizza night" is visible only to a household that has no favourite foods***; **3 of 5 story types have no reachable render path.** **THE TRADITION DRIFTS WITH THE QUERY DATE** — `familyTraditions` keys on `e.date.getDay()` (`engine.ts:418`) over `household-history.ts:44-46`'s back-projected `approxDate` (`planner_entries` has **no date, no `createdAt`**): the same *"Monday roast"* is a Monday tradition or a Thursday one depending on **when the report runs**. **Rule HT7's fabrication, live** — and **the builder's own docblock is falsified by its consumers** (`:18-20` justifies it as *"used only for recency ordering"*; `:418` reads it as a **weekday**, `:534` as a **season**). **`TIME3:401`'s duplicate builder confirmed:** `routes.ts:11364-11400` is a **verbatim copy**, and **6 story routes call the copy** while 1 caller uses the extraction. **SEASONAL — the canon's answer is exact and stated 4×** (`OLB:151`,`:155`,`:177`,`:188`): ***"It reads the family's season in what they cook, not in a calendar."*** The season appears in the **food**, never the room; seasonal dressing **declined categorically** (`OHDB §11:193`; `EXP5:788-790`), and **the mission's own deliverable is one word from that anti-pattern** (§11.2). **WS11 implements the law perfectly on top of a fabricated clock**; 3 season impls confirmed exactly as `TIME3:400` counted — **all behaviourally consistent** (maintenance risk, **not** TIME2's five-that-disagree), all `getMonth()` = server-process-local. **THE EXPECTED PLAN1-SHAPED FINDINGS DID NOT APPEAR — 3 PASSes:** the **arrival is clean** (2 layout divs from the room's edge to the first card, `:3500`→`:3941`; every candidate lobby element conditional) — **PLAN1 found five strips; this room has none**; the **card grid is canonically correct** (E2 shelf + object-cards is *this* room's posture — **the opposite of the Planner's one-table, and both currently obey**); the **strip does not fabricate a zero** (quadruple-gated `:78`/`:89`/`:104`; `?? 0` consumed only by `> 0`) — **the same coercion EXPCOMP2/PLAN1 found broken on Home and the Planner, written correctly here.** ***The Planner needs clearing. The Cookbook needs connecting.*** **THE ONE LIVE BREACH: "Cooked N times"** (`CookbookMealIntelligenceStrip.tsx:93`,`:183-186`; spread to `routes.ts:5691-5696`) — ORCH1's *"only place in the codebase that breaks the trust model"* (`stories/types.ts:20-24`), and the codebase **documents its own conflation in scare quotes** (`food-intelligence-assembler.ts:266`). **NEW FIND: the honest sentence already exists one page away for the same field** — `food-detail-page.tsx:85-96`, *"has featured in your plans N times"*, under the comment *"household history (careful language)"*. **THA already owns the true sentence and declines to use it here.** **Also: the Companion is not in this room at all** (0 `AmbientIntelligence`; `surfaceKey="cookbook"` = **zero product-wide**) despite `intelligence/index.ts:4-5` naming Cookbook **first** — **but that is the COMPLIANT state**: it is the only room with **no ungoverned channel to evict**, so it can adopt the Companion Card correctly first time. **And `buildHouseholdHistory(userId)` returns a type called `HouseholdHistory` under the title *"Your household loves"* — THA already tells one person their own history is their household's.** **CORRECTION recorded not amended (§13.2):** `BLUEPRINT §12.2:312`'s *"Cook/plan counts"* — **one of those two does not exist**; the slash reads as an implementation detail and is the difference between the room's premise and a fabrication. **Delight bounded by `EXP ARCH §17.9`, applied not invented:** the room's delight is **recognition** (`OLB:74`), register = **a half-step of warmth, nothing else**; *"Tomatoes became a family favourite"* **qualifies but is not yet honest** (plan counts ≠ something that happened) — available the moment ORCH1's wire lands, **not one day before**; **the favourite star is REFUSED and its absence recorded as COMPLIANCE** (a declared favourite is a rank — `BLUEPRINT:312`, `types.ts:13`), so a future workstream does not "fix" it; **the seasonal wrap-up is refused by the engine's own architecture** (`seasonal/engine.ts:12` — *"no season report table… so a seasonal scorecard cannot quietly accrete"*). **NORTH2 gate on all 9 deliverables → 6 owned with no remainder** ⇒ composition + citation, **no law created**. **10 of my own OLB citations found drifted and corrected before delivery.** **No screens, no code, no schema, no architecture modified** — 7 Experience docs + both Capability Cards + Recipe Acquisition byte-untouched; README untouched; **the one-character `.slice` fix deliberately NOT applied** (a live UI change belongs to a workstream). Gate: `docs/investigations/ has no loose files` **PASS**. **Next: (1) stop saying "Cooked" — a copy-paste from `food-detail-page.tsx`, blocked by nothing; (2) delete `.slice(0,2)` — verify a considered limit isn't wanted, but "the family with the most history sees the least of it" is not defensible; (3) COOK2? — give the Cookbook its family's stories (a re-pointing, not a build; must apply §12.2's warmth ceiling, since story CARDS may exceed a room whose Living Detail is WARMTH, and route via `COMPANION_CARD:97`); (4) name & route the family's book — sharper than PLAN1 §6's seam (the Planner at least HAS a `householdId`); (5) TIME4 reaches this room — stories engines absent from TIME2's matrix; cheap win: delete the `routes.ts` copy; (6) offer the Companion a chair — LAST, so it inherits NTC-P2's converged pattern rather than a 4th variant.** **Explicitly NOT recommended: a Cookbook visual prototype** (`EXP5:951`; P1/Home has not run) **or a favourite button.** | [`runs/COOK1_Orchard_Cookbook_Blueprint.md`](./runs/COOK1_Orchard_Cookbook_Blueprint.md) |
| `PANTRY1_Orchard_Pantry_Blueprint` | Waiting for User | `rollback/PANTRY1-orchard-pantry-blueprint-20260716` → `7d1dd2ce` | Delivered — the Orchard Pantry Blueprint (`docs/investigations/ux/PANTRY1_ORCHARD_PANTRY_BLUEPRINT.md`), the Pantry defined as **the household larder**, in principles, no screens. **HEADLINE: the one room in the house whose entire purpose is to tell the household the truth about their own home is the only room that invents its contents and presents them as fact.** **~141 items a household never put there:** `seedDefaultFoodPantryItems` (`storage.ts:2224-2374`, list inline `:2232-2353`, ~122 foods incl. **six named apple cultivars**, passion fruit, gooseberries, redcurrants) + `seedDefaultHouseholdItems` (`:2187-2222`, list `:2195-2202`, 19 items incl. bleach). Against `ARCHITECTURE_PRINCIPLES.md:74` **Principle 6** — *"No fabricated knowledge — honest gaps over invented facts"* — whose rationale is three words long: ***"Trust is the product… This is non-negotiable"*** (`:83`). **A `GET` WRITES IT** (`routes.ts:7788-7799`) — merely opening the page materialises the whole inventory; no action, no consent, nothing asked. **It also fires from the shopping list** (`shopping-list-page.tsx:1588`), which then marks Milk/Eggs/Butter/Olive oil as ***"staples you already have"*** (`:1763-1766`) for a household that never opened the Pantry. **THA COMPUTES THE HONEST SIGNAL, SHIPS IT TO THE BROWSER, AND THROWS IT AWAY:** `isDefault` is on the row (`schema.ts:1018`), set `true` by both seeds (`:2214`,`:2365`), reaches the Intelligence layer (`pantry-read-handler.ts:97`) and is **declared on the client's item type at `pantry-page.tsx:58` — referenced once and never used again.** No badge, no caveat, no filter. ***Not a missing capability: the truth is already in the browser and the room declines to say it.*** **THE ROOM'S ONE PERMITTED SIGN OF LIFE IS SOURCED FROM A FACT THAT DOES NOT EXIST** — `BLUEPRINT:314` *"**Freshness, honestly told** | Pantry | **Item freshness data**"*; **no `expiry`/`bestBefore`/`useBy`/`openedAt`/`purchasedAt` on `user_pantry_items`** (`schema.ts:1010-1025`). **The freezer has it** (`freezerMeals.expiryDate` `:813`, `remainingPortions` `:811`); **the pantry does not.** THA knows exactly **one bit** per food: does a row exist. **Strictly worse than `COOK1 §13.2`'s *"Cook/plan counts"*** — there is no slash and no fallback; the Living Detail is `BLUEPRINT §12.1 r2`'s **dead** case (*"Data-borne or dead"*), survivable only by **r3** (*"honest in absence… the room is still complete"*). **THE CANON NAMES AN ACTION THE PLATFORM LACKS** — `EXPLANG:427` *"one easy motion at a time (**mark used**, add an item)"*: **no `consumed`/`usedAt`/`depleted`/`waste` anywhere**; `savingsEvents.'pantry_used'` **declared `schema.ts:1351` with no producer — always zero** (`storage.ts:3722`). ***The room can be filled and never emptied.*** **AND `needQuantityValue` IS SHOPPING INTENT, NOT STOCK** — *"In Pantry"* is **literally defined as `needQuantityValue === null`** (`pantry-page.tsx:470`), so **flagging that you need milk removes milk from "In Pantry" in the UI while every consumer still counts it present.** **THE EXPECTED FINDING INVERTED — THE PANTRY *PASSES* WHAT THE COOKBOOK FAILED (§12.1):** `user_pantry_items.householdId` **exists** (`schema.ts:1013`) and **all** reads/writes scope to it (`storage.ts:2134/2145/2164/2174`); no `getPantryItemsForHousehold` exists **because `getPantryItems` IS it** (signature reads user-scoped, behaviour is household-scoped). **Deliberate, with the reasoning left in the migration comment** (`runner.ts:820-822` — *"It only prevented a single user from adding the same key twice — **not two household members**"*), partial unique index at `:834-836`. ***"You've got most of tomorrow's already" is honestly plural here — the only audited room where that is true*** — and it is the **existence proof that `COOK1 §15.4`'s hardest recommendation is achievable.** **KNOWLEDGE GROWTH — `pantry_ingredient_knowledge` SKIPS STAGE 3 ENTIRELY:** a **cache miss on one household's read** fires OpenAI (`routes.ts:7893`) and **writes the output straight into the shared cross-household table** (`:7895`). Candidate ✅ · Gate ✅ (real wording firewall banning `prevents`/`cures`/`clinical`/`superfood`, `openai-enrichment.ts:43-58`) · **Confirm ❌ does not exist** · Publish → immediately, to the next household that asks · **Rejection ❌ silent** (discarded; the next cache miss re-asks). **Violates PKCA Rule KC1** (*"has not built a knowledge pipeline — **it has built a fabrication risk**"*) **· KC2 · KC3** (client never receives provenance — grep for `enrichmentSource`/`isLocked` returns nothing but one fetch at `pantry-page.tsx:432`; an AI sentence and a hand-written one are identical on the shelf) **· KC9** (*"Automation authors candidates, never publishes them"*). **`isLocked` IS A LOCK WITH NO KEY** — defaults false (`schema.ts:1390`), upsert honours it (`storage.ts:3528-3536`), **nothing anywhere sets it true.** **THE PRECEDENT IS IN-REPO, TWICE, UNAPPLIED:** `ingredient_classifications` (**20 lines away**, `schema.ts:1400`) has candidate/gate/confirm/reject **+ an admin surface** (`classification-store.ts:62`; `routes.ts:10878-10947`), and **`canonical-foods-gate.ts:5-26` fixed *this exact defect* elsewhere** (*"346 of 610 live foods arrived that way, unstamped… wore `source: 'THA editorial'` despite being `authored_by: ChatGPT` drafts"*) **and wrote Rule KC9 into its own header while doing so.** **`PKCA:139` REFUSES THE MISSION'S WORD** — household knowledge does **not** grow *"naturally"*: *"a pattern below Confirmed Understanding… **is not yet a fact at all**"*; the platform says it in its own voice (`routes.ts:11763-11764` — *"**THA does not tell a household what it has learned about them until they have agreed it is true**"*). EL1/EL2's gates **verified real** (`framework.ts:53/56`, enforced `:150/:156`) — confirming `PKCA §4.1:173`'s claim. **WS10 Stories has NO ROW IN ANY PKCA TABLE (§6.4)** — household knowledge, threshold-promoted (`engine.ts:53-87`), rendered **unconfirmed**; the collision sits **20 lines apart in one handler** (`routes.ts:11743-11748` vs `:11763-11764`). Counter-argument (**a recollection is not an inference**) stated, **not resolved** — `PKCA:139` explicitly hands the question forward and **WS10 predates PKCA and was never asked**. **`PKCA §6.1:265` IS STALE (§12.3)** — *"currently zero of either"* is false; reporter (`opportunity-delivery/framework.ts:364`) **and** consumer (`:369`) both wired. Recorded, not fixed — its own **Rule KC15** names this as the defect. **FOUR PASSes, and they matter:** **no scoreboard** in the room where a scold is most natural (the plant counter exists — `PlannerIntelligenceStrip.tsx:175`, `PlantDiversityReport.tsx:887-901` — and is **imported by none of the three Pantry files**); the single `?? 0` is **provably unreachable** (server returns `null`, not `{count:0}` — `routes.ts:5672-5679`) — **the same coercion EXPCOMP2/PLAN1 found broken on Home and the Planner, written correctly here for the third time**; **stillest room audited** — **zero framer-motion** in all three files, and the one child that has it uses `initial={false}` (**COOK1 had to flag the Cookbook's staggered cards; this room has nothing to flag**); **the Companion is in the right chair** — `surfaceKey="pantry"` titled ***"Ways to use what you have"***, which is `OLB:87`'s own sentence (*"you've got most of tomorrow's already"*) in production — **the only audited room where the friend is seated correctly** (PLAN1 found 2 ungoverned channels; COOK1 found no Companion at all). **ARRIVAL near-clean — ONE unconditional element** (Planner=5, Cookbook=0, **Pantry=1**): a micro-insight (`pantry-page.tsx:1154-1156`) from a 9-item array (`:18-28`) rotating on **day-of-month** (`:1095`) — generic gut-bacteria copy, **undismissable, owned by no intelligence system**, sitting *above* the Companion's own row; `NOTICE_ENGINE §6`'s *"never invents a 'tip of the day'"* almost literally describes it. **THE KNOWLEDGE HUB BREAKS ITS OWN STATED RULE** — header `:29` *"Empty sections are silent (never 'nothing yet')"* vs `"Nothing to show here yet."` at `:604`,`:620`,`:628` (Home view correct; breach scoped to Topic pages). **THE LIST↔PANTRY WIRE RUNS THE WRONG WAY (§11.6):** **typing an item onto the shopping list instantly POSTs it to `/api/pantry`** (`shopping-list-page.tsx:1787`) — asserting possession **before purchase**, forcing everything to `larder` (`:1785`), errors swallowed (`:1791`) — while **buying changes nothing** (no purchase/checkout handler touches the pantry; pantry→basket is telemetry only, `routes.ts:3751-3761`; `sendToBasket` sends `sendQty`, so ***"Need 2 litres" reaches the basket as 1***). ***THA learns you have something when you type it on a list, and learns nothing when you actually buy it.*** **FOOD IS STORED, NOT UNDERSTOOD (§5.4):** write-time is `normalizeIngredientKey` only (`storage.ts:2140`) — **no canonical resolution, no validation, and no FK from `user_pantry_items` to any canonical table** (a typo becomes a permanent first-class row); read-time `resolveCanonicalFood` (`routes.ts:5649`,`:5762`) is **recomputed per request and never persisted**; the *panel* fails honestly (`{resolved:false}`, `:5651`/`:5666` — **recorded as a PASS**, the domain's best-designed edge) but the *item* fails **silently** (`:5763`). ***The pantry does not store what the food is. It stores what someone typed.*** **THE CANON'S ANTI-PATTERN WAS AIMED AT ILLUSTRATORS AND HIT A SEED FUNCTION (§10.1)** — three governing docs forbid *"fake fullness"*/*"invented fullness pretending the cupboard is more than it is"* (`OLB:89`; `OHDB:256`; `TRANSLATION1:271` — *"**no shelf pretending to hold more than the data says**"*), **all three read as visual rules about drawing jars**; `storage.ts:2232` achieves it without drawing anything. **CONFIRMED FROM THIS SIDE (§12.4):** `COOK1 §12.5`'s stories-in-the-Pantry finding — `/api/pantry/stories` (`routes.ts:11453`) + `.slice(0,2)` (`PantryKnowledgeHub.tsx:828`) are **the Pantry's code**; seconded COOK1's fix **and added the caution that the stories should LEAVE, not multiply** (E2 density + `BLUEPRINT §12.1 r4`). **NORTH2 gate on all 8 deliverables → 6 owned with no remainder** ⇒ composition + citation, **no law created**. **ONE NOUN NARROWING (§0.2):** mission says *"larder"*; canon says *"the household **pantry** — what is in the house right now, **honestly told**"* (`BLUEPRINT:148`). *Larder* imports a **store-of-provisions** model; **the room's noun is not the food, it is the honesty** (`OLB:85` — *"its whole grace is **honesty without guilt**"*). Canon's noun kept. **No screens, no code, no schema, no architecture modified** — 7 Experience docs + PKCA + ARCHITECTURE_PRINCIPLES + SoT Register byte-untouched; README untouched; **the seed deliberately NOT touched** (a live data change affecting every existing household belongs to a workstream). Gate: `docs/investigations/ has no loose files` **PASS**. **Next: (1) STOP INVENTING THE HOUSEHOLD'S PANTRY** — the most serious finding in any of the three room audits (**PLAN1 found a fabricated *zero*, COOK1 a fabricated *verb*, this is a fabricated *inventory***, in the room whose only virtue is honesty); needs a decision an investigation may not make — *does the seed become a confirmed candidate list (`PKCA §1`'s shape) or go away?*; **`isDefault` is already in the browser, so telling the truth today is an afternoon — deciding the shape is the workstream**; also in scope: `syncAllPantryDefaults` **resurrecting deliberate deletions** (`storage.ts:2376`; partial index at `runner.ts:835-836` means `onConflictDoNothing` never fires for soft-deleted defaults) and the **`defaultHave` no-op** (`planner-explanation-context.ts:230` excludes nothing because neither seed sets it false). **(2) stop asserting possession when someone types a list** — or #1 gets undone by the list. **(3) give `pantry_ingredient_knowledge` a confirm stage** — an unapplied in-repo precedent ×2, not a design problem; **and give `isLocked` a key or delete it.** **(4) ask governance two questions** — is WS10 inside PKCA? PKCA §6.1:265 is stale. **(5) audit the other 8 Living Details against their named data sources** — **2 of 10 now confirmed sourced from absent facts** (COOK1's *"Cook/plan counts"* + this room's *"Item freshness data"*), **found by two investigations asking the same question of two rooms, neither looking for it**; worth doing **before** any room's Living Detail is built. **(6) retire the micro-insight or give it an owner.** **Explicitly NOT recommended: a Pantry visual prototype** (`EXP5:951`; P1/Home has not run); **a freshness/expiry feature YET** — it is the room's Living Detail and **the single most likely thing to be built next and built wrong** (`OLB:87` forbids the red badge in one sentence), **and it cannot be honest until #1 lands — dates on invented items make the fabrication worse by making it specific**; **a stock-level/check-in model** (`EXPLANG:427` forbids **bookkeeping** by name — the obvious fix for §5.2 is the anti-pattern). | [`runs/PANTRY1_Orchard_Pantry_Blueprint.md`](./runs/PANTRY1_Orchard_Pantry_Blueprint.md) |
| `CONV1_Architecture_Convergence_Programme` | Waiting for User | `rollback/CONV1-architecture-convergence-programme-20260716` → `7d1dd2ce` | Delivered — the **Architecture Convergence Programme** (`docs/investigations/governance/CONV1_ARCHITECTURE_CONVERGENCE_PROGRAMME.md`): every divergence between the governing architecture and the implementation, classified, prioritised and sequenced. **24 candidates across 7 classes; ZERO new owners** — every item returns a fact to a store the architecture already declared. **HEADLINE: THA's convergence problem is not that it doesn't know — everything it knows is written in prose, and every gate it owns reads code.** `npm run verify:publication` was **executed for this document**: 22 domains — 5 🟢 · 11 🟡 · **6 🔴**; 60 checks — 24 pass, 24 warn, **12 fail**. **The gate has been red since it was built, and every check carries a `cpi1:` back-reference — CPI1's findings became executable and were left failing.** **THE STRUCTURAL FINDING:** the gate reports *"the Source of Truth Register agrees with the owners it governs — no known register drift"* **while the Register declares `users.dietPattern` the SoT in two places and `ARCHITECTURE_PRINCIPLES.md:38` calls those same columns a redundant shadow to retire** — *the gate that checks whether the Register is right cannot read the Register*. THA can mechanically detect projection drift (`verify:publication`), schema drift (`verify:schema-coverage`, **51%**, ran it) and adoption drift (`adoption:check`); it has **NOTHING** for document-vs-document or document-vs-code — **and that class produced every 🔴 in the backlog.** **CONV1'S OWN CONTRIBUTION (beyond PEOPLE1 § 9.1's two-document conflict):** it is **four positions across three documents**, the Register **contradicts itself** (Domain 7 *"Contested"* vs Phase 4 *"Retain… the SoT"* vs Appendix A), **and the two governing docs are not arguing about the same rival** — Principles says `household_eaters`, the Register says `user_preferences`. **An engineer told to "resolve Domain 7" would promote the wrong store, mark it converged, and leave Principle 2's actual violation intact.** **TIER 0 (ahead of normal convergence):** `SEC-1` departed-member live-data feed (**verified end-to-end at source** — `leaveHousehold`/`removeHouseholdMember` never touch `householdEaters`; `getHouseholdEaters` filters on `householdId` only; `routes.ts:9041` reads the departed account live) · `SEC-4` `/api/uplift/accept` accepts **any** rule id unvalidated, 8 live phantom rows · `SEC-2`/`SEC-3` auth token timestamps. **THREE CPI1 FINDINGS RE-VERIFIED AS FIXED and excluded** (dropped allergens — SURF1B closed it properly; 30-plants over-count; the bug-preserving CI fixture). **TIME2 § 9.4 #11's citation corrected** — the path is `server/migrations/runner.ts:74`, and the claim **survives**; its 🔒 grade is **narrowed to *latent*** (masked by UTC containers). **Recommended first workstream: `CONV2` = `SEC-1`** — the only live data-protection defect; blocked by nothing, no schema, no legal question, one call site; **it removes an exposure rather than adding a claim** (agrees with PEOPLE1 § 10.2, does not re-decide it). **Planning only — no code, no schema, no architecture modified; 2 files created.** Gate: `docs/investigations/ has no loose files` **PASS** (mission's specified root path would have **failed** it — filed under `governance/`, reported in § 0.1). | [`runs/CONV1_Architecture_Convergence_Programme.md`](./runs/CONV1_Architecture_Convergence_Programme.md) |
| `SEC1_Departed_Member_Eater_Exposure` | Complete | `rollback/SEC1-departed-member-eater-exposure-20260716` → `7d1dd2ce` | **None — complete.** Implemented **CONV1 item `SEC-1`** (its § 5 rank 1; source finding PEOPLE1 § 8.1): **a person who left a household was still having their allergens read out of their LIVE account and served to the household they left — on every page load, updating as they changed them.** `getHouseholdEaters` filtered on `householdId` and nothing else; `leaveHousehold`/`removeHouseholdMember` never touch `household_eaters`; `routes.ts:9041` then re-read the departed member's current profile. **THE FIX IS ONE `WHERE` CLAUSE** — return children (`userId IS NULL`) plus adults who are **still active members**. **Not a new rule: it is the rule the WRITE side already applied 20 lines above** (`syncMembersAsEaters:2897` only ever creates a row for an *active* member). *The read and the write disagreed about who was at the table, and the read was the looser of the two.* **FILTER, NOT SEVER, NOT DELETE — both refusals load-bearing:** nulling `userId` springs **PEOPLE1 § 8.6's trap** (row flips to `kind:"child"` → promoted to canonical owner of allergens it stores as `[]` **by design** → a real person's restrictions silently become the empty set while they stay plannable — a privacy defect traded for a *safety* defect); deleting breaks `planner_entry_eaters` and takes a retention decision this layer may not take. **VERIFIED, NOT ASSUMED:** new DB-backed suite **17/0** (deliberately not mocked — the defect lived in a `WHERE` clause, and `test-intelligence-household-binding.ts:156` **mocks** `getHouseholdEaters`, which is why its 51 assertions passed against the defect for its whole life); **★ proved to DETECT the bug** — reverted `storage.ts` to `7d1dd2ce` → **13 passed, 4 failed**, exactly the four leak assertions, then restored → 17/0; **E2E on port 5098** (concurrent 5000 untouched): Alice left, then declared **new** Dairy/Eggs allergens in her own home — **never reached Bob**, while her row sat in the table the whole time with `userId` intact (**proof it is a read filter, not a delete**). **14 consumers enumerated** — the API, the Companion's `eaters` scope, meal tailoring, `HouseholdSafeFor`, Smart Suggest, discovery, Food Intelligence all closed at one owner; **seeders verified unaffected**; a rejoining member keeps their **original row, id and planner history** (no duplicate). **Regressions 1,070 passed / 0 failed** across 17 suites; typecheck **304 → 304** (identical, zero in touched files); `verify:publication` **unchanged (6 🔴)** — correct, SEC-1 is not a publication item. **Wired to a gate** (`test:sec1-departed-member-eater-exposure`, added to the aggregate `test` suite) per CONV1 **CP10**. **REPORTED, NOT ABSORBED:** § 3.2 residue — `getPlannerEntryEaters` joins the table directly, so a departed member's **frozen** name can still appear on planner entries they were explicitly assigned to; **not a live account read** (no `getUser` on that path) — planner history, and PEOPLE1 § 8.3's question. **THE CAUSE IS UNTOUCHED** — the ownership inversion is exactly as live as before; **STEP 8 convergence recorded as *Unknown — requires audit*** because this change converged **zero** duplicate owners and claiming a gain would be false. *A bandage on a correct wound.* **4 files; no schema, no migration, no client, no architecture.** Report: `docs/implementation/platform/SEC1_DEPARTED_MEMBER_EATER_EXPOSURE.md`. **Next CONV1 item: `SEC-4`** (`/api/uplift/accept` persists any caller-supplied `ruleId` unvalidated; 8 live phantom rows) — validate server-side **first**, then delete the client constant. **`OWN-1` still must not start until `DOC-1` lands.** | [`runs/SEC1_Departed_Member_Eater_Exposure.md`](./runs/SEC1_Departed_Member_Eater_Exposure.md) |
| `SHOP2_Orchard_Shopping_Blueprint` | Waiting for User | `rollback/SHOP1-orchard-shopping-blueprint-20260716` → `7d1dd2ce` | Delivered — the Orchard Shopping Blueprint (`docs/investigations/ux/SHOP2_ORCHARD_SHOPPING_BLUEPRINT.md`), Shopping defined as **the list by the door**, in principles, no screens. **⚠️ ID: `SHOP1` IS TAKEN** — `rollback/SHOP1-intelligent-shopping-evolution-20260712` → `docs/implementation/shopping/SHOP1_INTELLIGENT_SHOPPING_EVOLUTION.md` (2026-07-12, live prior art on deliverable #3 that **names this workstream by ID**, `SHOP1:320-327`) ⇒ this is **SHOP2**; the rollback tag was cut before the collision was found and keeps its slug (unambiguous by slug+date; § 0.1). **HEADLINE: the room the canon calls *"the list by the door"* does not show the household their list.** `shopping-workspace-page.tsx:1212-1217` defaults `mode="add"`; the list is gated `mode !== "add"` (`:2551`); **the nav carries no `?stage=`** (`nav-bar.tsx:43/458/512` — WX10C removed it) ⇒ **a household with 14 items taps Shopping and gets a blank auto-focused textarea** (`:2340-2352`, `:1318-1320`), their list **nowhere on screen**. Against `OLB:96` (*"The list is gathering by the door"*) + `EXPLANG:408` (*"a calm, **ready list**, not an anxious tally"*). **THE CANON NAMES THIS ROOM'S PRIMARY ACTION AND THE ROOM DOES SOMETHING ELSE** — `EXPARCH:140`: *"On a meal: cook or plan it. **On a shopping list: work through it.**"* — **the only realm whose primary action the canon states in its own words**, and the room opens on *add*; `EXPARCH:141`: *"A surface that cannot name its primary action is not finished being designed"* — **it was named for it, three documents up**. **THIRD INSTANCE OF THE LAST-INCH SHAPE:** `:2176-2178` computes `"14 items in list"` **on the `mode==="add"` branch** while its only container is gated `mode !== "add"` (`:2202`) — **computed and unreachable**; joins PANTRY1's `isDefault` and Shopping's own **`confidenceLevel`/`confidenceReason`** (`schema.ts:201-202`, plumbed to `shopping-read-handler.ts:64/128`, **ZERO hits in all of `client/src`**). ***THA does not have a confidence problem — it has a last-inch problem.*** **THE ROOM HAS TWO DOORS AND THE WRONG ONE IS UNGUARDED:** `WX10C` declared `/shopping-workspace` canonical, ticked *"No duplicate shopping workflows ✅"*, and **never mentions `shopping-list-page.tsx`** — **4,242 lines**, live at `/basket`+`/analyse-basket`, **linked 3× from the Dashboard** (`dashboard.tsx:394/449/671`), **advertised by the canonical page's own footer** (`:2770`), nav-aliased (`nav-bar.tsx:663`). **Its compliance table is true of the nav and false of the app.** Plus a **dead link**: `:2155` `<Link href="/shopping">` labelled "Basket" → no such route → **404**. **"INDUSTRIAL" — THE FABRICATED VERDICT** (`shopping-list-page.tsx:1150`): `canShowScoreForItem` returns true for **any whole food** (`basket-item-classifier.ts:49-57`) and `(item.thaRating ?? 0)` turns **null → 0** → final branch ⇒ **THA labels an unscored banana "Industrial", in red**; also 0-apple renders `:888/929/1141`. **The same file gets it right 8 lines up** (`:884`); the **canonical workspace pairs the same classifier with `!= null`** (`:909/912/1008`) and is correct; **the server NEVER fabricates** (`routes.ts:4115` — a `0` is `skipped`, never written). **The fabrication is added by ONE client, at the last inch** — and **`SHOP1:147-150` explicitly refused it** (*"treating an unrated line as a 0 to beat"*): **the rule is written, held on the server, and broken in the browser**. **FOUR-ROOM PATTERN COMPLETED:** each room fabricates in its own currency — Planner a **zero** (*"🌱 0/30"*) · Cookbook a **verb** (*"Cooked N times"*) · Pantry an **inventory** (~141 items) · Shopping a **verdict** (*"Industrial"*); **every one a `?? 0` or its cousin, added by a client over a server that returned an honest null**. **THE CANON DOES NOT KNOW THA SELLS ANYTHING:** searched all 7 Experience docs (~2,600 ln) for `price|money|commerce|retailer|supermarket|checkout|affiliate|revenue|monetis*` → **ZERO HITS** (only rollback footers); `cost` = attention/effort only. **Commerce appears exactly 4× and every one is a prohibition** (`EXPLANG:413/379/493`; `EXPARCH:176`) + `EXPARCH:140` (*"never 'upgrade'… whatever the business currently wishes people did more"*) — **while the room runs `POST /api/basket/checkout` (`routes.ts:829`), 9 UK retailers, real prices, per-tier totals.** **Stated precisely, NOT overclaimed:** commerce **is** governed — `capabilities/partners.md` is governing architecture and is unsparing about itself (*"**MAJOR FINDING** — none of the three apiSurface routes the registry lists are what 'Partners / Supermarkets' implies"*; `recommend`/`compare` have **no safe grounded owner**). **A Capability Card governs what the *Companion* may say about retailers; no document governs what it should *feel like* to spend money in a house whose emotional constitution was written about having nothing sold to you.** **Not a defect in any document — a question nobody has been asked.** Routed § 14.4, **not answered**. **CONFIDENCE IS CREATED BY REFUSING TO GUESS — 3 independent owners, all real running gates:** `item-resolver.ts:13` (*"**Never silently force a bad category — prefer `needs_review` over a wrong guess**"*) · `apple-score-trust.ts:31-33` · `routes.ts:4341-4351` **HARD FAIL-SAFE** (*"never look up prices for unrecognised items… keyword-matching it would silently grant it a real category and **trigger fake prices**"*, w/ the "Boorboans"→"Bourbon Biscuits" example). **Three files, three registers, one doctrine — Principle 6 arrived at independently.** Trust gate is **display-layer only** (`apple-score-trust-gate-complete.txt:8-9`; scores stay in the DB; 3 declared live risks `:148-166`; `thaRatingSource` **"REQUIRES APPROVAL"** `:155`) ⇒ **`PKCA:169` KC8 — it guards the window, not the well, and `:1150` is what walking around it looks like.** **FEEDS — Planner→Shopping is the best engineering in the domain:** **freezer stock deducted FIRST** (`routes.ts:4170-4176`); quantities **aggregate** (`ingredient-utils.ts:881/885` — 2 meals × 1 onion → **one row, qty 2**); **dedupe twice** (memory + DB on `householdId+normalizedName+unit`, `storage.ts:709-726` — re-running **increments**, never duplicates); **member swaps protected** (`:701-717` — *"a vegetarian swap never collapses into the shared ingredient line"*). ***The feed is "natural" because it aggregates like a person would.*** **Cookbook has no wire — it is a CALLER of the Planner's** (`meal-detail-page.tsx:335-352`), the correct shape — **but COOK1's defect reaches through it**: meals read **user-scoped** (`:4168`), list written **household-scoped** (`storage.ts:680`) ⇒ ***a member cannot generate a list from their partner's recipe***. **Shopping NEVER asks the Pantry** — zero pantry reads in `:4132-4314`; **SHOP1:419 states it as a deliberate exclusion**, and **SHOP1:129-131's refusal is PRINCIPLED and must hold** (*"deriving it here would **give one fact two owners**"* = Principle 2 held under pressure by an implementation that wanted the feature). **THE TWO ROOMS' DEFECTS COMPOSE — neither investigation could see it alone:** PANTRY1 found **~141 fabricated pantry items**, so a generation path that deducted today's pantry would **silently remove real groceries from a real list on the authority of a hardcoded array** ⇒ ***Shopping must not read the Pantry until the Pantry is true*** (**explicitly NOT recommended**). **Day-level provenance is DEAD:** schema holds `weekNumber`/`dayOfWeek`/`mealSlot` (`schema.ts:590-592`), the read returns them, **the UI already renders them** (`shopping-list-page.tsx:102-104`) — and **both `addIngredientSource` call sites omit all three** (`routes.ts:4236`, `:4283`) ⇒ *"Needed for Tuesday dinner"* is **unreachable**; cheapest real win here. **PLAN1 § 6's GAP IS CLOSED IN THIS ROOM:** `shopping_list` is **household-scoped** (`storage.ts:678-681/2807/2842`), records **`addedByUserId` on every insert** (`:691/744`), resolves it (`:2814-2819`), **renders *"Added by Dad"*** (`shopping-list-page.tsx:98`), **and refuses to reorder under a second pair of hands** (`:2805-2806`). ***The only room of four that is household-scoped, author-aware AND concurrency-considerate*** — **the existence proof PLAN1 § 14.4 and COOK1 § 15.4 both needed: done, small, 40 lines away.** **COMPANION IN THE RIGHT CHAIR:** `surfaceKey="shopping"`, **`title="Worth a look before you shop"`** = `OLB:100`'s threshold in production — **the only title in the house with a deadline built into its grammar**. **THE BEST ARCHITECTURE THIS 4-ROOM AUDIT TOUCHED:** `shopping-restriction-conflict` is the platform's **sole `critical` emitter**, **structurally enforced** — `assertCriticalAllowed` **THROWS** (`shared/attention/index.ts:115-121`); *"`critical` is valuable precisely and only in proportion to how rarely it is used"* (`:80-89`); fires on unchecked items only, cites 2 evidence rows, **never modifies the list**; test-locked `CRITICAL_TYPES.size === 1`. **…AND IT IS ABSENT FROM THE OTHER DOOR** — `shopping-list-page.tsx` mounts **no `AmbientIntelligence`** ⇒ **no SHOP1 cards** (`SHOP1:428-434`) **and no allergen conflict card** on the surface **the Dashboard links to 3×**. **DELIGHT: Shopping owns 1 of only 2 qualifying moments the canon names** (`EXPARCH:337` — *"a first plan made, **a first list completed**"*; the Planner has the other; Cookbook/Pantry/Nutrition/Diary/Analyser have **none**) — **and the canon then forbids celebrating it** (`OLB:102`; `OHDB:268`). **Not a contradiction** — `EXPARCH:341`: *"**acknowledgement, not celebration theatre**"*. **THE CLEANEST PASS OF THE AUDIT:** the Living Detail *"Struck and settled into shade; no celebration per item"* (`BLUEPRINT:313`) met **to the word** — `:835` `line-through text-muted-foreground`; the mutation `:1483-1511` has **no `onSuccess` handler at all**; **`grep confetti` across `client/src` = ZERO, no confetti library in `package.json`** — *the room could not celebrate an item if it wanted to*. **One line to watch, reported precisely not inflated:** `:2686-2696` *"**Great shop** — nothing left to find"* — **static** (doesn't touch `OHDB:268`'s *"confetti or reward animation"*; `CelebrationCard` imported **only** by the Planner); first line passes outright, **second line is a compliment** — *"a household finishing a shop would recognise the milestone and would not describe it as great"* — **a copy decision for a workstream, not a defect**. **ALL VIEW, NO ROOM — LIVE, IN THE DEFAULT:** `:2325-2333` gives the Add-mode surface **its own `backgroundImage: url('/orchard-bg.webp')`** + `:2336` `rgba(255,255,255,0.80)` **not theme-aware** (a bright white panel in dark mode) ⇒ **`BLUEPRINT:442`'s anti-pattern is *"a search box floating on a landscape"* and the room's default state IS that sentence, rendered** — and Shopping is **E1** = *"the orchard as illumination and warmth, **not image**"* (`:223`) while **rendering the image twice**; `OHDB:193` has the phrase — ***"a second orchard by the back door"*** — **and Shopping has built one**. **3 MATERIAL DIVERGENCES, ALL THE ROOM'S OWN CODE:** `<Card>` **imported (`:42`) and never rendered**; the list is a raw div at **`bg-card/60` with NO `backdrop-blur-md`** (`:2569` — 22pts more transparent than the system's `/82`, **orchard reads through the list sharp**); full-opacity `bg-card` (`:2852/2873/2892`). **PLAN1/COOK1/PANTRY1 each found ONE shared defect (`card.tsx:12`) in 4 rooms; Shopping added 3 of its own** = `BLUEPRINT:440` *the costume*. **ARRIVAL COUNT: Shopping = 0 always-on strips** (Planner 5 · Cookbook 0 · Pantry 1) — ***and it scores 0 because the content is not there to bury; the cleanest arrival in the house is clean because it is empty***. **Shopping has NO `data-realm`** — the only realm without one (its only one is on the **portalled** header, a **sibling** of `<main>`); reported as a **measurement** finding — *a realm the DOM cannot name is one no automated Blueprint check can verify*. **`Est. total` — real numbers, dishonest framing:** the server is **honest** (`supermarket-basket-service.ts:212` returns `undefined`, not `0`), the render is **a partial sum presented as a total** (unmatched items contribute nothing — 10 items, 3 priced → *"Est. total: 12.40"*), **no currency symbol**, and the sibling *"3 of 10 matched"* message **never connects to the total**. **NOUN REFUSED, BOTH HALVES (§ 0.3):** *"market"* names a place the house does not contain (`BLUEPRINT:149` — *"**preparing to leave the house**"*; the shop is **off-stage** — *there is no chapter at the market because THA never goes there*); *"basket"* names the **live 4,242-line non-canonical door** where every fabrication lives and no safety card reaches; and *market basket* is thirdly an economics **measurement instrument** (`EXPLANG:294` forbids measuring). ***The mission's noun points at the one door in this room the canon does not govern and the platform does not guard.*** **FILING (§ 0.2):** `ux/` as PLAN1/COOK1/PANTRY1 — **and rejecting the competing `shopping/` folder surfaced a defect**: `implementation/shopping/` exists but `investigations/README.md:18-30`'s workstream table has **no `shopping` row** while `:12-13` claims the vocabulary *"is shared with docs/implementation/"* — **it is not** (§ 12.3, recorded not fixed). **NORTH2 gate on all 8 deliverables → 6 owned with no remainder** ⇒ composition + citation, **no law created**. **SHOP1 cited, never restated; its named follow-ons not re-derived.** **No screens, no code, no schema, no architecture modified** — 7 Experience docs + ARCHITECTURE_PRINCIPLES + PKCA + `capabilities/partners.md` + SoT Register + `investigations/README.md` **byte-untouched** (verified via `git status`); README untouched; **the `?? 0` and the default `mode` deliberately NOT changed**. Gates: `docs/investigations/ has no loose files` **PASS**; `no duplicate documents` **PASS**. **Next: (1) SHOW THE HOUSEHOLD THEIR LIST** (`:1214`) — the room's missing Arrival beat and the only item that is not a defect fix; **the one-word diff is probably wrong alone** (WX10C made `add` default deliberately when folding in Quick List; reversing it blind re-opens the duplication it closed) — needs a workstream answering *what does a household with a list see on arrival, and one without?* against `EXPLANG:408` + `BLUEPRINT:229`'s E2-when-clear rule; **free win inside it: `:2176-2178` already computes the string**. **(2) STOP CALLING UNSCORED FOOD "INDUSTRIAL"** — **the fix is already written 3× in this codebase**; one clause on a condition; blocked by nothing; **do it first among the fixes**. **(3) put the safety card on the other door — or close the door**, + fix the `/shopping` 404. **(4) ask governance what money feels like in this house** — a governance decision, not an investigation. **(5) write the day into the provenance** — 2 call sites, no schema, no design. **(6) retire the second orchard + the 3 bespoke card materials** — **but Shopping has no warmth budget; what must survive is *competence*, not warmth**. **(7) SHOP1's own follow-ons remain open** — client-reasoning retirement (`SHOP1:320-327`), ranking **drift** (`:329-334`), **WS9 content gap** (`:425`, 6 of 10 anchors, its *"single highest-leverage follow-up"*); **⚠️ SHOP1 reserved the name `SHOP2` for the client-reasoning retirement and this document has taken it — name the next workstream deliberately, not by sequence**. **Explicitly NOT recommended: wiring the Pantry into list generation**; **any Shopping visual prototype** (`EXP5:951`; P1/Home has not run); **changing *"Great shop"***. | [`runs/SHOP2_Orchard_Shopping_Blueprint.md`](./runs/SHOP2_Orchard_Shopping_Blueprint.md) |
| `SEC4_Uplift_Rule_Provenance` | Complete | `rollback/SEC4-uplift-rule-provenance-20260716` → `7d1dd2ce` | **None — complete.** Implemented **CONV1 item `SEC-4`** (its § 5 rank 2; source finding CPI1 S1-3): **`POST /api/uplift/accept` believed everything it was told** — ruleId, ruleName, ingredient, quantity and explanation all came straight from the request body and went straight into `meal_uplift_applications` stamped `added_by: 'tha_uplift'`, **so a nutrition suggestion invented in a browser was indistinguishable from guidance a human reviewer signed off**, and the **`reviewedAt` approval gate that `uplift-engine.ts` applies to every match had NO counterpart on the write path**. **THE FIX — `resolveAcceptedSuggestions` (new, pure, in `uplift-persistence.ts`):** the rule must **exist**, be **reviewed**, and actually **author** that ingredient+action — **three gates, each closing a different hole** (the third matters: the first two alone would still let a real reviewed rule smuggle double cream). Returns the **owner's** copy; the caller's is discarded. Resolved **before** the system-meal fork, so a rejected request mutates nothing. **`added_by: 'tha_uplift'` is unchanged and is now TRUE.** **WHY `uplift-persistence.ts` AND NOT THE ROUTE:** **no test in the aggregate suite drives HTTP**, so a validator left inline could only ever be checked by hand — **the architecture and the testability had the same answer** (CONV1 **CP10**). **CLIENT:** `buildFallbackUpliftMatch`/`buildMergedMatches` **deleted** (the minted `fallback-deterministic-boosts` identity), 3 call sites simplified, 5 dead imports removed; **`client/src/lib/nutrition-boosts.ts` DELETED** (193 lines, 0 importers) — **deleted rather than recorded as a known orphan, because the adoption register's own note says its entries are *"defects, not exemptions… The gate fails on a NEW one"*** and registering an orphan I'd just created would use the escape hatch to defeat the gate. **VERIFIED:** new suite **24/0** (pure); **E2E on 5097** (concurrent 5000 untouched) — phantom **400** · invented id **400** · real-but-unreviewed `draft-omelette-rule` **400** · real rule + unauthored `double cream` **400** · **★ legitimate accept 201 while the caller shouted "Doctor-approved miracle cure / 700 kg / clinically proven to prevent disease" and the row recorded "½ tsp" in the reviewer's own words** · 4 rejections mutated nothing · phantom vs a system meal did **not** fork (889→889). **GATE MOVEMENT: `verify:publication` 24 pass/12 fail → 26 pass/11 fail** — `up-client-publisher` FAIL→**PASS**, `up-rival-display` WARN→**PASS**; **two green, none red**. Regressions **311/0** (uplift-engine 45/0, uplift-persistence 39/0); typecheck **304→304**; `adoption:check` back to baseline **64/2**. **DB restored to its exact pre-SEC4 state** — found and removed **2 `test_rule_boundary` rows** the unregistered diagnostic script left behind, plus my own E2E residue. **REPORTED, NOT ABSORBED:** the **8 historical phantom rows stay** → domain still 🔴 — **deleting them would strand 8 real ingredients in real meals** (the row *is* the undo path: `DELETE /api/uplift/applications/:id` reads `application.ingredient`); a product/retention decision, and **no new such row can ever be created** — the red is archaeology, not a leak. **Register D18 NOT amended** — it contests "Nutrition Boost Display" against `nutrition-benefit-library.ts`, **a file deleted long ago**, and is silent on the vocabulary actually retired: **CONV1 `DOC-1`'s scope is bigger than the diet columns.** Households see **fewer** boost suggestions — they were never reviewed and were shown as though they were (Principle 6); LIFE2's trade in another domain. **Net −169 lines. No schema, no migration, no architecture.** Report: `docs/implementation/platform/SEC4_UPLIFT_RULE_PROVENANCE.md`. **Next CONV1: `SEC-2`+`SEC-3`** (auth token timestamps, one change, graded **latent**) — **that closes Tier 0**; then **`DOC-1`**, and **`OWN-1` must not start until it lands**. | [`runs/SEC4_Uplift_Rule_Provenance.md`](./runs/SEC4_Uplift_Rule_Provenance.md) |
| `HOUSE4_Orchard_House_Blueprint` | Waiting for User | `rollback/HOUSE4-orchard-house-blueprint-20260716` → `7d1dd2ce` | Investigation delivered (`docs/investigations/ux/HOUSE4_ORCHARD_HOUSE_BLUEPRINT.md`). The whole house read against the Experience canon + the five room/journey blueprints filed the same day. **NORTH2 gate: 5 of 8 deliverables owned with no remainder** — the deliverable is composition + citation, plus three real gaps. **HEADLINE: the Orchard House is completely specified room-by-room and completely unspecified *between* the rooms, and every word for the difference is already spent.** `BLUEPRINT § 5.1`'s map has **six columns and no adjacency column** — a list of rooms, not a plan of a house. **The hall chain is broken *today*:** `BLUEPRINT:224` names E0 *"Lit from the hall"*, `:181` calls Admin *"the study off the hall"*, and `OHDB:302` cites *"(Experience Language § 5.9; Experience Blueprint § 5.1)"* for it — **EXPLANG contains "hall" zero times** and **§ 5.1's only hall is the four words OHDB is citing it for**; the only description is `OLB:98`, the book that *"legislates nothing"*. **The vocabulary is spent four for four**: *doorway* = the one primary action (`TRANSLATION1:148`), *corridor* = only ever a failure, *threshold* = the top of one surface, *hall* = undefined. `BLUEPRINT:138` declares *"a room, a doorway, or a note"* and **never says what a doorway connects**; `EXP ARCH:178`'s *"shortest honest path wins"* is **in force and uncheckable** (no graph). **§ 9: the Experience Test rejects a new room by existing** — and `ORCH1` already routed **Community** to that gate. **§ 11 (the strongest section, written entirely from the siblings' findings): four shapes, eleven presentations, zero visible from inside the reporting room** — `card.tsx:12`'s bleed (3 rooms), the fabricated zero (2), the last inch (3), the private book (3 on one line of scope); `SHOP2:351` — *"neither investigation could see it alone"*. **§ 12: the perimeter is drawn three times in three documents and nowhere as one thing.** Gaps **named and routed, not filled**. **§ 15.7 recommends AGAINST promotion** (`ORCH1` rec 5's reasoning, harder). Nothing implemented; 7 governing docs byte-untouched. | [`runs/HOUSE4_Orchard_House_Blueprint.md`](./runs/HOUSE4_Orchard_House_Blueprint.md) |
| `SEC23_Auth_Token_Expiry_Timestamptz` | Complete | `rollback/SEC23-auth-token-expiry-timestamptz-20260716` → `7d1dd2ce` | **None — complete. ★ TIER 0 IS COMPLETE.** Implemented **CONV1 `SEC-2` + `SEC-3`** (origin TIME2 § 9.4 #11/#12). **⚠️ HEADLINE — CONV1's PREMISE WAS WRONG, IN BOTH DIRECTIONS, AND THIS REPORT CORRECTS IT.** Probed the DB before touching anything: **both columns were already naive and schema.ts declared both naive — they AGREED.** CONV1 claimed declaration ≠ physical (SEC-2) and that the two siblings had *different* physical types (SEC-3). **Both false.** **Why: I verified the 2026-02-27 migration *says* `TIMESTAMPTZ` and never checked the column it produced — the same class of error I caught a subagent making, one file further along the chain.** **WHAT ACTUALLY HAPPENED IS WORSE:** that migration **is recorded as applied and did nothing** — `drizzle-kit push` created the column first, and **`ADD COLUMN IF NOT EXISTS` silently succeeds against an existing column of the wrong type**. **So the physical type of a password-reset expiry is decided by a push-vs-migration race, can differ per environment, and NO gate can see it** — `verify:schema-coverage` is table-level and admits it in its own closing line. **THE CONVERGENCE WAS REAL BUT POINTED THE OTHER WAY:** not the columns toward each other, but **both → `TIMESTAMPTZ`**, because a token expiry is an **INSTANT** (Household Time § 7's own test) and a naive column has no instant in it. **THE FIX:** 2 declaration flags + **1 appended, guarded, reviewed migration** (`2026-07-16_sec23_auth_token_expiry_timestamptz`) — `ALTER COLUMN … TYPE TIMESTAMPTZ USING … AT TIME ZONE 'UTC'`, **fired only on what is still naive**. **The guard is the whole engineering:** where the migration won the race the column is *already* timestamptz, and `AT TIME ZONE 'UTC'` on a timestamptz returns a **naive** value that would **silently shift every live token** — so one migration is correct in both environments the race created. **★ THE TEST CORRECTED ME TWICE:** v1 asserted a naive column drifts by the process offset → **FAILED**, because a JS round-trip is **symmetric and cancels**; **the defect needs TWO frames** (writer's TZ vs reader's) — which is exactly a SQL `> now()`. Rewritten, Case 3 spawns a child under **`TZ=America/New_York`** and asks **Postgres**: a token with an hour left is stored naive as `13:27` and judged **ALREADY EXPIRED, 4h early** (lockout west of UTC; **an extended password-reset window east of it**) — timestamptz judges it correctly. And v1 of the value check reported a 1s shift that was **my own `::bigint` rounding of `.89`**, not the data. **Both recorded, not quietly re-run.** **CONVERSION LOSSLESS:** the 1 row went `2026-05-09 19:23:06.89` → `…+00`, epoch `1778354586.89` both sides; **0 reset tokens existed**. **Idempotent** (re-run shifts nothing; boot = *"Up to date · Schema at head"*). **E2E on 5096** (concurrent 5000 untouched): real `storage.setPasswordResetToken` + `POST /api/reset-password` — **live token 200, expired 400** ("Reset link has expired") — **7/0**. Suite **15/0**; regressions **231/0** incl. **TRUST1-O8 schema protection 33/0** (the gate guarding schema.ts); typecheck **304→304**; adoption **64/2**; publication unchanged; **`drizzle-kit check` → "Everything's fine"**. **`auth.ts`/`storage.ts` UNTOUCHED — auth behaviour preserved by not touching it.** `schema.ts` = **+2/−2, only the 2 columns**; `runner.ts` = **pure append, 0 deleted lines**. **REPORTED, NOT FIXED:** `schema_migrations.applied_at` is itself naive; the runner's docblock **recommends the pattern that caused this**; `verify:schema-coverage` cannot see column-level drift (**CONV1 `SCH-3`**). **★ TIER 0 COMPLETE — `SEC-1` ✅ `SEC-4` ✅ `SEC-2`+`SEC-3` ✅ · `SEC-5` ⏳ routed, not closed (legal/product; blocks nothing else).** Report: `docs/implementation/platform/SEC23_AUTH_TOKEN_EXPIRY_TIMESTAMPTZ.md`. **Next: `DOC-1`** (P1, free, docs-only) — **Principles names the rival `household_eaters`, the Register names it `user_preferences`; they are not arguing about the same thing.** **`DOC-1`'s work-list is CPI1 § 4.4, not CONV1's row alone** — SEC-4 found a 2nd instance (D18 contests an already-deleted file), SEC-2/3 a 3rd (**CONV1's own text**). **`OWN-1` must not start until `DOC-1` lands.** | [`runs/SEC23_Auth_Token_Expiry_Timestamptz.md`](./runs/SEC23_Auth_Token_Expiry_Timestamptz.md) |
| `HOUSE5_Orchard_House_Concept` | Waiting for User | `rollback/HOUSE5-orchard-house-concept-20260716` → `7d1dd2ce` | Delivered — **the Orchard House visual concept**, built from the Experience canon + **HOUSE4**. **The deliverable is a board, not a document**: `docs/ui-audit/house5-orchard-house/HOUSE5_orchard_house_concept.html` — a **10-sheet architect's concept drawing set** (`A-000`→`A-900`), published private: https://claude.ai/code/artifact/94429b72-6dd9-45bb-91d1-8ffd972fab25 · written half at `docs/investigations/ux/HOUSE5_ORCHARD_HOUSE_CONCEPT.md`. **HEADLINE: THA already has a photorealistic visual North Star, and its own author wrote that it must never be built — so the decisive question was not what to draw but what kind of drawing is safe to make.** `NORTH1:16`, about its own render: *"**it must never be built**… it earns its warmth by exactly the means the canon forbids — a drawn kitchen, painted props, an aphorism on the wall. Its power and its danger are the same property: **it is so persuasive that a team will copy the picture instead of extracting the principle**, and copying it produces *the theme park* — **the single most-forbidden thing in the entire document set**."* **The predicted failure has a DATE:** `PLAN1 § 0` found the render's noun arriving in a mission brief **one day later**, attached to a room — *"the first live instance of the risk NORTH1 predicted"*. **⇒ THE FORM OF THE DELIVERABLE IS THE FINDING:** a render invites copying; **a plan cannot be copied** — nobody ships a floor plan, nobody pastes a hatch into a component. **An architect's drawing set is the one visual form that forces extraction of the principle, because the picture is unusable on purpose** — and *"architect's presentation board"* is **exactly what the mission asked for**. ***The mission's own words are the safe answer to the mission's own risk.*** Confirmed by `BLUEPRINT:167` (*"The analogies are **feelings to design toward, never pictures to draw**"*) and `:439` (*"the moment it becomes a picture of a table, the place has become a costume"*). **THE ONE NEW OBSERVATION, AND THE DRAWING PRODUCED IT (§ 4):** `E0` = *"lit from the hall"* ⇒ **a room lit from the hall has no window of its own**, so on any true floor plan **Admin must be drawn with NO external wall** — the only room in the house without one — borrowing its only light from the void. ***Prose can hold that as an inconsistency; a plan cannot — you cannot leave a wall undecided.*** Three consequences, all the canon's: Admin's light source is the one space no document defines · **E0 is not "no orchard" but *borrowed* orchard** · **E3/E2/E1 each describe a room's own aperture; E0 alone describes a relationship to a neighbour.** Recorded as observation — `HOUSE4 § 5.6` forbids amending E0 and this does not. **THE CENTRAL PLATE (A-101) STAMPS THE CIRCULATION `NIC — NOT IN CONTRACT`** — a real architect's term for a zone outside anyone's scope, which is precisely the circulation's condition (**it exists** — the household walks it every time they move between the three rooms they use most; **it is depended on** — by E0, by `BLUEPRINT:138`'s doorway category, by `EXP ARCH:178`'s uncheckable *"shortest honest path"*; **nobody owns it** — and all four possible names are spent). Renders **only** `HOUSE4 § 5.5`'s verified table: solid = built at `7d1dd2ce` (the **Cookbook→Planner→Shopping spine**; **Cookbook→Shopping via the Planner's wire** = *one owner, one funnel*, the house's best join) · dashed red = absent/refused (**Shopping→Pantry**, **Planner→Diary**) · feint dashed = *"implied by three rules, owned by none"*. **Proposes no adjacency, names no circulation, invents no route** (`HOUSE4 § 5.6` inherited verbatim) — ***a line drawn between two rooms IS an adjacency claim, and a picture of a hall would be believed faster than a sentence proposing one***. **10 SHEETS, each answering one mission requirement, each citing its owner:** A-000 status/why-not-a-render · A-001 site plan (orchard + E3→E0 drawn as depth) · A-100 floor plan (10 rooms, verbatim `BLUEPRINT § 5.1`) · **A-101 adjacency** · A-200 section (**one sun, one morning**; window size = exposure) · A-300 materials (three grounds, only the middle varies) · A-400 **the Companion as a fixture symbol, not a space** · A-500 **room character schedule** · A-600 circulation · A-700 perimeter (**three doors**) · A-900 general notes + the star. **THREE DRAWING DECISIONS THAT ARE READINGS OF RULES, NOT TASTE:** the **Companion is a fixture** (`BLUEPRINT:325` — *"not a room… the person in the house"*; on a plan the thing that is in every room and is none of them **is a fixture** — *the canon's definition was already a drafting convention, just never drafted*); the **room schedule is a real architectural instrument doing real work** (`BLUEPRINT § 5.1` + `OHDB § 13` **already are** a schedule, in two documents never read as one table); **the board does not move** (`BLUEPRINT § 12.1 r5` *"Still."*) — *an architect's board does not animate, and neither does this house*. **A-500 MAKES VISIBLE WHAT NO PROSE COULD: 2 of 10 signs of life specify a finish nobody can supply** — Cookbook's *well-thumbed page* ← *"cook/plan counts"* (**no cook count in 90 tables**, `COOK1 § 13.2`); Pantry's *freshness, honestly told* ← *"item freshness data"* (**no expiry/purchase/opened date anywhere**, `PANTRY1 § 12.2`). **Found by separate investigations asking the same question of different rooms, neither looking for it** — first artefact where all ten sit in one table and the gap reads at a glance. **NOUN DEVIATION — 6th CONSECUTIVE, and the first to reach an *artefact* (§ 0.3):** *"the definitive visual North Star"* is taken **three ways** — *"Visual North Star"* = `NORTH1` (the condemned render) · *"North Star"* = `BLUEPRINT § 17`'s **Design North Star**, which is **governing** and **is a sentence** · *"definitive"* = **a status an investigation may not hold** (`README.md:4`). **Board states the star and declines to be it** (A-900): *"That sentence is the North Star, and this board is not… This set is a drawing of the house that sentence describes."* `HOUSE4 § 5.2` diagnosed the cause; **the pattern has now extended from concepts to deliverables — the house has run out of names for its own artefacts.** **FILING — NO DEVIATION, THE FIRST TIME IN SIX MISSIONS (§ 0.1):** mission said `docs/investigations/ux/` and **that path is correct as given**. *(Note, not deviation: the HTML board sits in `docs/ui-audit/` per `DESIGN1`'s precedent — investigations hold analysis, ui-audit holds visual artefacts; linked both ways.)* **ID (§ 0.2):** `HOUSE1`–`HOUSE4` all taken (`HOUSE4` = **this mission's named co-source**) ⇒ **HOUSE5**; **collision checked BEFORE cutting the tag**, so neither `HOUSE4 § 0.3`'s rename nor `SHOP2 § 0.1`'s refusal-to-rename arose. **BOARD AESTHETIC DELIBERATELY NOT THA's PALETTE (§ 2.3)** — graphite on drafting paper; **one green** (orchard only; `BLUEPRINT:191` fixes the orchard as *life*); **one red, semantic only** (conditions no document owns). *A board that looked like the product would be a render wearing a plan's clothes and would fail `BLUEPRINT:440`'s costume test from the outside.* **5th INSTANCE OF THE CANON-AND-PLATFORM-WROTE-THE-SAME-LAW-INDEPENDENTLY SHAPE (§ 6.2)** — and this one is about the board: **`NORTH1:16` arrived at `BLUEPRINT:167`'s law by violating it and noticing.** *The render is the only artefact in the set that proves the rule by being the counter-example, and it says so itself.* **NEITHER `NORTH1` NOR `HOUSE4` SUPERSEDED (§ 6.3)** — NORTH2 already adopted **1 of NORTH1's 5** principles, narrowed; NORTH1 *"remains the best evidence in the repository that the Kept Room is real and warm — it simply must not be built"*. **This board is `HOUSE4` rendered** (its § 5 void, § 6 hub, § 8.1 fixture, § 12 perimeter) **+ § 4's one new observation. Where the two differ, HOUSE4 is right and this board is a drawing.** **THE BOARD'S ONE UNRESOLVABLE TENSION, STATED ON THE BOARD (§ 6.5):** `BLUEPRINT:444` — *"**Metaphor taxing function**… **If the concept ever fights the Planner, the concept loses**"* — **binds this board too**, and A-900 says so. *The rule is right, and if this board ever argues with a room, the board loses.* **★ HONESTY CORRECTION MADE BEFORE DELIVERY:** § 7 first claimed *"ARCHITECTURE_PRINCIPLES, the SoT Register and README are byte-untouched"* — **`git status` showed all three modified** (mtimes 15:12–17:01 vs this doc's 17:02), **by concurrent sessions (`CONV1`/`SEC1`), not by me.** Rewritten to state precisely what was checked: **the 7 Experience docs + `HOUSE4` + `NORTH1` carry NO tracked modification** (verified); **this session created 2 files and edited nothing else.** *A blanket "everything is byte-untouched" would have been false; a claim about a shared working tree is only honest when it names what it actually checked.* **NOT implementation · NOT production UI · NOT governing · no rule created · no value set** (no colour/token/size/duration enters the product) · **circulation not named/drawn/proposed** (stays with `HOUSE4 § 15.4`) · **E0 not amended** · **shop door drawn, not opened** (stays with `HOUSE4 § 15.5`/`ORCH1 § 13`) · **not promotable** (`HOUSE4 § 15.7` declines its own promotion; a concept board less so). Gates: `docs/investigations/ has no loose files` **PASS**; `no duplicate documents` **PASS**. **Next: none — observations only were requested and no follow-on work is recommended or implied.** The five observations are § 6 of the concept doc. | [`runs/HOUSE5_Orchard_House_Concept.md`](./runs/HOUSE5_Orchard_House_Concept.md) |
| `DOC1_Diet_Ownership_Canon_Correction` | Waiting for User | `rollback/DOC1-architecture-conflict-resolution-20260716` → `7d1dd2ce` **+ scratchpad working-tree backup** | **None — complete.** Implemented **CONV1 `DOC-1`** (Tier 1, § 7 P1): the canon held **four positions on who owns a person's diet**, and the Register **mis-named the rival**. **Docs only — zero code, zero schema, zero runtime** (verified by `git diff`). **ROOT CAUSE, bigger than DOC-1 enumerated: Phase 3 Duplication 4 compared the wrong pair** — `users.diet*` vs **`user_preferences`** (a genuinely low-risk overlap), graded it *Conflict LOW / Launch GREEN*, and **never compared it against `household_eaters`**; Domain 7, Phase 4, Phase 5, Phase 7 and Appendix A **all inherit that comparison**. So **4 named sites → 8 corrected** across 3 docs (reported in § 3.1, not hidden): fixing the leaves and not the root would have left the register *freshly inconsistent with itself*. **The load-bearing distinction: *"still the live path"* and *"the SoT"* are not the same claim** — Phase 4 collapsed them (*"Retain… mark them as the SoT until `user_preferences` is promoted"*) and thereby contradicted Principle 2 outright. **VERIFIED, NOT INHERITED:** the bridge fires only on a `dietPattern` write while `PUT /api/user/preferences` writes `dietTypes` without touching it → **divergence reachable**. **CONV1 CORRECTED (§ 5.2):** `WRITE-1`'s *"the split-brain is live"* is **imprecise about which pair** — `:192` concerns `users.diet*` vs `household_eaters`, where *"no live split-brain"* is **still literally true** (adult rows empty + 403); the live one is vs `user_preferences.dietTypes`, on the **pattern** (soft preference), **not** the hard safety fact. **`:192` was misleading, not false** — the absence of divergence is *manufactured by locking the correct owner's write door*. Propagating CONV1 verbatim would have written a falsehood into a Principle. **CPI1 S1-1 verified CLOSED** (SEC-1) — **not** written in as live; **CPI1:263's proposed repair is superseded** (it rested on the Phase 4 ruling, hedged *"if that ruling still stands"* — it doesn't). **`household.md` cited `routes.ts:8526–8541` — now the CPV1 publication route**; line-number citations removed. **`NK1` already agreed** (`household_eaters` Authoritative) — a 4th governing doc CONV1 didn't count. Regraded **🟢 SAFE → 🟡 IMPORTANT** (CP11: 🟡 not 🔴 — plate-safety symptom closed; reported as an overrulable judgement). **Errors preserved, not erased.** **ONE-POSITION TEST: 11 sites, 4 governing docs, one owner at every one.** **`OWN-1`/`WRITE-2` CP4 precondition cleared.** Report: `docs/implementation/governance/DOC1_DIET_OWNERSHIP_CANON_CORRECTION.md`. **Next: `DOC-2`** (NK1's `age` column that never existed) → `DOC-3` → `DOC-4` → `OWN-5`; **`OWN-1` must not start until P1 lands.** | [`runs/DOC1_Diet_Ownership_Canon_Correction.md`](./runs/DOC1_Diet_Ownership_Canon_Correction.md) |
| `VIS1_Orchard_House_Visual_Design_Programme` | Waiting for User | `rollback/VIS1-orchard-house-visual-design-programme-20260716` → `7d1dd2ce` | Delivered — **the Orchard House Visual Design Programme, board set 1**: `docs/ui-audit/vis1-orchard-house-visual-concepts/` — `index.html` (A-000→A-900) + five room boards (`A-200` Entrance Hall · `A-300` Family Table · `A-400` Family Cookbook · `A-500` Honest Cupboard · `A-600` Threshold) + `shared.css`. **Rendered and verified in Chromium: 6/6 clean, zero page errors, zero failed requests, zero horizontal overflow, motion count 0, `<img>`/`<script>`/external/base64 count 0.** **THE FORM DECISION IS THE DELIVERABLE'S SPINE (A-010).** Three forms exist; two were taken. **The render** (`NORTH1`) answers *"is the Kept Room warm?"* — yes — and **its own author condemned it**: *"it must never be built… **it is so persuasive that a team will copy the picture instead of extracting the principle**"*; `HOUSE5 § 1` records the predicted failure **has a date** (`PLAN1 § 0`, one day later). **The plan** (`HOUSE5`) is uncopyable **but graphite cannot show warmth — which is exactly what this mission asked for** (atmosphere · materials · light · depth). **⇒ VIS1 is the third form: the specimen board** — *an architect who has shown the client the plans next brings out the material and light studies: real specimens, under the real light of the site, on geometry that is deliberately not the building.* Bound by two rules the board **obeys rather than promises**: **(1) nothing on it is a screen** — every figure is an instrument (specimen · section · ladder · aperture · massing), and **ground posture is drawn as MASSING, never layout**, because *a plan of the Planner's cells would BE the Planner's grid*; **(2) the board is graphite and its only warmth is a real cited specimen** — it practises the law it describes (*warmth is carried by light and material, never by ornament*), and keeps `HOUSE5 § 2.3`'s palette refusal (*"a board that looked like the product would be a render wearing a plan's clothes"*). **★ THE BOARD'S CONSTRUCTION ENFORCES THE ONE-MORNING LAW ON ITSELF:** board chrome re-themes for dark scheme; **specimens do NOT re-light** — *a swatch that re-lights for its viewer is a second sun.* **Verified live in both schemes.** **DEVIATION 1 — “the canonical UI design location” does not exist.** `REPOSITORY_CONVENTIONS.md § 3` has rows for investigations/implementation/architecture/capabilities/scripts/tooling and **no row for visual artefacts**; `docs/ui-audit/<workstream>/` has **four precedents and no rule**. Followed `HOUSE5`. **A convention with four precedents and no rule is not canonical, it is habitual.** **DEVIATION 2 — “produce a visual language every future screen can inherit” is ALREADY OWNED.** `TRANSLATION1 § 6` **is** *"THE COMPLETE VISUAL LANGUAGE OF THE HEALTHY APPLES"*, governing since 2026-07-15; under `NORTH2 § 2`'s gate authoring one would be a **restatement**, which every document in the set forbids. **The mission's own next line resolves it** (*"begin producing visual concept work that **expresses the existing architecture**"*) — the language is **expressed, never authored**. **DEVIATION 3 — “The Threshold” is a noun collision, drawn on A-600:** `SHOP2` earns it (*"Shopping is not the market. It is the threshold before it"*) but `TRANSLATION1 § 4.1` **already owns THRESHOLD as the arrival — the door IN**. One house, two thresholds, opposite directions — **the fifth spent spatial word, after `HOUSE4 § 5.2`'s four** (hall · doorway · threshold · corridor). Kept the mission's noun; **naming is not a board's to settle.** *(`Entrance Hall`, `Family Table`, `Family Cookbook`, `Honest Cupboard` are all clean — HOUSE1, OHDB § 13.2, BLUEPRINT:147, and PANTRY1's "the noun of the room is not the food, it is the honesty".)* **THE BOARD REDLINES ONLY WHAT IS ALREADY RECORDED, AND ONE THING IT COMPOSED:** the **two defects are one defect and are load-bearing on each other** (A-130) — `--card: 0 0% 100%` is **pure white, zero saturation**, the material of *every room's primary working surface*, against `TRANSLATION1 OAK § 3` (*"never a white or cool canvas"*); it does not read as clinical **only because `card.tsx:12` bleeds 18% of the orchard through it** — *the wallpaper is warming the white*. ⇒ **remove the wallpaper without warming the value and the house gets a cold plane in every room at once**, which `HOUSE4 § 13.2` calls **the one failure the canon calls unrecoverable**. **Neither finding is new** (NORTH1; NORTH2 § 6: *"the architecture is sound and does not need refining — it needs **obeying**"*); **their interlock is what the specimen board made visible, because a swatch board is the one instrument that puts the value and the bleed on the same sheet.** **LIVING DETAIL HEALTH, DRAWN PER ROOM:** Planner's *sun on today* **unimplementable** (no date column) · Cookbook's *well-thumbed page* **half-sourced and fabricating the other half** (*"Cooked N times"* from plan counts) · Pantry's *freshness* **dead** (no freshness data) · Shopping's *crossing-off* **✓ met to the word** (`line-through text-muted-foreground`, no `onSuccess` to celebrate from). **1 of 4 works.** **The board also draws what the rooms get RIGHT** — the Cookbook's clean arrival, Shopping's counts-not-bars, its two-handed list ordering — *a board that only redlines is not an honest board.* **NOT production UI · NOT React · NOT a design system · NO token, value, colour, component or route created · no rule, no second owner, no gate.** Authority: **none**. A-900 declines to be the star: *"That sentence is the North Star, and this board is not."* And it binds itself to `BLUEPRINT:444`: **"if this set ever starts arguing with a room, the board loses."** **Next: none — observations only were requested.** | [`runs/VIS1_Orchard_House_Visual_Design_Programme.md`](./runs/VIS1_Orchard_House_Visual_Design_Programme.md) |
| `OWN5_Source_Of_Truth_Register_Domains` | Waiting for User | `rollback/OWN5-source-of-truth-register-domains-20260716` → `7d1dd2ce` | **None — complete. CONV1 phase P1 is COMPLETE** (`DOC-1`·`DOC-2`·`DOC-3`·`DOC-4`·`OWN-5`; the other four verified `IMPLEMENTED`, and DOC-3's fix confirmed live at `routes.ts:11390`). Implemented **CONV1 `OWN-5`**: **Register Rule 1 breached by omission, four times.** Declared **Domain 30 Pantry State · 31 Evidence & Learning (EL1) · 32 Platform Observations (OBS1) · 33 Benchmark World**. **Docs only — 1 file, 134 insertions, ZERO deletions, zero code/schema/runtime** (verified `git diff stash@{0}`; the 234/30 figure vs HEAD is **DOC-1's uncommitted work, not this item's**). **Purely additive — nothing renumbered, so every `SoT D14`-style citation still resolves.** **A third defect class, and neither of the other two:** not *a law ahead of its platform*, not *a false inventory*, but **an absent one** — *a false claim misleads a reader who finds it; an absent one misleads a reader who goes looking, concludes there is nothing to find, and declares a second owner in good faith.* **Nothing created** (CP1): every owner already existed in code; Rule 8 **checked and not triggered**. **★ CPI1 CORRECTED — Benchmarks owns NO table.** `CPI1:173` (quoted verbatim by CONV1) says *"three live, **table-owning**, runtime-read domains"*; **all 88 `pgTable`s enumerated (none benchmark) + both benchmark trees swept for `pgTable`/`pg-core` (zero)**. Its SoT is an **authored fixture + an append-only filesystem history dir**; its only DB footprint is the `benchmark-run` kind in **Domain 32's** table. **Not pedantry — declared as table-owning, the row would have named a table that does not exist**, and a reader hunting the ten households would search the DB and find nothing. **★ EL1's shipped sole-ownership claim is FALSE, not merely unregistered** — *"No other module reads or writes these tables directly"* vs **7 direct-access sites**, incl. **`publication-register.ts:1298`: the verifier bypasses the owner it verifies.** Root cause recorded so the fix isn't guessed: **`IEvidenceLearningStore` exposes no reset method** (fixtures *cannot* comply) and **no test asserts the rule** — CP10 in miniature. **OBS1's identical claim VERIFIED TRUE** (zero access outside the store). **Citation drift: `capability-registry.ts:725` → `:711`** — third P1 item to find one. **Pantry: household-scoped on `household_id`** (`user_id` is the authoring member, *not* the access scope) and **owns no food ageing at all** — no expiry/purchase/shelf-life; only `created_at`. **Gap recorded, deliberately NOT closed** (adding a fact is a Rule 8 act) — but **pantry freshness is now unblocked**. **Refused:** fixing EL1/pantry code, updating `publication-register.ts:1289`'s now-stale known gap, the OBS 13-vs-14-kind doc drift, and the Register's stale "27 domains" DoD — **all filed as F1–F6 (§ 6.2), not silently folded in** (PKR1 R7). **§ 5 declines to call the four domains *converged*** — two carry live defects and **no gate fails on either** (CP11); the Register's FINAL QUESTION still answers **NO**, correctly. *The declaration did not create the defects; it made them visible. That is what the row is for.* Report: `docs/implementation/governance/OWN5_SOURCE_OF_TRUTH_REGISTER_DOMAINS.md`. **Next: `SEC-2`/`SEC-3`** (CONV1 § 8 — cheap, out-of-band, *latent* not exploitable, *"their mask is not a control"*); **`OWN-1` is now unblocked but is P4 and carries the programme's whole risk.** **F1 is the highest follow-on.** | [`runs/OWN5_Source_Of_Truth_Register_Domains.md`](./runs/OWN5_Source_Of_Truth_Register_Domains.md) |
| `CONV1_Phase_P1_Completion_Milestone` | Waiting for User | `rollback/CONV1-phase-p1-completion-milestone-20260716` → `7d1dd2ce` | **None — complete. CONV1 phase P1 is CLOSED**, and **P0 with it** (`SEC-1`/`SEC-4`/`SEC-2`/`SEC-3` — which **no P1 report records**). Delivered the completion milestone `docs/implementation/governance/CONV1_PHASE_P1_COMPLETION.md` — **the only file written; nothing implemented, no code/schema/architecture touched.** All five items (`DOC-1`·`DOC-2`·`DOC-3`·`DOC-4`·`OWN-5`) **re-verified against the live tree, not inherited** (`DOC-3` live at `routes.ts:11390`; `DOC-2`'s false `age` claim GONE; `OWN-5`'s Domains 30–33 present; `DOC-1`'s rival/SoT claims survive only inside correction notes). **★ READINESS: READY — with one material qualification stated first, because it is the honest headline.** `CP4` is discharged and **`OWN-1`/`WRITE-2` are unblocked** — but **P1 landed ZERO gates: 14+ corrected sites across five governing documents, not one defended by a check.** Every one could regrow tomorrow, silently. **Not hypothetical** — § 4.6 records a P1 report's own sequencing claim going stale **within a day**. That is `CP10` applied to P1 itself, and the precise current shape of `R1`/`R2`. **Measure re-run, not inherited** (`CP11`): `verify:publication` still FAIL — 22 domains (5🟢·11🟡·**6🔴**), 60 checks **25 pass·24 warn·11 fail** (P0 moved one FAIL→PASS; **P1 moved nothing and could not have — every P1 item lives in the one class the gate cannot read. That is not P1 failing; it is P1's thesis restated as a number**). Governing-document coherence: **still UNMEASURED — no gate exists.** **Scope pattern found: named ≠ required** — 3 of 5 items corrected more sites than CONV1 named, each **reporting the expansion**; **`DOC-2` is the control** (named 3, corrected 3 — when the inventory was right, the item matched it exactly). **Recommended next: P2 (`WRITE-4`+`BEH-8`)** — pure deletions, **the gate already fails on them**, addressing `R2`, *the risk already happening*; then **★ the two "Low cost" coherence checks (CONV1 § 8.2) — this milestone's strongest amendment**, since P1 just produced five items' evidence both would fire on day one. **Sequencing corrected twice:** `OWN-5` § 7's *"next: `SEC-2`/`SEC-3`"* is **stale — they are done**; and CONV1 § 7's P2 gate (reds 6→4) **contradicts its own item entries (three reds)** — P2 must **resolve by measurement, not assumption**. **`OWN-1` is legal to start — that is not the same as next** (it is P4). **DEVIATION (6th consecutive) — put to the user, NOT decided:** mission specified `docs/milestones/`, which `REPOSITORY_CONVENTIONS.md` § 2 does not own; unlike CONV1 § 0.1 it breaches **no mechanical gate**, and § 4 reserves folder creation as *"a governance decision"* — so it was asked, not taken. *Creating it would have made § 2 stale the same day — the exact defect `DOC-4` had just finished correcting.* **Refused:** inventing a convergence % (**8 of 24 is a count, not a figure** — 7 of the remainder are permanently correct), amending the canon from an implementation report, and editing the investigations (history is never edited). **To roll back: delete the one file** — a tag checkout is **neither needed nor safe** (`7d1dd2ce` predates every uncommitted P0/P1 correction). | [`runs/CONV1_Phase_P1_Completion_Milestone.md`](./runs/CONV1_Phase_P1_Completion_Milestone.md) |
| `CONV1_Phase_P2_Gate_Convergence` | Waiting for User | `rollback/CONV1-phase-p2-gate-convergence-20260716` → `7d1dd2ce` | **None — complete. CONV1 phase P2 is CLOSED.** `WRITE-4` + `BEH-8` implemented and **both § 8.2 "Low cost" coherence checks BUILT** (`npm run verify:coherence` — new gate). Report: `docs/implementation/governance/CONV1_PHASE_P2_COMPLETION.md`. **★ `verify:publication` reds 6 → 5** (Capability Registry 🔴→🟡); checks **25→31 pass · 11→8 fail**. **CONV1 § 7 demanded 6 → 4. It is 5, and is reported as 5** — exactly what P1 § 8.1 correction 2 required (*resolve by measurement, not assumption*). Meal Templates **cannot** go green in P2: `storage.ts` remains an unauthorised writer (an **ownership** decision, Rule 7) and **1,261 stub rows** remain (**destructive data surgery** on rows `meals.meal_template_id` references). **Refused the alternative — authorising the remaining writers to force 6→4 is turning a gate green by lowering it** (§ 8.3's *"defect wearing a canonical badge"*). **★ THE COHERENCE GATE IS RED ON TWO REAL DEFECTS NEITHER CONV1 NOR ANY P1 ITEM FOUND:** Domain 6 names `server/lib/dietRules.ts` — **the file is `shared/dietRules.ts`**; Domain 18 declares its owner *"Contested"* between WS0 and `nutrition-benefit-library.ts`, **which exists nowhere in the tree**. *CONV1 audited every domain by hand for three days and found neither — a gate does not get tired at Domain 6.* **36 → 2 findings:** three false-positive **classes** found by verifying every finding at source and fixed before shipping — basename shorthand (~30), **the retired rival** (Domain 22's `nutrition-variety.ts` is gone *because the architecture won* — M4), and **positional bare-ref binding** (`:8544` binds to `routes.ts` by *meaning*; a gate cannot read meaning). **Coverage surrendered deliberately each time: a false failure is paid out of the gate's credibility, a missed one out of the codebase — the first account is much smaller.** **★ CORRECTED CONV1 § 8.2's OWN CLAIM:** COH-2 would **NOT** have caught `household.md`'s `:8526–8541` — `routes.ts` has **12,869** lines, so it **resolves**; that rot was **semantic**, and no `file:line` check can see it (**`L2` stands: no gate replaces "cite the owner and the behaviour"**). **`BEH-8`'s inventory was stale, its impact exact** (`L1`/`L2` again, in the document that teaches them): registry is **24 not 25**, **`household-health` does not exist**, 6 gated out not 5 — but *"6 enrichment items + 1 guidance block"* is **precisely right** (3+2+1; `food-intelligence` carries the block). **★ TWO of the six were gated out CORRECTLY** — `administration` (admin plane) and `developer` (`availability: "never"`, TIP1 § 7 physical isolation). **The gate's `cr-domain-bridge` is OVER-BROAD: obeying it literally would make both planes Companion-reachable — a privilege escalation delivered in the name of a green check.** **DECISION PUT TO THE USER, NOT INVENTED** (7th consecutive): the Card vocabulary has 7 terms and 3 cross-cutting capabilities fit none. **User's direction — extend the vocabulary; preserve user destinations vs platform capabilities.** **The governing document then supplied the discriminator, so nothing was invented:** `THA_COMPANION_CARD_EXPERIENCE_PRINCIPLE.md` — *"Next Steps … routing to the domain's canonical landing page"* → **a Card domain IS a destination**, and the registry **already declares** `food-intelligence`/`opportunity-delivery`/`evidence-learning` as *"owns zero business-domain data"* → **no data of its own to land on, so no landing page.** One new term (**`platform`**): attributable, **never** routable. `CAPABILITY_DOMAIN` **deleted**; the registry owns `companionDomain`; **absence is now a declaration, not an omission.** **`WRITE-4`'s declared mechanism DID NOT EXIST** — no `seed:ready-meals`/`seed:food-knowledge`/`seed:pantry-knowledge`; *unwiring boot alone would not have moved the writers to the front door, it would have removed the door.* Built `run-boot-retired-seeds.ts` + 4 scripts, **after breaking a circular import** (both seeds imported `log` from `../index`, whose boot IIFE is unguarded — a CLI seed booted the whole server). **`template-migration.ts` DELETED** (its own read-port called it *"a ONE-TIME BACKFILL … not a live owner"*; it fed `meal_plan_entries`, **a store already retired**). **Destructive boot wipe removed** (nulled `image_url` on **all** system meals incl. the 500 cookbook rows it does not own). **★ Two defects found IN THE GATE ITSELF:** `pl-dead-store`'s hardcoded *"template-migration still writes it at boot"* **went false the moment the code converged** (corrected — *a gate's prose rots exactly like a document's*); and `xc-boot-publication` is a **naive source-text grep that cannot tell a call from a comment about one** — **P2's own explanatory comment kept the gate red on a file with no writers in it** (comment rewritten to name no function; the crudeness is now documented at the site that lives with it). **Also refused:** deleting `CAPABILITY_DOMAIN` without reviving the 6 dead items (**the gate goes green either way** — which is why the § 4.2 decision was escalated, not taken quietly); correcting Domain 6/18 (**governing architecture — a separate, deliberate act**, CONV1 § 9); deleting the 1,261 stub rows; re-recording the typecheck baseline. **NOT wired into `release:check`** — it is red on defects P2 may not fix, and **wiring in a red gate manufactures `R2` rather than curing it**. **Verification:** **652 assertions green** (74·35·139·323·81), 0 failed; **32 typecheck regressions exist and NONE is P2's** — *proved*, not asserted: `publication-checks.ts` is **modified by nobody** yet carries the same NEW `TS2802`; `adoption:check` **64/2 identical**; `repo-structure-verify` fails only on 2 untracked strays (`.glibcheck.txt`, `.libdirs_uxhome.txt`) from a concurrent session. **Recommended next: correct Domain 6 + Domain 18** — one-line factual corrections, **zero rule change, zero ownership change** (Domain 6's owner *is* `dietRules.ts`; only the path is wrong) — **which turn the new gate green and let it be wired in.** *A gate that ships red and stays red is `R2` reborn, in the phase built to end it.* Then **P3** (`BEH-1`·`BEH-4`·`BEH-7`). **To roll back: revert the named files individually** — a tag checkout is **neither needed nor safe** (`7d1dd2ce` predates every uncommitted P0/P1 correction); only `server/template-migration.ts` needs restoring from the tag. | [`runs/CONV1_Phase_P2_Gate_Convergence.md`](./runs/CONV1_Phase_P2_Gate_Convergence.md) |
| `CONV1_Phase_P3_Renames_And_Off_By_Ones` | Waiting for User | `rollback/CONV1-phase-p3-renames-and-off-by-ones-20260716` → `7d1dd2ce` | **None — complete. CONV1 phase P3 is CLOSED.** `BEH-1` · `BEH-4` · `BEH-7` all implemented. Report: `docs/implementation/governance/CONV1_PHASE_P3_COMPLETION.md`. **★ THE HEADLINE — CONV1 filed `BEH-1` as *"a rename"* and *"correct today … one innocent change from wrong"*. Its load-bearing claim — *"NO PRODUCTION CODE BRANCHES ON `kind`"* — is FALSE, and the innocent change had already been made.** **Four live client sites branch on it: `profile-page.tsx` draws a 👶 `<Baby />` icon beside every household member WITHOUT AN ACCOUNT and prints the raw `{eater.kind}` in a badge; `weekly-planner-page.tsx` tags them `(child)` twice.** `kind` is `row.userId != null` — *does this person have an account* — and THA holds **no age for anyone** (`SEC-5`'s legal gate), so no derivation here could be about age even in principle. **A live-in grandparent without an account was not merely *"to the Companion, a child"* — they were a BABY on the household's own Profile page.** *The gun was not waiting to be picked up; it had been fired, into the UI, and the census counted it as unloaded.* **This is the first CONV1 item whose GRADE was wrong, not just its line numbers — a census ages from the edges in: paths first, counts next, and eventually the verdict.** **★ TWO THINGS CONV1 MISSED ON `BEH-1`:** the `"guest"` rival collides with a **live, legitimate, OPPOSITE `guest`** — `GuestEater` (`shared/household-eater.ts:8`), a visitor at a single planner entry who is **not in the household at all**, stored in `planner_entries.guestEaters`; *one word, two opposite meanings, both reaching the model*. And `role: "guest"` **breached the engine's OWN WRITTEN HARD BOUNDARY** — *"No fabrication: every field in `HouseholdDiscoveryItem` comes from a stored row"* — **three lines of documentation above the code**: an account-less eater has no `household_members` row, so the role came from no row. **Vocabulary converged:** `"user"|"child"` → **`"account"|"no-account"`**; the fabricated `"guest"` retired; the retired word now **asserted absent** in tests so it cannot silently return. **★ `BEH-4` — EXECUTED, NOT REASONED:** drove the **real `seedDemoData` path against the real database** and read back the rows — **pre-fix `Sunday, Monday, Tuesday, Wednesday` → post-fix `Monday, Tuesday, Wednesday, Thursday`.** The defect was **reproduced before it was fixed**. Key space **NOT renumbered** (`HT8` — *renumbering silently rotates every planner consumer and no test would catch it*). **Corrected CONV1: it is every *DEMO/TRIAL* household, NOT *"every new THA household"*** — `seedDemoData` is reached from exactly one place, `POST /api/demo/start`; registration seeds pantry/household items and **never** calls it. Key space confirmed at the **surface**, not assumed: `weekly-planner-page.tsx:102` renders `DAY_NAMES[dayOfWeek]` with `DAY_NAMES[0] === "Sunday"`, ordered by `MONDAY_FIRST_ORDER = [1,2,3,4,5,6,0]` — **exactly `HT8`**. **★ `BEH-7` — ONE DELETION, AND A GATE PROVEN TO FIRE.** `<OrchardBackdrop />` gone from `App.tsx` (+ its import + the now-pointless `bg-background/25` scrim — a scrim exists only to soften wallpaper). **NO NEW PRINCIPLE WRITTEN** — NORTH2's refusal, CONV1 `R8`, *the canon was right and the render was wrong*. **Corrected CONV1 TWICE: there is NO PARALLAX** (the live component is 20 lines, a `fixed inset-0` div and an `<img>` — so §6.1's *"never animates"* limb was **already clean**: **one prohibition breached, not three**) **and there are THREE mount sites, not one** — the other two are `orchard-shell.tsx` (`/auth`, `/onboarding`) and `home-page.tsx` (marketing), which are **arrival, not rooms**, and Blueprint **§6.2 rule 3 expressly permits arrival at E3** (*retiring them would obey a rule that was never written*). **Recorded in the Adoption Register as concern `orchard-exposure` — and recorded as a RATCHET, because CONV1 §9's own lesson is that a record is not enough:** *"`BEH-7` proves a checklist cannot catch a component nobody re-read."* Uses the register's `retired` check + `excludePaths` to state the Blueprint's rule **mechanically** (zero `<OrchardBackdrop>` outside arrival). **Proven to fire, not assumed:** re-introducing the exact defect yields `FAIL … RETIRED predecessor is live again … 1 occurrence(s) in code: client/src/App.tsx:1` — **it names the file**. Creates **no law** (the register is *"operational, not architectural"*); the Blueprint's rule is unchanged and unrestated, merely now **checkable**. **`adoption:check` 64 → 66 pass.** **★ THE DECLARED GAP, reported not papered over:** removing the wallpaper drops rooms onto the warm canvas (`--background: 42 27% 95%`) — **which IS the Blueprint's own E1** (*"the orchard as illumination and warmth, not image"*) — so Planner/Shopping/Analyser/Household land **exactly on their governed level**. **Home (E3) and Cookbook/Pantry/Nutrition/Diary (E2) are now a DECLARED, UNBUILT gap.** *A move from a **violation** to an **under-build**: wallpaper is forbidden by name; under-exposure is a gap with a governed path.* **Building Home's E3 inside P3 was refused — it would jump the path §2.4 fixes (UIA §4 amendment, tokens by admission) in the very item whose lesson is *obey what is already written*.** **Both design decisions PUT TO THE USER, NOT INVENTED** (8th consecutive): *user's direction —* **retire the wallpaper only**, and **say what's known / drop where irrelevant** (Profile states *"No account"* because it explains the edit affordance; the Planner's two chips are **DELETED — not mislabelled but IRRELEVANT**: account backing has no bearing on whether a meal suits someone, the only question those rows ask). **Verification: 629 assertions / 11 suites, 0 failed.** `adoption:check` **64 → 66 pass**, same **2 pre-existing** fails (raw-`<button>` 539/538; orphaned `HouseholdNutritionPanel.tsx`) — **neither P3's**. **Typecheck: 32 regressions / 11 files — NOT ONE is P3's**, *proved*: the 32 span 11 files and **P3 touched none of them** (the same set P2 proved was not its own). Build passes. **★ NOT VERIFIED, AND SAID SO: `BEH-7` is a VISUAL change and was NEVER LOOKED AT** — headless Chromium cannot launch in this sandbox (`libglib-2.0.so.0: cannot open shared object file`; the tree's `.glibcheck.txt`/`.libdirs_uxhome.txt` strays are a concurrent session hitting the same wall). Source, gate and build were verified instead. **A reviewer should look at a room before this ships.** **Refused:** renumbering the `dayOfWeek` key space (`HT8`); fixing **`roleMap.get(uid) ?? "member"`** — *the same fabrication, one expression away in the same statement, against the same breached boundary* — **outside `BEH-1`, recorded as backlog rather than smuggled in**; building Home's E3; retiring arrival's orchard; **rewording** the Planner chips instead of deleting them; adopting the 2 pre-existing `adoption:check` fails or the 32 typecheck regressions (*adopting other sessions' debt makes P3's own measure unreadable*). **★ RECOMMENDED NEXT — correct Domain 6 + Domain 18, NOW URGENT, AHEAD OF P4.** P2 recommended it and it did not happen; **since then a concurrent session WIRED `verify:coherence` INTO `release:check`** (`package.json:83`) — **exactly what P2 declined to do while it was red.** Two one-line factual corrections (Domain 6 names `server/lib/dietRules.ts`; the file is `shared/dietRules.ts` — Domain 18 declares a contest with `nutrition-benefit-library.ts`, **which exists nowhere**), **zero rule change, zero ownership change**, now stand between THA and **a release gate that is red for reasons unrelated to whatever is being released**. **`R2` has graduated from a risk into the release path.** Then **P4 — The Household Person**, which `BEH-1` has just strengthened the case for (`childrenCount`/`babiesCount` — *"the model is told on ~9 turns in 10 that there is a baby in the house, by a platform with no rule about babies and no age for anyone"*). **To roll back: revert the named files individually** — a tag checkout is **neither needed nor safe** (`7d1dd2ce` predates every uncommitted P0/P1/P2 correction in a tree dirty from ~9 sessions). | [`runs/CONV1_Phase_P3_Renames_And_Off_By_Ones.md`](./runs/CONV1_Phase_P3_Renames_And_Off_By_Ones.md) |
| `DOC5_Source_Of_Truth_Path_Corrections` | Complete | `rollback/DOC5-sot-register-path-corrections-20260716` → `7d1dd2ce` | **None — complete. ★ `npm run verify:coherence` is GREEN: `2 failed` → `0 failed, 0 warned`, exit 0.** The gate P2 shipped red (§ 6.4: *"wiring follows the correction"*) is corrected. Report: `docs/implementation/governance/DOC5_SOURCE_OF_TRUTH_PATH_CORRECTIONS.md`. **★ IT WENT GREEN BECAUSE THE DEFECTS WERE FIXED, NOT BECAUSE THE GATE WAS LOWERED — coverage is IDENTICAL either side: 33 domains · 102 citations · 91 tables.** *0 warns is the proof*: a `COH-1` row whose owner stops resolving **warns** (*"names no checkable artefact — prose only"*), so **both corrected rows still declare a real, resolving owner** rather than having been softened into prose the gate cannot judge — **that was the available cheat, and the gate reports it was not taken.** **P2's diagnosis was exactly right: path rot, zero ownership change.** Domain 6 `server/lib/dietRules.ts` → **`shared/dietRules.ts`** — *not a new location, but the one the architecture ordered*: **Rule 4** (*"must be moved to `shared/` and imported from there"*, naming `client/src/lib/dietRules.ts` under **"Applies immediately to"**) and `ARCHITECTURE_PRINCIPLES.md` **§ M3** (*"Move to `shared/dietRules.ts`; delete both existing files; update all imports"*) — **all three clauses satisfied in the tree**: both rivals ❌, client `meals-page.tsx:46` + server `routes.ts:54` both import `@shared/dietRules`. ***M3 was done and nobody came back to tell the Register.*** Domain 18 → **WS0 (`shared/knowledge/` → DB `knowledge_*`, read via `nutrition-knowledge-registry.ts`)** — **the replacement wording is VERBATIM Rule 1's own example of an acceptable declaration, not composed**: *when the document that owns ownership has already written the sentence, the correction is to use it.* **★ THE REGISTER WAS RECORDING A LIVE CONTEST AGAINST A FILE ITS OWN PHASE 4 HAD ALREADY CONDEMNED** (*"Retire. Superseded by WS0. Covers only 25 foods vs 188"* — under **Rule 6**, *static client files are not knowledge stores*) **and the tree had already removed** (absent from tree **and `HEAD`**; **zero consumers** of `getNutritionBenefit`/`BENEFIT_MAP`) — *five sections apart, in one document, contradicting itself, and no one noticed for months because nothing could.* **★ THE DEFECT HAD A THIRD SITE THE GATE CANNOT SEE:** Domain 6's path is stated **twice** — the domain row **and Appendix A `:1081`** (which carried the same dead path **plus a second false claim**, *"← duplicated in client"*). `COH-1` parses only `### Domain N:` blocks' Authoritative Source rows, **so Appendix A — where a second copy of every path in this document lives — is invisible to it.** Fixing only what the gate reported would have left the Register answering *"where does this fact live?"* **two ways — and a green gate would have certified it.** `DOC-1` had already set the precedent (it corrected Domain 7 **and** its Appendix A twin `:1082`); followed exactly. **★ A SECOND DEFECT FOUND, DELIBERATELY NOT FIXED:** `xc-register-currency` warns the Register *"still marks Plant Diversity Contested"* — **it does not** (Domain 22 reads *"Authoritative — published and verified"*). Its `/Plant Diversity[\s\S]{0,400}?Contested/` regex **spans 400 chars across four unrelated rows** from Appendix A `:1079` into **Domain 7's legitimately-contested row — which `DOC-1` itself wrote**. *A `DOC-1` success is being miscounted as drift.* Fixing it is **code**; **and the Register was NOT reworded to dodge it** — that is P2 § 3.6's trap taken deliberately (predicates **identical before and after**: this workstream neither caused it nor conceals it). **REFUSED — both `Status` rows left byte-unchanged though BOTH are stale:** recording a contest as over is an **ownership act**, and Domain 18's additionally needs a **live-consumer audit** — *a missing rival does not prove WS0 serves the fact today*. **Phases 3–8 NOT edited** (history + migration plans), per this document's own OWN-5 doctrine: *"the correction of record is this row"* — ***rewriting an audit to match today erases the evidence that the audit worked.*** **Verification:** `verify:publication` **identical** to P2's baseline (22: 🟢5·🟡12·🔴5; 60: 31/21/8); `repo-structure-verify` — same 2 untracked strays (`18:52`, **not this workstream's**); **diff is 1 file, 3 rows, docs only** — only two code paths read this file (**both run**), every other `server/`/`shared/` mention **verified** to be a comment citation; all three cited owners exist **at `HEAD`** with no uncommitted modifications, so **the corrections hold on a clean checkout and depend on nobody else's session**. **Next: `COH-3` — wire `verify:coherence` into `release:check`** (the item this unblocked; a one-line `package.json` change, **but a code act**). ⚠️ **Coherence can be wired ALONE** — `typecheck:ci` has 32 pre-existing regressions and `verify:publication` is red on 5 domains. Then **`DOC-6`** (the two stale Status rows — Domain 6 nearly free, Domain 18 needs the audit; **do not merge them**), the regex scope, deepening `COH-1` to Appendix A, then **P3**. **To roll back: restore one file** from the md5-verified pre-edit snapshot — *reverting restores two red `COH-1` findings and nothing else; there is nothing else to undo.* | [`runs/DOC5_Source_Of_Truth_Path_Corrections.md`](./runs/DOC5_Source_Of_Truth_Path_Corrections.md) |
| `COH3_Wire_Coherence_Into_Release_Check` | Complete | `rollback/COH3-wire-coherence-into-release-check-20260716` → `7d1dd2ce` | **None — complete. `verify:coherence` is wired into `release:check`** at **position 3**, ahead of typecheck/test/build — a **pure insertion** (no gate reordered, removed, weakened or re-baselined; valid JSON; diff = **exactly one line**). `RELEASE.md` gained **Step 0c** and `:340`'s *"Both gates run…"* → *"All three gates run…"* — ***a gate added to the pipeline and absent from the ritual is documentation rot committed by the workstream whose purpose is to detect documentation rot.*** Report: `docs/implementation/governance/COH3_WIRE_COHERENCE_INTO_RELEASE_CHECK.md`. **★ THE WIRING BUYS LESS THAN THE TICK SUGGESTS, AND IS REPORTED THAT WAY, NOT ROUNDED:** **(1)** `release:check` **never reaches the new gate today** — it dies at **gate 1** (`verify:deployment-config`) and **ALL FOUR** pre-existing gates are red; `&&` short-circuits. *The link itself was proved live*: run from position 3 the gate passes (exit 0) and hands off to `typecheck:ci`. **(2) ★ CI DOES NOT RUN `release:check` AT ALL** — `ci.yml` runs `typecheck:ci → test → build` directly, so **COH-3 adds NO enforcement on any push or PR** (`AUDIT_2026-07-13` § 81 already named it: *"CI runs 3 of the 7 gates"*). ***The mission was `release:check`, and `release:check` is a ritual a human must choose to run.*** **(3) ★★ `COH-3` CANNOT SHIP WITHOUT `DOC-5`** — at `HEAD` both corrected defects are **still present** (`:164` `server/lib/dietRules.ts`; `:302` `nutrition-benefit-library.ts`), so **this line committed ALONE turns `release:check` RED on coherence — the exact `R2` P2 § 6.4 refused, committed by the workstream that quotes it.** **ONE LANDING, OR `DOC-5` FIRST.** **Position 1 considered and REJECTED:** it is the only placement that would execute in today's tree — ***because* gate 1 is red**; that is **optimising against a temporary failure, permanently** (the chain dies at gate 1 either way, so it buys nothing and leaves a distortion once the gate is repaired). **REFUSED — did not touch `ci.yml`** (the enforcement that would actually bite, but **not this mission**, and **not free**: the required status check is matched **by JOB NAME** — `"typecheck · test · build"`, recorded in `branch-protection.json` and defended by `verify:branch-protection` — so **adding coherence makes the name a lie; renaming it strands every open PR on a check that no longer exists**); **did not fix any of the four red gates** (*"do not fix unrelated release checks"* — **fixing them to make COH-3 look green would be lowering gates to flatter a workstream**); **did not touch the coherence rules** (*deepening a check while wiring it would make a failure impossible to attribute to either act*); **did not re-record any baseline.** **Verification:** **every other gate re-run AFTER the edit reports its pre-edit numbers to the digit** — gate 1 **5 passed/1 failed** (`.replit` uncommitted), gate 2 **4/1** (2 untracked North Star `.png`s), `typecheck:ci` **baseline 168 → 304 = 32 regressions**, `adoption:check` **64/2** — **all four are concurrent sessions' uncommitted work, none is COH-3's, none was fixed**; **rules untouched verified not asserted** (`verify-governing-coherence.ts` mtime **20:24** predates the first edit; `git diff` reports the other four gate scripts **clean**); **diff is 2 files** — no server/client/shared/schema/migration file touched, and **nothing in the running application reads either**. **A pre-existing gap found and LEFT:** `adoption:check` has been in `release:check` since `PX1-W5` and appears **nowhere** in `RELEASE.md` — *the same pattern `DOC-5` found in the Register: a chain changed, and the document describing it not.* **Next: `COH-4` — run the gate in CI**, where enforcement actually happens (needs a decision on the required-check job name). **First: commit `DOC-5` + `COH-3` together — not optional.** Then `DOC-6`, the regex scope, deepening `COH-1` to Appendix A (**now that it is wired, this is when it starts to bite**), then **P3**. **To roll back: restore 2 files** from md5-verified snapshots — *one gate out of one script and one documentation section; there is nothing else to undo.* | [`runs/COH3_Wire_Coherence_Into_Release_Check.md`](./runs/COH3_Wire_Coherence_Into_Release_Check.md) |
| `COH4_Enforce_Coherence_In_CI` | Complete | `rollback/COH4-enforce-coherence-in-ci-20260716` → `7d1dd2ce` | **None — complete. ★ THE COHERENCE GATE NOW RUNS BY ITSELF** — every push (`push:` has **no branch filter**), every PR to `main`, inside job `verify` **whose name IS the required status check**. `COH-3` wired it into `release:check`, *a ritual a human must choose to run and which CI never invokes*; **this is where it finally runs without being remembered.** Report: `docs/implementation/governance/COH4_ENFORCE_COHERENCE_IN_CI.md`. **★ STOP CONDITION EVALUATED AND NOT TRIGGERED — proof is mechanical:** a required check is matched on the **JOB NAME, not its steps**; `jobs.verify.name` is **byte-identical** (`typecheck · test · build`), `branch-protection.json` **not edited**, and **`verify:branch-protection` Check 1 still PASSES**. **No rename, no branch-protection change, no human reapply.** **★ THIS WITHDRAWS `COH-3`'s OWN OVERSTATEMENT:** COH-3 § 5 claimed adding the step *"makes the name a lie"* and graded the item **Low/Medium** on it — **`ci.yml:43` had already answered it: the name is *"an API IDENTIFIER, NOT A LABEL"*.** The job has always run **9 steps and named 3** (`npm ci` and `ci:setup-db` were never in it either). ***COH-3 reasoned from a real hazard (renaming) to a wrong conclusion (that it made the work expensive) — the cheap approach was always available. `L1` applies to one's own reports too.*** **★★ IT REPORTS; IT DOES NOT BLOCK.** `verify:branch-protection` **Check 2 FAILS — *"main is NOT protected … nothing blocks an untested merge"*** — **identical before and after**, corroborated by `AUDIT_2026-07-13` § 80 (*"branch protection is not applied"*). **An agent MUST NOT fix it**: branch protection is **production configuration** and `COMMIT_PUSH_DEPLOY_PROTOCOL.md` § 3 forbids it — **a named human runs `verify:branch-protection -- --print-apply`.** ***This is not coherence's deficiency — `typecheck`, the 65 suites and all six TRUST1 security suites are enforced exactly as much: they report, and a human is trusted to look. The gate is unlatched; that is a finding about the door, not the lock.*** **★★★ `COH-4` CANNOT SHIP WITHOUT `DOC-5`, AND CI SHARPENS IT:** at `HEAD` both corrected defects remain (`:164`, `:302`), and `push:` has no branch filter — **pushing this without the Register corrections turns CI RED ON EVERY PUSH, ON EVERY BRANCH, for a defect the pusher did not introduce.** *The fastest possible way to get the gate switched off (`R2`), delivered by the workstream that exists to enforce it.* **`DOC-5` + `COH-3` + `COH-4` ARE ONE LANDING.** **Placement measured, not assumed: 676 ms**, no DB, no secret (ran with `DATABASE_URL` **unset** → exit 0; the only `pg` in the script is the `pgTable` **regex** reading `schema.ts` as text) — placed after `npm ci` (needs `tsx`), before `ci:setup-db`, **ahead of a Postgres container, a 14,065 ms typecheck, 65 suites and a bundle**. **★ A MISTAKE THIS WORKSTREAM MADE, CAUGHT, AND REPORTS:** the first protocol edit **deleted the `ci:setup-db` row** while inserting coherence — **silently removing an existing gate from the document that defines what the required check IS.** Caught by **diffing against the pre-edit snapshot**; restored **verbatim** (final diff = 1 line rewritten + 1 added; all six rows present in CI's real order). ***A workstream forbidden to lower a gate came within one unverified edit of deleting one — and the only thing that caught it was checking instead of trusting. The snapshot is why "no gate was lowered" is a measurement, not an intention.*** **REFUSED:** renaming job `verify` (**the one act that breaks the contract** — strands every open PR on a context that never reports); editing `branch-protection.json`; **applying protection** (production config, agent-forbidden); fixing Check 2; fixing the protocol's stale *"the merge button is disabled"* / *"a direct push to `main` is rejected"* (**neither is demonstrable today** — unrelated, recorded); touching the coherence rules (*deepening a check in the act of enforcing it would make a red CI impossible to attribute to either change*); fixing `typecheck:ci`'s 32 regressions (**in this job, stays red, unhidden**); adding the other 3 gates to CI (**CI now runs 4 of 7**, was 3 — real, not this mission). **Verification:** `ci.yml` **parsed with a real YAML parser** (valid; step **5 of 9**; `npm ci → verify:coherence → ci:setup-db → typecheck:ci → npm test → build`; **not `continue-on-error`, not conditional**); rules untouched (`verify-governing-coherence.ts` mtime `20:24` predates this workstream; `git diff` clean on it, the other gate scripts, and `branch-protection.json`); **no baseline re-recorded**; **diff is 2 files** — no server/client/shared/schema/migration touched. **Next: land the three together, then A NAMED HUMAN APPLIES BRANCH PROTECTION** (the highest-value item and **the only one an agent cannot attempt**). Then `DOC-6`, **deepening `COH-1` to Appendix A** (*green AND automated now — this is when it bites*), the regex scope, then **P3**. **To roll back: restore 2 files** — *one step out of one job and two documentation rows; and because the job name never changed, a rollback cannot strand the required check either.* | [`runs/COH4_Enforce_Coherence_In_CI.md`](./runs/COH4_Enforce_Coherence_In_CI.md) |
| `FI20_Food_Intelligence_Activation` | Complete — awaiting owner review | `rollback/FI20-food-intelligence-activation-20260717` → `7bfad50c` | Owner to review `docs/implementation/FI20_FOOD_INTELLIGENCE_ACTIVATION.md` & decide commit + follow-ons. **Activated COMP1 Food Comparison Engine: read-only `GET /api/foods/compare` + `/compare` page + Food-page entry. Verified (route 200 w/ cited bundle, COMP1 56/56, FI20 typecheck-clean), 4 screenshots captured. Product source +52 tracked lines + 2 new client files; no owner/schema/knowledge touched.** | [runs/FI20_Food_Intelligence_Activation.md](./runs/FI20_Food_Intelligence_Activation.md) |
| `AFI1_Ambient_Food_Intelligence` | Complete — awaiting owner review | `rollback/AFI1-ambient-food-intelligence-20260717` → `7bfad50c` | Owner to review `docs/implementation/AFI1_AMBIENT_FOOD_INTELLIGENCE.md` & decide commit + follow-ons. **Added the first new Ambient Food Intelligence capability since FI4 — one `planner-meal-uplift` recommendation ("a small lift for a meal you've already planned this week", the reusable Uplift Rules knowledge made ambient) through the EXISTING FI4/OD1/DEC1/PHASE5C pipeline (NO new pipeline, NO duplicated logic). Household-specific, cited, safety-filtered, calm (one/week). Surfaces on Planner/Home/Companion automatically + NEW scoped mount on Meal cards; Food pages already carry it via ConnectedFoodPanel. Verified: 51/51 tests, API 200 w/ cited uplift, typecheck-clean, screenshots. +226 lines across 4 files + 1 new client prop; no owner/schema/knowledge/route/capability added.** | [runs/AFI1_Ambient_Food_Intelligence.md](./runs/AFI1_Ambient_Food_Intelligence.md) |
| `MAT1_Platform_Maturity_And_Trust` | Complete — awaiting owner review | `rollback/MAT1-platform-maturity-and-trust-20260718` | Owner to review `docs/implementation/MAT1_PLATFORM_MATURITY_AND_TRUST.md` & decide commit + follow-ons. **Closed the two items AFI_VERIFY1 named as the gate before wider rollout (R2, R3) plus the CRITICAL §4.1 finding. M1: OD1 now asks producers for the CANDIDATE set, so LEARN1 ranks every observation a household generated instead of the top slice — measured on real households 57/183/65 as +20 newly eligible each, reproducing the audit's own figure. M2: the client `DOMAIN_LABEL` — the one unguarded registry of four and the last step before the household's eyes — moved to `shared/attention` and is now asserted for every domain in the ONE existing pipeline (no second runner). M3: retired the dead nutrition limb (panel + assembler + opportunity limb; dead at four layers, its route non-existent). Plus `WEEKLY_PLANT_TARGET` converged from 4 declarations to 1, and an orphaned 54-assertion suite repaired and WIRED. Verified: npm test exit 0 / 130 suites / 0 failures, new tests negative-controlled, tsc 258→252 with zero in touched files, 7/7 screenshots. Net −653 lines. Maturity 6.6 → 7.8. Open: DOCGOV1 filing gate blocks closeout (6 PRE-EXISTING loose reports, not MAT1's); top follow-on is audit §4.5 duplicate pairs, which M1 raises from latent to likely.** | [runs/MAT1_Platform_Maturity_And_Trust.md](./runs/MAT1_Platform_Maturity_And_Trust.md) |
| `INT20_Natural_Language_Action_Resolution` | Planning | `rollback/INT20-natural-language-action-resolution-20260718` | Fill in the first concrete step | [runs/INT20_Natural_Language_Action_Resolution.md](./runs/INT20_Natural_Language_Action_Resolution.md) |
| `PROD1_Product_Completion_Programme` | Complete — awaiting owner review | `rollback/PROD1-product-completion-programme-20260718` | Owner to review `docs/implementation/PROD1_PRODUCT_COMPLETION_PROGRAMME.md` & decide commit + the three commercial blockers. **Completed PX1's canonical-states rollout to the six bottom-nav rooms — the surfaces PX1 built these owners for and never reached. The defect closed: `retry:false` meant one failed request left data undefined forever, so every room took its `= []` default and rendered its EMPTY state — a server outage was presented to the household as 'you have nothing'. Worst case, now fixed and proven: a family in a supermarket whose /api/shopping-list failed was told their list was empty and invited to retype it. 5 rooms now name their own failure and offer retry; 404 rewritten out of developer language; ErrorBoundary extended over auth/onboarding/shared/lazy-chunk-404; og+title metadata (the share loop previewed as a bare URL); console dropped from the production bundle. NO new component, capability, route, entity or owner — EmptyState 3→6 importers, LoadError 9→14. Verified: acceptance 7/7 with each room's OWN api request ABORTED and assertions on rendered text; build exit 0; 0 console.* in shipped bundle; tsc 252→251 with zero in touched files. The forced-failure capture caught 4 things code review missed, incl. half the Pantry still lying and my own checker being first too narrow then too broad. One audit finding WITHDRAWN as a false positive (nav-bar TopBar has zero consumers; the real logo already went to /home) and my change to it reverted rather than claimed. Product completion ~72%: the experience is near-complete, the commercial layer is absent — no billing SDK at all, no Terms/Privacy (GDPR + App Store blocker), 3 unenforced premium limits. All evidenced, none attempted (each is new architecture, legal authorship, or a pricing decision). npm test: exit 0 — 131 suites, 0 failures. Open: DOCGOV1 filing gate blocks closeout (pre-existing).** | [runs/PROD1_Product_Completion_Programme.md](./runs/PROD1_Product_Completion_Programme.md) |
| `ENGINT1_FIX1_Boundary_Restoration` | Complete — committed, awaiting owner review | `rollback/ENGINT1-FIX1-boundary-restoration-20260718` → `729dcb91` | Owner to review `docs/implementation/engineering/ENGINT1_FIX1_BOUNDARY_RESTORATION.md`, then begin **ENGAUTO1** (now unblocked). **`session-verify.sh` had been RED since `bc360ba5` (ENGINT1) and both ENGINT1 and ENGINT2 shipped claiming verified-clean status.** ENGINT1 registered `.engineering/protocols` as an Intelligence Platform source root — which `ENGINEERING_BOUNDARIES.md:29` forbids in terms (*"must never … integrate with the Intelligence Platform"*). **Silent, not decided:** the boundary was clean at `7bfad50c` (`git grep` → empty), neither the standard nor `session-verify.sh` was amended, and ENGINT1's report never mentions it (its two "boundary" refs are the *developer-plane* §7 isolation, a different boundary). **The finding that makes it a correctness defect, not housekeeping: `.engineering/` is not an input to `script/build.ts`, so in any deployed artefact it does not exist** — the capability was reading 4 documents structurally absent from production, and its claimed "796 documents … 4 protocols" was reachable only from a source checkout. Retracted the source root and the `protocol` kind that existed solely to serve it; `classify()` now returns `null` rather than mislabelling by fallback; corrected 2 user-facing gap strings that named `.engineering/protocols/` among the places searched. **Verified: `session-verify.sh` exit 0 (green for the first time since ENGINT1) · ENGINT1 34/0 · ENGINT2 36/0, both at baseline · build succeeded · ZERO typecheck regressions introduced, proven in a clean worktree at the tag (29 there vs 20 here).** ⚠️ **Reported not fixed: `HEAD` already fails its own CI gate with 29 pre-existing typecheck regressions** — `typecheck:ci` is inside the single required check, so a PR from this branch goes red on unrelated causes. Nothing pushed; 233 uncommitted changes by others untouched. | [runs/ENGINT1_FIX1_Boundary_Restoration.md](./runs/ENGINT1_FIX1_Boundary_Restoration.md) |
| `ENGAUTO1_Engineering_Automation` | Complete — committed, awaiting owner review | `rollback/ENGAUTO1-engineering-automation-20260718` → `0eb293d7` | Owner to review `docs/implementation/engineering/ENGAUTO1_ENGINEERING_AUTOMATION.md` and settle 4 decisions. **THE FINDING: `session-verify.sh` — the verifier of the `.engineering/` boundary — was referenced by 5 documents (README:121, OPERATING_MANUAL:32, RECOVERY_PROTOCOL:121, HOUSEKEEPING:55, BOUNDARIES:72) and executed by NOTHING: no script, no hook, no CI job.** Its sibling `repo-structure-verify.sh` is run by both `session-complete.sh` and CI. That asymmetry is exactly how ENGINT1's breach shipped and survived ENGINT2 — completion checked *filing* and never checked the *boundary*. Built: `rollback-verify.sh` (NEW — asserts ROLLBACK_PROTECTION_PROTOCOL §1/§2/§6/§7 against the live dashboard) · `engineering-verify.sh` (NEW — one entry point; **explicitly NOT a deploy gate, `ci.yml` byte-untouched** per PRE_DEPLOYMENT §1) · boundary gate in `session-complete.sh` · rollback gate in `session-new.sh` (it previously accepted ANY string as a rollback tag). **Proven to refuse, not assumed:** missing/lightweight/unresolvable tags all exit 1; and in an isolated repo the boundary gate **refused the ENGINT1 breach reproduced** (`server/leak.ts`) while mutating nothing. **First live run: 98 active sessions · 420 PASS · 45 FAIL — including 12 sessions whose rollback tag DOES NOT EXIST (no protection at all), 31 lightweight (§2), 1 run-file/dashboard mismatch (§6), 1 missing run file.** 🔴 **ROOT-CAUSED the 98-session dashboard: `session-complete.sh` gates on `repo-structure-verify.sh`, which fails on 10 loose files (ENGGOV1 corrected this from 12 — the 2 README.md files are permitted) — so NO session can be completed at all, repo-wide, and they accumulate forever.** Nothing remediated — 4 decisions STOPPED AT and reported: (1) §2 requires annotated tags but 31/98 are lightweight — enforce or relax? (2) the 12 unprotected sessions **cannot** be back-tagged (a tag made today points at today, protection that looks real and is not — recommend against, same logic as HT7); (3) **file the 10 loose files — the single change that unblocks all 98 completions**; (4) `session-verify.sh` in CI? deliberately NOT done — `.engineering/` never ships, so its boundary is not a shippability question. Build not required and not run (only `.engineering/` shell + Markdown changed) — stated, not skipped. **ENGAUTO2 NOT begun, per instruction.** Nothing pushed; ~239 uncommitted changes by others untouched, incl. a concurrently-live PROD1 session. | [runs/ENGAUTO1_Engineering_Automation.md](./runs/ENGAUTO1_Engineering_Automation.md) |
| `ENGGOV1_Engineering_Governance_Restoration` | Complete — committed, awaiting owner review | `rollback/ENGGOV1-engineering-governance-restoration-20260718` → `d535e7cb` | Owner to review `docs/implementation/engineering/ENGGOV1_ENGINEERING_GOVERNANCE_RESTORATION.md`. **STOPPED AND REPORTED FIRST: all 10 loose files blocking every session completion are OTHERS' UNTRACKED work — and the same verifier run against committed state alone (clean worktree at HEAD) PASSes both checks. The repository's *committed* filing is already compliant; only the working tree is not.** `OPERATING_MANUAL` §2/§9 forbid touching it and `ROLLBACK` §3 means no tag could protect a move of untracked files — so filing was left to the files' authors by owner decision. **Delivered instead:** (1) fixed `repo-structure-verify.sh` false-positiving on `.git` inside a **linked worktree** — where `.git` is a FILE — which made it unusable in exactly the place `OPERATING_MANUAL` Step 6 sends you (*"compare against the rollback tag in a temporary worktree"*); (2) **NEW `.engineering/GOVERNANCE_EXCEPTIONS.md`** — 7 exceptions covering 45 defects across 43 sessions, owning ONLY the decision-to-accept and citing every underlying owner (verified no existing owner first: no duplicate governance); (3) `rollback-verify.sh` now honours it — **because EXC-1's 12 sessions can NEVER be repaired, so without this the gate could never go green, and PRE_DEPLOYMENT §4 records where that ends: *"a gate that cannot be satisfied is a gate that gets switched off"*.** Reports **EXC**, never PASS; prints and counts every one; **PROVEN a strict whitelist — a NEW session with the identical defect class still FAILs.** **NOTHING FABRICATED: zero historic tags created, zero lightweight tags converted** (`git tag -a -f` would stamp TODAY's date on a rollback point from days ago), **zero missing run files generated** — same logic as HT7's refusal to back-fill. **Rollback protection FAIL → PASS: 425 PASS / 45 EXC / 0 FAIL.** Live `engineering-verify` **1 of 3 → 2 of 3**; on a modelled compliant repo **3 of 3 with session completion proven end-to-end** (CURRENT→INDEX moved, marker retained). 🔴 **NEW FINDING EXC-7: 12 session run files exist ONLY in the working tree, not in git** — their dashboard rows are committed but the run files are untracked, so a fresh clone resolves the recovery link to nothing. ✅ **Corrected ENGAUTO1's loose-file miscount 12 → 10** (the 2 README.md files are permitted) in all 3 places — while verifying the ADJACENT '12 sessions with no tag' claim is CORRECT. Build not required/not run (only `.engineering/` shell + Markdown). No app file, no schema, no route; `ci.yml` byte-untouched. **ENGAUTO2 and later phases NOT begun.** | [runs/ENGGOV1_Engineering_Governance_Restoration.md](./runs/ENGGOV1_Engineering_Governance_Restoration.md) |
| `ENGPROG1_Programme_Completion_Audit` | Complete — committed, awaiting owner review | `rollback/ENGPROG1-programme-completion-20260718` → `4d666c98` | Owner to review `docs/implementation/engineering/ENGINEERING_PLATFORM_PROGRAMME_COMPLETION.md`. **Programme (ENGINT1 · ENGINT2 · ENGINT1-FIX1 · ENGAUTO1 · ENGGOV1) closed as SUBSTANTIALLY COMPLETE — 7 of 8 objectives met, 1 NOT met, and it is not claimed as fully complete.** Every compliance row re-verified BY COMMAND rather than carried over: capability count **32 → 32**, ENGINT1 added no capability id, `developer` verbs identical (`explain`/`read`/`report`), `ci.yml` and schema/migrations **untouched across the whole programme**, both suites re-run green (34/0, 36/0). **"No additional Intelligence Platform" VERIFIED not accepted** — all four locks checked independently (`availability: "never"`; not imported by `routes.ts` or `intelligence-platform.ts`; role; env flag). 🔴 **AUDIT FINDING 1 — Engineering Recoverability NOT ACHIEVED: 43 of 137 session run files are UNTRACKED**, so a third of THA's engineering history exists only on this machine and the recovery protocol's *read CURRENT.md → open the run file* resolves to nothing elsewhere. **ENGGOV1's EXC-7 recorded 12; the true figure is 43 — corrected here.** 🔴 **AUDIT FINDING 2 — ENGINT1's commit `bc360ba5` contains 620 lines of MAT1's dead-code deletion, UNREPORTED.** Substance correct (MAT1's report claims both files; live tests at `test-time3-p8-t5:303,307` enforce their retirement) but process defective: `OPERATING_MANUAL` §9 forbids committing work you did not author, and one concern per commit. **The contrast is the programme maturing** — ENGGOV1, facing the same situation, staged only its own 6-line hunk. **Debt (8 items): top is the 29 typecheck regressions at HEAD — nothing this programme built is visible to CI while the single required check is red for unrelated reasons.** 4 open governance decisions. Live `engineering-verify` **2 of 3**; 3 of 3 on a compliant repo. Report only — no app file, no script, no schema. **ENGAUTO2 / Release Intelligence / Engineering Assistant / Remote Operations NOT begun; three remain blocked by an unresolved conflict with governing law.** | [runs/ENGPROG1_Programme_Completion_Audit.md](./runs/ENGPROG1_Programme_Completion_Audit.md) |

> Multiple rows may be active at once — simultaneous Claude Code sessions are
> supported. Each session owns exactly one run file named by its Session ID, so
> concurrent sessions never write to the same file, and writes to this dashboard
> are serialised with a lock.

When a session reaches **Complete**, move its row out of this table and add a
line to [INDEX.md](./INDEX.md) — `.engineering/scripts/session-complete.sh` does
both. If no active sessions remain, set the top marker to `ESR:IDLE`.

_Last automatic heartbeat (Stop hook): 2026-07-18T11:02:28Z UTC_
