# Session: SHOP2_Orchard_Shopping_Blueprint

| Field | Value |
|---|---|
| **Session ID** | `SHOP2_Orchard_Shopping_Blueprint` |
| **Rollback ID** | `rollback/SHOP1-orchard-shopping-blueprint-20260716` → `7d1dd2ce` |
| **Start time** | 2026-07-16T00:00:00Z UTC |
| **Current stage** | Waiting for User |

## Objective
Define Shopping as **the household's market basket**, in principles, from the existing Experience architecture + the Orchard Blueprint. Eight deliverables: philosophy · feeling · how buying decisions are supported · how confidence is created · how Planner/Cookbook/Pantry feed Shopping · Companion participation · moments of delight · anti-patterns. **No screens. No implementation.** Investigation only.

## Files being modified
- `docs/investigations/ux/SHOP2_ORCHARD_SHOPPING_BLUEPRINT.md` — the deliverable (new)
- `.engineering/session/CURRENT.md` + this run file — session recovery bookkeeping

**Three deviations, all reported in the deliverable § 0:**
1. **Workstream ID (§ 0.1)** — **`SHOP1` is TAKEN**: `rollback/SHOP1-intelligent-shopping-evolution-20260712` → `docs/implementation/shopping/SHOP1_INTELLIGENT_SHOPPING_EVOLUTION.md` (2026-07-12, 442 ln, AMBER), live prior art on deliverable #3 which **names this workstream by ID** (`SHOP1:320-327`). This session = **SHOP2**. ⚠️ The rollback tag was cut before the collision was found, so it reads `rollback/SHOP1-orchard-shopping-blueprint-20260716` — unambiguous (differs by slug + date); renaming a rollback tag after the fact is worse than an untidy one. Recorded in § 0.1.
2. **Filing (§ 0.2)** — mission specified `docs/investigations/` (root); violates `REPOSITORY_CONVENTIONS.md:47` + `investigations/README.md:11`, fails `repo-structure-verify.sh`. Filed under `ux/` as PLAN1/COOK1/PANTRY1. **Competing candidate existed and rejecting it surfaced a defect**: `implementation/shopping/` exists but `investigations/README.md:18-30`'s workstream table has **no `shopping` row**, while `:12-13` claims the vocabulary *"is shared with docs/implementation/"*. **It is not.** Recorded § 12.3, not fixed. Gate re-run: **PASS**.
3. **Noun (§ 0.3) — REFUSED, both halves.** Mission says *"market basket"*. **"Market"** names a place the house does not contain — `BLUEPRINT:149` *"Shopping is **preparing to leave the house**"*; `OLB:94` chapter = *Preparing To Leave For The Shop*; the shop is **off-stage** and the next chapter is *Returning Home* — **there is no chapter at the market because THA never goes there**. **"Basket"** names a **live route** — `/basket` = `shopping-list-page.tsx`, 4,242 ln, the non-canonical twin where **every fabrication lives** and which **neither SHOP1's cards nor the sole `critical` safety signal reach**. Third meaning refused explicitly: *market basket* = an economics **measurement instrument** (`EXPLANG:294` forbids measuring). Canon's noun kept: **the list by the door**.

No other file touched. All governing docs byte-untouched (7 Experience docs, ARCHITECTURE_PRINCIPLES, PKCA, `capabilities/partners.md`, SoT Register, `investigations/README.md`). `docs/architecture/README.md` untouched. No code, no schema, no UI.

## Checkpoints
<!-- Append one line per checkpoint. Newest at the bottom. -->
- [x] Architecture Bootstrap read (`docs/architecture/README.md`)
- [x] Git status confirmed (HEAD `7d1dd2ce`); rollback tag created; session registered
- [x] `PANTRY1`/`COOK1` read as direct sibling precedents — shape, NORTH2 gate method, filing decision inherited
- [x] **Prior art found before writing** — `SHOP1_INTELLIGENT_SHOPPING_EVOLUTION.md` read in full; § 5 written to **cite, never re-derive**
- [x] **NORTH2 gate applied to all 8 deliverables — 6 owned with no remainder.** Deliverable is composition + citation, not law
- [x] **HEADLINE — the room the canon calls *"the list by the door"* does not show the list.** `shopping-workspace-page.tsx:1212-1217` defaults `mode="add"`; list gated `mode !== "add"` (`:2551`); **nav carries no `?stage=`** (`nav-bar.tsx:43/458/512` — WX10C removed it). A household with 14 items gets a **blank auto-focused textarea** (`:2340-2352`, `:1318-1320`). Against `OLB:96` (*"The list is gathering by the door"*) + `EXPLANG:408` (*"a calm, **ready list**"*)
- [x] **THE CANON NAMES THIS ROOM'S PRIMARY ACTION AND THE ROOM DOES SOMETHING ELSE** — `EXPARCH:140`: *"On a meal: cook or plan it. **On a shopping list: work through it.**"* **The only realm whose primary action the canon states in its own words**; the room opens on *add*. `EXPARCH:141`: *"A surface that cannot name its primary action is not finished being designed"* — **it was named for it, three documents up**
- [x] **THIRD INSTANCE OF THE LAST-INCH SHAPE (§ 6.2)** — `:2176-2178` computes `"14 items in list"` **on the `mode==="add"` branch**; its only container is gated `mode !== "add"` (`:2202`). **Computed and unreachable.** Joins PANTRY1's `isDefault` (computed→shipped→discarded) and Shopping's own `confidenceLevel`/`confidenceReason` (`schema.ts:201-202`, plumbed to `shopping-read-handler.ts:64/128`, **ZERO hits in all of `client/src`**). ***THA does not have a confidence problem — it has a last-inch problem***
- [x] **THE ROOM HAS TWO DOORS AND THE WRONG ONE IS UNGUARDED** — `WX10C` (2026-06-27) declared `/shopping-workspace` canonical, ticked *"No duplicate shopping workflows ✅"*, and **never mentions `shopping-list-page.tsx`** (4,242 ln, live at `/basket`+`/analyse-basket`, `App.tsx:387-388`), **linked 3× from the Dashboard** (`:394/449/671`), **advertised by the canonical page's own footer** (`:2770`), aliased in the nav pip (`nav-bar.tsx:663`). **Its compliance table is true of the nav and false of the app.** Plus a **dead link**: `:2155` `<Link href="/shopping">` labelled "Basket" → **no such route** → 404 (`App.tsx:419`)
- [x] **"INDUSTRIAL" — the fabricated verdict** (`shopping-list-page.tsx:1150`): `canShowScoreForItem` returns true for **any whole food** (`basket-item-classifier.ts:49-57`), `(item.thaRating ?? 0)` turns **null → 0** → final branch → **"Industrial" in red about an unscored banana**. Also 0-apple renders `:888/:929/:1141`. **The same file gets it right 8 lines up** (`:884`). **The server NEVER fabricates** (`routes.ts:4115` — a `0` is `skipped`, never written). **The canonical workspace pairs the same classifier with `!= null`** (`:909/912/1008`) and is correct; `ShoppingListView.tsx:1602` correct. **The fabrication is added by ONE client, at the last inch**
- [x] **FOUR-ROOM PATTERN COMPLETED (§ 11.3)** — each room fabricates in its own currency: Planner = a **zero** (*"🌱 0/30"*) · Cookbook = a **verb** (*"Cooked N times"*) · Pantry = an **inventory** (~141 items) · Shopping = a **verdict** (*"Industrial"*). **Every one is a `?? 0` or its cousin, added by a client over a server that returned an honest null**
- [x] **`SHOP1:147-150` REFUSED THE EXACT FABRICATION `:1150` COMMITS** — it declined *"treating an unrated line as a 0 to beat"*. **The rule is written, held on the server, and broken in the browser**
- [x] **THE CANON DOES NOT KNOW THA SELLS ANYTHING (§ 5.1)** — searched all 7 Experience docs (~2,600 ln) for `price|prices|pricing|money|commerce|retailer|supermarket|checkout|affiliate|revenue|monetis|monetiz` → **ZERO HITS** (only rollback footers). `cost` = attention/effort only; `buy` only in `EXPARCH:178` (*"every added tap must **buy** the person something"* — a metaphor). **Commerce appears exactly 4× and every one is a prohibition** (`EXPLANG:413/379/493`, `EXPARCH:176`) + `EXPARCH:140` (*"never 'upgrade'… whatever the business currently wishes people did more"*). **Meanwhile the room runs `POST /api/basket/checkout` (`routes.ts:829`), 9 UK retailers, real prices, per-tier totals**
- [x] **STATED PRECISELY, NOT OVERCLAIMED (§ 5.2)** — commerce **is** governed: `capabilities/partners.md` is governing architecture and is **unsparing about itself** (*"MAJOR FINDING — none of the three apiSurface routes the registry lists are what 'Partners / Supermarkets' implies"*; `recommend`/`compare` have **no safe grounded owner**). **A Capability Card governs what the *Companion* may say about retailers. No document governs what it should *feel like* to spend money in a house whose emotional constitution was written about having nothing sold to you.** **Not a defect in any doc — a question nobody has been asked.** Routed § 14.4, **not answered**
- [x] **SHOP1's grammar test is `OLB:100`'s Companion written independently 3 days earlier** — `SHOP1:75`: *"Could a kind, knowledgeable friend say this sentence to your face, about a food you just chose, **without it sounding like a correction**?"* ≡ *"the friend who catches you at the threshold"*. **4th time in this audit series the canon and an engine wrote the same law in the same voice without being introduced**
- [x] **CONFIDENCE IS CREATED BY REFUSING TO GUESS — 3 independent owners, all real gates (§ 6.1)**: `item-resolver.ts:13` (*"**Never silently force a bad category — prefer `needs_review` over a wrong guess**"*) · `apple-score-trust.ts:31-33` · `routes.ts:4341-4351` **HARD FAIL-SAFE** (*"never look up prices for unrecognised items… keyword-matching it would silently grant it a real category and **trigger fake prices**"*, w/ the "Boorboans"→"Bourbon Biscuits" example). **Three files, three registers, one doctrine: a bad guess is worse than an admitted gap** = Principle 6 arrived at independently
- [x] **The trust gate is display-layer-only** (`apple-score-trust-gate-complete.txt:8-9`) — scores **stay in the DB** (`:120-123`); 3 declared live risks (`:148-166`): autoSmp still writes them; **sort still uses the ungated `thaRating`**; `getItemThaRating()` falls through ungated. `thaRatingSource` column **"REQUIRES APPROVAL"** (`:155`). **`PKCA:169` KC8: it guards the window, not the well — and `:1150` is what walking around it looks like**. `MANUAL_OVERRIDE` unreachable (`:35-57`) — recorded, not graded (documented reservation ≠ defect)
- [x] **FEEDS: Planner→Shopping is the best engineering in the domain (§ 7.1)** — **freezer stock deducted FIRST** (`routes.ts:4170-4176`); ready meals not decomposed (`:4220-4244`); quantities **aggregate** (`ingredient-utils.ts:881/885` — 2 meals × 1 onion → **one row, qty 2**); **dedupe twice** (memory + DB on `householdId+normalizedName+unit`, `storage.ts:709-726` — re-running **increments**, never duplicates); **member swaps protected** (`:701-717` — *"a vegetarian swap never collapses into the shared ingredient line"*). **The feed is "natural" because it aggregates like a person would.** Caveat: `ingredientMatchesMeal` (`:255-279`) substring + 60% token overlap → *"onion"*/*"spring onion"* **cross-attribute**
- [x] **Cookbook has no wire — it is a CALLER of the Planner's** (`meal-detail-page.tsx:335-352`, `count:1`). Correct shape (one owner, one funnel). **But COOK1's defect reaches through it**: `from-meals` gates meals **user-scoped** (`:4168`) then writes **household-scoped** (`storage.ts:680`) → ***a member cannot generate a list from their partner's recipe***
- [x] **Shopping NEVER asks the Pantry (§ 7.3)** — zero pantry reads in `:4132-4314`; **SHOP1:419 states it as a deliberate exclusion** (*"still never consults the pantry — SHOP1 **surfaces** the duplicate, it does not **prevent** it"*). **The refusal is PRINCIPLED and must hold**: `SHOP1:129-131` declined to wire `cupboardQuantity` because *"deriving it here would **give one fact two owners**"* = Principle 2 held under pressure by an implementation that wanted the feature
- [x] **THE TWO ROOMS' DEFECTS COMPOSE — neither investigation could see it alone (§ 7.3)**: PANTRY1 found **~141 fabricated pantry items**; a generation path that deducted today's pantry would **silently remove real groceries from a real list on the authority of a hardcoded array**. ***Shopping must not read the Pantry until the Pantry is true.*** **Explicitly NOT recommended** (§ 14)
- [x] **Day-level provenance is DEAD (§ 7.4)** — schema holds `weekNumber`/`dayOfWeek`/`mealSlot` (`schema.ts:590-592`), read returns them, **UI already renders them** (`shopping-list-page.tsx:102-104`) — and **both `addIngredientSource` call sites** (`routes.ts:4236`, `:4283`) **omit all three**. *"Needed for Tuesday dinner"* is **unreachable**. Cheapest real win here
- [x] **PLAN1 § 6's GAP IS CLOSED IN THIS ROOM (§ 12.1)** — `shopping_list` is **household-scoped** (`storage.ts:678-681/2807/2842`), records **`addedByUserId` on every insert** (`:691/744`), resolves it (`:2814-2819`), **renders *"Added by Dad"*** (`shopping-list-page.tsx:98`), **and refuses to reorder under a second pair of hands** (`:2805-2806` — *"marking an item 'already_got' never changes its position"*). ***The only room of four that is household-scoped, author-aware AND concurrency-considerate.*** **The existence proof PLAN1 § 14.4 and COOK1 § 15.4 both needed — done, small, 40 lines away**
- [x] **COMPANION IS IN THE RIGHT CHAIR (§ 8.1)** — `surfaceKey="shopping"`, **`title="Worth a look before you shop"`** = `OLB:100`'s threshold in production; **the only title in the house with a deadline built into its grammar**. Self-hides (`AmbientIntelligence.tsx:105` — *"nothing to say → say nothing"*)
- [x] **THE BEST ARCHITECTURE THIS 4-ROOM AUDIT TOUCHED** — `shopping-restriction-conflict` is the platform's **sole `critical` emitter**, **structurally enforced**: `assertCriticalAllowed` **THROWS** (`shared/attention/index.ts:115-121`); *"`critical` is valuable precisely and only in proportion to how rarely it is used"* (`:80-89`); fires on **unchecked items only**, cites **2 evidence rows**, **never modifies the list** (`opportunity-engine.ts:276-318`). **Test-locked: `CRITICAL_TYPES.size === 1`**
- [x] **…AND IT IS ABSENT FROM THE OTHER DOOR (§ 8.2)** — `shopping-list-page.tsx` mounts **no `AmbientIntelligence`** → **no SHOP1 cards** (`SHOP1:428-434` names it) **and no allergen conflict card** on the surface **the Dashboard links to 3×**. **The finding to act on**
- [x] **DELIGHT: Shopping owns 1 of only 2 named qualifying moments in the whole canon** (`EXPARCH:337` — *"a first plan made, **a first list completed**"*; the Planner has the other; Cookbook/Pantry/Nutrition/Diary/Analyser have **none**) — **and the canon then forbids celebrating it** (`OLB:102`; `OHDB:268`). **Not a contradiction**: `EXPARCH:341` — *"**acknowledgement, not celebration theatre**"*. **The sharpest test of the delight law in the house**
- [x] **THE CLEANEST PASS OF THE AUDIT (§ 9.3)** — Living Detail *"Struck and settled into shade; no celebration per item"* (`BLUEPRINT:313`) met **to the word**: `:835` `line-through text-muted-foreground`; mutation `:1483-1511` has **no `onSuccess` handler at all**. **`grep confetti` across `client/src` = ZERO; no confetti library in `package.json`.** *The room could not celebrate an item if it wanted to*
- [x] **ONE LINE TO WATCH, reported precisely not inflated (§ 9.4)** — `:2686-2696` *"**Great shop** — nothing left to find"*. **Static** (no animation → doesn't touch `OHDB:268`'s *"confetti or reward animation"*; `CelebrationCard` imported **only** by the Planner). First line passes outright. **Second line is a compliment — not theatre, not acknowledgement either.** *"A household finishing a shop would recognise the milestone and would not describe it as great."* **Copy decision for a workstream, not a defect**
- [x] **ALL VIEW, NO ROOM — LIVE, IN THE DEFAULT (§ 10.3)** — `:2325-2333` gives the Add-mode surface **its own `backgroundImage: url('/orchard-bg.webp')`** + `:2336` `rgba(255,255,255,0.80)` **not theme-aware** (bright white panel in dark mode). **`BLUEPRINT:442`'s anti-pattern is *"a search box floating on a landscape"* — the room's default state IS that sentence, rendered.** And Shopping is **E1** = *"the orchard as illumination and warmth, **not image**"* (`:223`) — **it renders the image twice**. `OHDB:193` has the phrase: ***"a second orchard by the back door."*** **Shopping has built one**
- [x] **3 MATERIAL DIVERGENCES, ALL THE ROOM'S OWN CODE (§ 11.5)** — `<Card>` **imported (`:42`) and never rendered**; list is a raw div at **`bg-card/60`, NO `backdrop-blur-md`** (`:2569` — 22pts more transparent than the system's `/82`, **orchard reads through the list sharp**); full-opacity `bg-card` (`:2852/2873/2892`); + the second orchard. **PLAN1/COOK1/PANTRY1 all found ONE shared defect (`card.tsx:12`) in 4 rooms. Shopping added 3 of its own** = `BLUEPRINT:440` *the costume*
- [x] **ARRIVAL COUNT: Shopping = 0 always-on strips** (Planner 5 · Cookbook 0 · Pantry 1) — **and it scores 0 because the content is not there to bury.** *The cleanest arrival in the house is clean because it is empty*
- [x] **Shopping has NO `data-realm`** (§ 11.7) — the only realm without one; its only `data-realm` is on the **portalled** header (`workspace-header.tsx:243/483` → a **sibling** of `<main>`, not an ancestor). Reported as a **measurement** finding (forced stating the count twice: 0 inside the container / 2 platform header rows) — *a realm the DOM cannot name is one no automated Blueprint check can verify*
- [x] **`Est. total` — real numbers, dishonest framing (§ 11.4)** — server **honest** (`supermarket-basket-service.ts:212` returns `undefined` not `0`); **render is not**: a **partial sum presented as a total** (unmatched items contribute nothing, `:161-200` — 10 items, 3 priced → *"Est. total: 12.40"*), **no currency symbol**, and the sibling *"3 of 10 matched"* message **never connects to the total**. *(The canonical workspace declares `estimatedTotal` at `:1244` and **never renders it** — the honest room shows no total; the dishonest one shows a partial)*
- [x] More PASSes: **no scoreboard/progress/percentage/streak/savings** anywhere in either surface; canonical workspace has **zero framer-motion** (`/basket` **has** it — different motion policies = § 11.2 again); canonical `?? 0`s honest (`:373`, `:1559` — consumed by `> 0`; zero case says *"All scores are already up to date"*)
- [x] Deliverable written: `docs/investigations/ux/SHOP2_ORCHARD_SHOPPING_BLUEPRINT.md` (15 sections)
- [x] **Gates re-run:** `repo-structure-verify.sh` → `docs/investigations/ has no loose files` **PASS**; `no duplicate documents` **PASS**. Pre-existing unrelated FAIL on `.glibcheck.txt`/`.libdirs_uxhome.txt` persists (untracked before this session; also noted by ORCH1/PLAN1/COOK1/PANTRY1). Governing docs confirmed **byte-untouched** via `git status`

## Next action
None — delivered, awaiting review. Recommends (§ 14, by dependency):
1. **Show the household their list** (`shopping-workspace-page.tsx:1214`) — the room's missing Arrival beat, and the only item that is not a defect fix. **The one-word diff is probably wrong alone** (WX10C made `add` default deliberately when folding in Quick List; reversing it blind re-opens the duplication it closed). Needs a workstream answering *what does a household with a list see on arrival, and one without?* against `EXPLANG:408` + `BLUEPRINT:229`'s E2-when-clear rule. Free win inside it: `:2176-2178` already computes the string.
2. **Stop calling unscored food "Industrial"** (`shopping-list-page.tsx:1150`, +`:888/929/1141`) — **the fix is already written 3× in this codebase** (`:909/912/1008`, `ShoppingListView.tsx:1602`, and `:884` eight lines up). One clause on a condition. Blocked by nothing. **Do it first among the fixes.**
3. **Put the safety card on the other door — or close the door** (§ 8.2, § 11.2) + fix the `/shopping` 404 (`:2155`).
4. **Ask governance: what does money feel like in this house?** (§ 5.1) — a governance decision, not an investigation.
5. **Write the day into the provenance** (`routes.ts:4236`, `:4283`) — 2 call sites, no schema change, no design.
6. **Retire the second orchard + the 3 bespoke card materials** (`:2325-2336`, `:2569`, `:2852/2873/2892`, `:42`) — **but § 10.6: Shopping has no warmth budget; what must survive is *competence*, not warmth.**
7. **SHOP1's own follow-ons remain open, not re-derived** — client-reasoning retirement (`SHOP1:320-327`), ranking **drift** (`:329-334`), **WS9 content gap** (`:425`, 6 of 10 anchors, its *"single highest-leverage follow-up"*). ⚠️ **SHOP1 reserved the name `SHOP2` for the client-reasoning retirement; this document has taken it. Name the next workstream deliberately, not by sequence.**

**Explicitly NOT recommended:** **wiring the Pantry into list generation** (§ 7.3 — SHOP1's one-fact-two-owners refusal is correct and must hold; **and ~141 pantry items are fabricated, so deduction would remove real groceries from a real list on the authority of a hardcoded array** — *Shopping must not read the Pantry until the Pantry is true*); any Shopping visual prototype (`EXP5:951`; P1/Home has not run); changing *"Great shop"* (§ 9.4 — a copy decision, not a defect).
