# Session: BRAND1_Architectural_Branding

| Field | Value |
|---|---|
| **Session ID** | `BRAND1_Architectural_Branding` |
| **Rollback ID** | `rollback/BRAND1-architectural-branding-20260717` → `7bfad50c` |
| **WIP snapshot** | tag `brand1-wip-snapshot-7bfad50c` |
| **Start time** | 2026-07-17 |
| **Current stage** | Waiting for User |

## Objective
Explore how THA's **identity becomes part of the architecture** of the ARRIVAL1 Home — built into the
home, not placed on top of it. **Do NOT redesign the room / archway / orchard / furniture.** ARRIVAL1
is canonical and untouched. Produce 6–10 genuinely different architectural branding concepts, each
rendered on the identical room, and decide the fate of the current top banner (disappear · dissolve ·
become part of the orchard · remain in another form). Exploration only — nothing implemented.

## Outcome
10 branding concepts + 4 top-banner fates, all **rendered on the live canonical /home** by injecting
each treatment in the browser only (never editing app source). The framing finding: **THA's apple mark
already contains a sunrise-orchard-path scene — the exact image the arch shows at architectural scale**
— so the identity is already latent in the room; branding here is *revealing* it, not adding a logo.
Recommends the maker's-mark / keystone / no-banner family (identity built in, discovered not announced)
and that the top banner **remain in another form (D) — a single quiet apple, wordmark gone.**

## Next action
Awaiting owner decision on which branding concept(s) proceed. No implementation without a decision;
report is the deliverable.

## Product changed
**None.** Room byte-untouched. Added: this run file, the report, a render harness
(`scripts/capture-brand1-concepts.ts`), and `docs/ui-audit/brand1/` renders. A throwaway served asset
`client/public/_brand1-apple.png` (a copy of the apple icon, for browser-side injection) — safe to
delete; nothing imports it.
